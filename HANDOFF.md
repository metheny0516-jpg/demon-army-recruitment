# 引き継ぎメモ（Claude → CodeX）

最終更新: 2026-09-09

このファイルは開発を引き継ぐ人（AI含む）向けのメモ。
ここには「READMEに書ききれない、踏むと痛い所」と「次にやること」を書く。

読む順番:

1. **[`GAME_DESIGN_PRINCIPLES.md`](GAME_DESIGN_PRINCIPLES.md)** — 最上位の設計指針。
   何を作るか・作らないかの判断は常にここが優先する。最重要KPIは「もう1回遊びたいか」
2. **[`README.md`](README.md)** — いま何がどう実装されているか
3. このファイル — 落とし穴とバックログ

**「現状を整理して」「レビューして」と頼まれたときは、まず
[`docs/DIRECTION_BRIEF_2026-09-04.md`](docs/DIRECTION_BRIEF_2026-09-04.md) を読むこと。**
直近の変更・実測値・一度踏んだ失敗・未決の論点・分担の制約がそこに集約してある。
古いコミットや推測で判断すると、過去のレビューと同じ誤認を繰り返す。

---

## 0. 次チャットの開始点（2026-09-03）

### 現在：部門を「出撃隊」と「留守番」の二つに畳み、控えも廃止した（2026-09-10・Claude／オーナー試遊待ち）

オーナー決定（2026-09-10）：3部門（戦闘／建設／生活）は廃止。**プレイヤーの判断は「誰を戦場に出し、誰を城に残すか」の一つだけ。**
城に残った者（留守番）は職と特性で勝手に働く（食料調達・建材投入・経理・人事・改造癖の樽）。
背景は同日の設計会話：「王道の魔王軍育成ループが幹、人物の癖と小事件が周辺」「どこに置いたかより誰を採って誰と使ってきたか」。

実装：
- `departments.js`：`DEPARTMENTS` は `combat`（出撃隊）と `home`（留守番、手当半額）の二つ。`DEPARTMENT_ALIASES` で旧ID
  `construction` / `life` を `home` へ読み替える（`DEPARTMENT_ID(id)`）。`Aptitude.contribution` は留守番なら食料・建材・経理・人事の全部が効く。
- `run.js`：`departmentOf / departmentRoster / assignDepartment` は旧IDを受けても留守番として扱う。旧セーブは読み込み時に `home` へ移行。
  `processDepartments` は留守番が食料も建材も担う。改造癖の発酵も留守番にいれば起きる（生活部門限定を解除）。
- `events.js`：旧IDでの `assignDepartment(uid, "life")` などはそのまま動く（読み替え）。プレイヤー向け文言だけ「留守番」「出撃隊」に直した。
- `ui.js`：編成画面は「出撃隊」「出撃隊の控え」「留守番」の3枠。留守番の札に「城で何をしているか」を一行（`homeWork(m)`：🍲食料+3　🔨建材+2　📒給与-15%　🛢樽で何か寝かせている）。
  **落とし穴**：`UI.set()` は見出し文字列から scene クラスを推定している。見出しを変えると `.game-scene-formation` のCSSが外れて札が崩れる（first-play が落ちた）。推定条件に新見出しを足した。見出しを変えるときはここも見る。
- テスト：`test-departments`（留守番は食料も建材も出す、旧IDで聞いても留守番）、`test-department-events` / `test-labor-events`（異動先 home）、
  browser-tests `smoke`（留守番へ配属）/ `facility`（文言）。node テスト58本・browser-tests 全通過。

`node tools/sim.js 50`（＋改造癖 → 2部門）：平均クリア率 39.1 → 40.3（揺れ）。旧「三部門均衡」＝「留守番2人」は 54 → 62%、
食料不足 209 → 120回。留守番一人が食料と建材の両方を出すぶん、留守番を置く戦略が少し強くなった（意図どおり。管理が減って損をしないように）。

**控えは廃止（オーナー決定・同日）**：出撃隊（`activeUids`）に入っていない者は全員留守番。
- `departmentOf(m)` は `activeUids` から導出する。`m.department` は保存・KPI・旧コード互換の**写し**で、`syncDepartments()`（`save()` と
  `toggleDeploy / assignDepartment` の中）が揃える。直接 `m.department = ...` と書いても効かない。テストも `Game.assignDepartment` を使う。
- **落とし穴**：`departmentRoster("combat")` は「今出撃している者」になった。「出撃候補の全員」の意味で使っていた sim.js・
  test-chain-view・test-kpi・test-mercenary・test-run-records・analysis 系は `st.roster` から選ぶように直した。
- 編成画面は「出撃隊」「留守番」の2枠。ボタンは「🏰 留守番へ」「⚔ 出撃隊へ」（枠が埋まっていれば押せない）。
- 給与：出撃隊は満額、留守番は半額手当。0Gの控えは無くなったので、軍団が大きいほど固定費が増える。

`node tools/sim.js 50`（2部門＋控えあり → 控え廃止）：平均クリア率 **40.3 → 51.2**（+10.9、系統的）。
理由：sim の多くの戦略は部門を使わず余りを控え（無給・無職）に置いていた。控えが無くなり、余りが自動で食料・建材を出すため、
平均施設Lv 1.0 → 1.9、未払い発生 27% → 14%。**ゲームが易しくなった**。オーナーの「6Gのメンツで勝てる気がしない」からすると
方向は悪くないが、意図した調整ではない。締めるなら留守番の手当率（`DEPARTMENTS.home.wageRate`）か食料調達量が最初のつまみ。

**次のタスク：撤退の判断**。仕様は `docs/SPEC_RETREAT_2026-09-10.md`（実装者向け。契約・受け入れ条件・落とし穴・未決を含む）。
役割分担（オーナー決定 2026-09-10）：方針の相談と設計は設計担当（仕様書を書く）、実装は別セッション（Opus）。
実装者は仕様書と HANDOFF §0・§2 だけを読んで始める。仕様から外れる判断はコミットメッセージに書く。
その先の候補：戦闘不能とLPの二層 → 痕跡の記録。

### 現在：撤退の判断を入れた（2026-09-10・Claude／オーナー試遊待ち）

仕様は `docs/SPEC_RETREAT_2026-09-10.md`。**味方が初めて倒れたラウンドの終わりに、モルモが一度だけ
戦闘を止めて「退きますか」と聞く。** 退けば倒れた者を担いで帰れる（戦死しない）が、報酬は無い。続ければ今まで通り。
オートバトルにプレイヤーの判断が一回だけ入る。ここで作った「戦闘を途中で分岐させる形」は
後の「戦闘不能とLPの二層」でも使う。

3コミット（`run.js` と `battle.js` を同時に触らないルール）：
- **A（battle.js）**：`retreat_offer` イベントと `result.retreatOffer`。契約は §2「撤退の提案」。
- **B（run.js）**：`deploy()` を「計算」と「決着」に割った。
- **C（UI）**：戦闘を止めてボタンを2つ出す。
  - `MormoScene.aside` に `choices: [{label, value, primary}]` と `onChoose(value)` を追加。
    渡すと「戦闘を再開」ボタンの代わりに二択になる。既定フォーカスは `primary`。
  - `BattleScene.askRetreat(ev)` が提案の専用入口。`speakAside` と違って**必ず止まる**
    （字幕へ落ちると選択肢ごと消える）。直前の一言が止まっていたら `closeAside()` で先に畳む。
  - **`skip()` は答える前は提案の位置で止まる。** `skip()` は `render()` を通らない独自経路なので、
    ここへ書かないと選択ごと飛ぶ。提案を出したまま `skip()` を呼んだ場合は何もしない
    （`stop()` が一言ごと消してしまう）。
  - `BattleScene.onRetreatChoice` を `UI.battle()` が挿す。BattleScene はゲーム状態を知らないまま。
  - **`tools/browser-tests/helpers.js` の自動送りは `.mormo-aside-choice.primary` も押す。**
    そうしないと既存のテストが提案の前で止まる（casualty など「紙の前衛＋頑丈な後衛」の編成は
    そのまま提案の条件を満たす）。

`run.js` の形（Bで変わったところ）：

```
Game.deploy(options = {})        // 引数なしは今までどおり：計算して即決着（sim・テスト59本の経路）
                                 //   options.offerRetreat（UI だけが渡す）かつ retreatOffer があれば
                                 //   st.pendingBattle へ決着材料を積み、st.phase = "battle" で止まる
Game.settleBattle("continue"|"retreat")   // 保留を消費して決着。戻り値はフェーズ名。無ければ false
Game.settleContinue(pending)     // 続行。**唯一の続行経路**。deploy() の即決着もここを通る
Game.settleRetreat(pending)      // 撤退
```

- **続行の経路を二つ持たないこと。** 二つあると「テストは通るのに UI からだけ結果が違う」が起きる。
  `tools/test-retreat-run.js` の3番が、引数なし `deploy()` と `settleBattle("continue")` の
  所持金・名簿・ターン・フェーズ・勝敗・報酬の一致を検査している。
- 保留中はラン状態を**一切**動かさない（所持金・名簿・警戒度・ターン・`lastBattle` すべて）。
- **`st.phase === "battle"` のセーブは `migrateState()` が続行として決着させる**（同じ戦闘を二度見せない。
  撤退の機会はリロードで取り直せない）。
- 撤退の結末：報酬なし・略奪金貨も没収・`applyMissionOutcome` を呼ばない（征服は進まない）・
  警戒度だけ `alertDelta`（無ければ+1）・**給与は払う**（払わないと「わざと退けば給与が浮く」抜け道になる）・
  留守番の仕事とツケは今までどおり・戦功は提案時点の `contribution` で・`st.retreatCount` を+1・フェーズは `"result"`。
- **負傷 `m.injured`**（新フィールド、旧セーブは0）。担いで帰った者は `injured = 1`。
  出撃できない（`toggleDeploy` / `assignDepartment(uid,"combat")` が `false`）が留守番としては働く。
  `activeUids` からは外す。戦闘が一つ決着するたびに `recoverInjuries()` が1減らす＝**次の1戦だけ休む**。
- ラン状態へ増えたフィールド：`retreatCount` / `pendingBattle` / 名簿の `injured`（`migrateState` の defaults 済み）。

未決（仕様書7節）は全部既定で進めた：
**U1** 開幕3日間の防衛戦では提案しない（`deploy()` が `noRetreatOffer: openingBattle` を渡す）。
**U2** 最終戦でも退ける。 **U3** 略奪金貨は没収。 **U4** 負傷は次の1戦だけ。

`node tools/sim.js 50`：撤退は sim が使わない経路（`deploy()` を引数なしで呼ぶ）なので数字は動かない。
実測は下の各コミットのメッセージ。

**次にやること**：オーナーが撤退を試遊。「退くか、賭けるか」になっているか、
負けが物語になっているかを見る。その先は戦闘不能とLPの二層 → 痕跡の記録。

### 現在：本編に《酒好き》の遅刻を入れた（2026-09-09 深夜・Claude／オーナー試遊待ち）

**今日初めて本編に触った。** 隔離試作4本（勇者襲来v1→面接v2→tick→事件→10日間）の結論は
「カオス単体は主菜にならない。王道の魔王軍育成ループ（＝index.html）があってこその波乱」。
オーナー・ChatGPT・私で一致。中心＝育成＋バトル＋勢力拡大、周辺＝人物の癖＋事件。

隔離試作から持ち帰る規則は一つ：**特性は数値ではなく、出来事の順番に効かせる。**
ガロを「攻撃+60%／命中-40%」にした版は笑えず、「杯を置かずに遅刻する」版だけが笑えた（同日の実測）。

最初の一手として最小のもの：**《酒好き》を持つ味方は開戦時に不在で、1〜2ラウンド遅れて到着する。**
本編の最終決戦で、育てた古参オークが開幕にいない。「今それやる！？」が、プレイヤーが本当に大事にしている戦闘の中で起きる。

- 触った本編ファイル：`src/core/battle.js`（遅刻の本体・`onField`）、`src/ui/battle_scene.js`（2行）、
  `src/data/traits.js`（末尾追記）、`src/data/monsters.js`（オークの traitPool に1語）。`run.js` は触っていない。
- 契約の追加は §2「遅刻」に書いた（Snap.late / contribution.late / lateArrival フック / summon の late）。
- 出現率：オークの追加特性は50%で3択なので、酒好きのオークは応募者の**約17%**。fixed にはしていない。

`node tools/sim.js 50` before → after（同日・同条件）：
  最強優先                44% →    48%（+4）
  ゴブリン統一              54% →    52%（-2）
  ゴブリン統一+求人           84% →    70%（-14）
  ゴブリン統一+追加採用         50% →    42%（-8）
  スライム統一              58% →    44%（-14）
  骸骨寄せ+求人              8% →    14%（+6）
  骸骨寄せ                20% →    26%（+6）
  安月給                 26% →    30%（+4）
  魔法職寄せ               56% →    54%（-2）
  精鋭3体                22% →    24%（+2）
  中盤で精鋭に転換            48% →    40%（-8）
  略奪4回→侵攻             28% →    16%（-12）
  慎重経営                28% →    28%（+0）
  三部門均衡               60% →    60%（+0）
  未払い搾取               46% →    56%（+10）
  平均 42.1% → 40.3%（-1.9）
50ランの揺れ（±10pt程度）の範囲。遅刻がバランスを壊してはいない。

検証：`node tools/test-late-arrival.js`（17件）／既存の戦闘テスト6本 OK／`tools/browser-tests/late-arrival.js`／`run-all.sh`（結果はコミットメッセージ）。

**試遊結果（2026-09-10）**：「意外性に虚を突かれた感じだが悪くない」。
指摘2点（到着時に「召喚」が出た／遅刻が明示されない）は同日に修正。台詞はオーナー案を採用。

**オーナー指示：今は広げず、"数値ではなく出来事に効く特性"を3つだけ磨く。** 酒好き・大食い・改造癖。
各特性は次の3つを遊びながら見る：
  1. 一回見たときに、何が起きたかすぐ分かるか
  2. 二回目に、また来るかもと少し期待するか
  3. ただの面倒ではなく、人物として面白いか

次の2つの仕様（オーナー案。数値補正にしない）：
- **大食い**：敵（スライム系など）を倒したとき一定確率で勝手に食べる。「……これ、食えるぞ」。
  そのせいで次の行動が1ターン遅れる。ただしスライム系の増殖を止められる。毒持ちを食べて腹を壊す、も可。
  「HP回復+10%」にはしない。
- **改造癖**：任せた施設の機能が勝手に変わる。鍛冶場→武器量産のはずが巨大兵器を作り始める／
  食堂→勝手に発酵設備を追加／訓練場→危険な障害物コース。その結果が後から戦闘中に出る。「施設能力+15%」にはしない。
  ※施設は run.js 側なので、battle.js と同時に触らないルールに注意。

**2回目の試遊（2026-09-10）**：「到着は一瞬で見えたか怪しい（一旦放置でよい）」「到着後の台詞が反映されていない気がする」
「モルモと本人の台詞は、前にやった下半分を埋める表示でいい。そのほうが目立つ」「他のも見たいからついでに実装して」。

対応（同日）：
- 到着後の台詞は出ていたが、到着イベントの尺（1.25秒）で次の攻撃の字幕に上書きされて読めなかった。
  **遅刻の三拍（モルモの不在／本人の舞台裏／到着）は全部、下半分の一言（`MormoScene.aside`）で戦闘を止めて読ませる。**
  `aside` に `speaker`（名前と履歴書絵）を足し、本人も話せるようにした。これで「一瞬」問題も消える。
- **大食漢（既存 big_eater）に「倒した敵の飯を食う」を足した。** 数値ではなく順番に効く：
  撃破直後に35%で食べ始め（1戦闘2回まで）、次の手番を一回飛ばす。食べた分だけ少し回復（15%）するが、それは結果。
  最初の一口は下半分の一言で止める。台詞は `traits.big_eater.lines.{eat, busy}`。敵側には効かない。
  ※本編の敵は全員人間で種族タグが無いため、「スライムを食って増殖を止める」は本編に接続先が無い。飯を食う形にした。

`node tools/sim.js 50`（遅刻のみ → 遅刻＋大食い）：
  最強優先 48→46
  ゴブリン統一 52→40
  ゴブリン統一+求人 70→68
  ゴブリン統一+追加採用 42→28
  スライム統一 44→52
  骸骨寄せ+求人 14→22
  骸骨寄せ 26→34
  安月給 30→22
  魔法職寄せ 54→50
  精鋭3体 24→22
  中盤で精鋭に転換 40→48
  略奪4回→侵攻 16→22
  慎重経営 28→36
  三部門均衡 60→54
  未払い搾取 56→52

**3回目の試遊（2026-09-10）**：「大食いは相手が人間なのでグロテスクに読める」「食べて何が起きたか把握しづらい」
「いろんなバリエーションを見たい」「戦闘中に止めて状況説明、これはいい」。

対応（同日）：
- 大食漢の台詞とキャプションで、食べる対象を毎回「相手の携行食（弁当・干し肉・酒）」と明示。
  一言の下に結果（次の手番は動かない／少し回復）を `note` で添える。飛んだ手番は「食事中」を本人に浮かせる。
- **改造癖（tinkerer）を追加。** 生活部門に置くと糧食が樽で発酵し、戦闘で食べた誰か（大食漢優先、アンデッド除く）が
  酔って1ラウンド遅刻する。**本人は出撃していない。** モルモの一言に「{name}殿が酔っています。{by}殿が樽で寝かせたそうデス」と
  原因を作った者の名前が出る。台詞は酒好きとは別プール（`traits.tinkerer.lines`）。戦果に `lateCause: "fermented_rations"`。
  ゾンビとコボルトの traitPool に入れた。
  **接続は2コミットに分けた**（battle.js と run.js を同時に触らないルール）：
  A = battle.js が伝票 `rations.fermentedBy / fermentedByName` を読む側（このコミット。run.js が書くまで何も起きない）。
  B = run.js の `rationContext` に、生活部門の改造癖の者を書く側（次のコミット。sim.js を流す）。
- 改造癖の建設部門版（建てているものが別物になる）は未実装。生活部門版で「任せた仕事が戦闘の順番に出る」を先に見る。

コミットB（run.js）：`deploy()` の `rationContext` に `fermentedBy / fermentedByName`（生活部門の改造癖の者）を書く。
発酵は毎回ではなく確率（`TRAITS.tinkerer.ferment.chance` = 0.4）。`node tools/test-tinkerer-run.js` で
「生活部門にいれば書かれる（確率を固定）／戦闘部門では書かれない／いなければ書かれない」を確認。

**教訓（計測で判明）**：最初は改造癖をゾンビ・コボルトの `traitPool` に入れた。すると `node tools/sim.js 50` の
平均クリア率が 39.7 → 34.5（-5.2、全戦略が下がる）。原因はプール希釈——非戦闘特性がプールに入ると再生や群れの
出現率が下がり、改造癖そのものの効果と無関係に弱くなる。**戦闘に効かない特性を traitPool に足してはいけない。**
対処：プールは元に戻し、応募者生成で独立に振る（`TRAITS.tinkerer.quirk` = ゾンビ・コボルトに 18%、既存の特性に上乗せ）。
再計測（遅刻＋大食い → ＋改造癖、独立ロール版）：
  最強優先 46→42　ゴブリン統一 40→42　ゴブリン統一+求人 68→80　ゴブリン統一+追加採用 28→40
  スライム統一 52→52　骸骨寄せ+求人 22→18　骸骨寄せ 34→32　安月給 22→22　魔法職寄せ 50→56
  精鋭3体 22→16　中盤で精鋭に転換 48→44　略奪4回→侵攻 22→12　慎重経営 36→30　三部門均衡 54→54　未払い搾取 52→46
  平均 39.7 → 39.1（-0.6、50回の揺れ ±10pt の範囲）
sim の戦略は生活部門に改造癖を置くとは限らないので、影響はほぼ揺れ。

バグ修正（オーナー試遊で発覚）：二人パーティで酒好きの遅刻と発酵が同時に出て両者不在のとき、
透明のままのオーガが敵を殴っていた。原因は「暴食の宴」（`rations.feastUid` の大食漢が1ラウンド目に追加行動）が
`alive` だけを見て `onField` を見ていなかったこと。発酵の犠牲者は大食漢優先なので、オーガ＋発酵では毎回起きる。
`battle.js` で宴の告知と追加行動の両方を離席中は出さないようにした。回帰は `tools/test-tinkerer.js` の6番。
**教訓**：戦闘中に「行動する側」を探すコードは `alive` ではなく `onField` を使う。`alive` は生死判定にだけ使う。

**次にやること**：オーナーが3特性（酒好き・大食漢・改造癖）を本編で試遊。3基準（一回で分かる／二回目に期待する／人物として面白い）で見る。
改造癖の建設部門版は未実装。

### 現在：魔王城の10日間（2026-09-09 夜・Claude／オーナー試遊待ち）

入口 **`castle-days.html`**。今日の4つの試作（勇者襲来v1→面接v2→tick→事件《スライム大増殖》）を経て、
オーナーの原点「こいつを雇ったら次どうなるんだろ／困惑して笑う」へ照準を戻した試作。

判定は三つだけ。オーナーが遊んで決める。勝率もバランスも測っていない（意図的）。
  「お前何やってんだよ（笑）」が一回出たか／「次こいつ何するんだろ」が出たか／「次の日ボタンを押してる」感覚が消えたか

自分に課した縛り（`src/prototype/castle_days.js` 冒頭にも書いた）:
1. 人物に持たせるのは特性だけ（`TRAITS`）。人物ごとの脚本は一行も書かない。役割（原因／乱入／便乗／傍観）は特性×配置で計算する
2. 割り込みは「行動」の中に落とす。「次の日」ボタンも事件ガチャも無い。毎日ひとつ行動し、その中で世界が反応する
3. 小さいことは朝の日誌に一行。状態が変わったときだけモルモが割り込む

スライムは牧場の排水から自然に増える（雇った奴が原因ではない）。雇った奴は加速も乱入も便乗もする。
「排水を止める」は戦って戻ったのを見るか研究室で調べるまで出ない（弧を飛ばせない）。
収束すると「スライムたちが労働条件について話したい」と応募してくる（指針6節）。

生ログで出た挙動（全部、規則から）: グド「餌を改良しました。愛情の配合です」／ガロ殿は今日も食堂におられました（毎日）／
ガロ「……今、行く。今……」→鎮圧に間に合わず／ガロ「……意外とうまいぞ、これ」／ヴィラ「少し分けてもらった」→3日後「研究室の水槽が夜に光る」／
翌朝「昨日12匹処分しましたヨネ？ ……今、106匹います」

道具: `node tools/days-sim.js [seed] [pasture|kitchen]` 生ログ／`tools/browser-tests/castle-days.js` 実機一周。
既存ファイルは引き続き1行も変更していない。`battle.js`/`battle_scene.js` はタイムライン契約経由で再生のみ。

既知の懸念: 朝の行動が最大11個並ぶ（ChatGPT案は3〜5）。全部状態由来だが多い。試遊で「多すぎ」なら「話す」を畳む。
10日目が空きがち。

### 現在：勇者襲来 A/B の独立試作 v2（2026-09-09・Claude／オーナー試遊待ち）

入口 **`hero-arrival.html`**（本編から完全に独立）。設計書 v0.3 は作っていない。

**v1 はオーナー試遊で否定された。** 評価は「ボタンをぽちぽちするだけ」「何のゲーム性もない」
「分岐する小説系」、続けて「**採用がメインなのに、そこの判断が弱い／強調されてないから、
適当にすすめちゃう**」。**この評価は正しい。** v1 はプレイヤーの選択が最初の4回だけで、
途中に判断が一つも無かった。仕組みの検証物であって、ゲームになっていなかった。

オーナーは「バトルやリアルタイム要素が要るのでは」とも述べたが、**そこは同意していない**。
設計書が参照する Balatro はリアルタイム要素ゼロで、違いは「毎回賭けをしていて結果が裏切る」こと。
またバトル中心は本編が既にそうで、そこから離れた経緯がある。**この論点は未決**。
オーナー判断で「もう一度だけ作り直す」を選んだのが v2。

**v2 で変えた五つ**（CHAIN・新戦闘機構は足していない）:
1. **面接形式**。応募者が一人ずつ来て、その場で採るか見送るかを答える。戻れない。
2. 履歴書を **確かなこと／本人の申告／未確認** の三段に分け、未確認の一文で賭けさせる。
3. **隠れた性質**（`HIDDEN_TRAITS`）。働かせて初めて判明し、必ず数値か能力が動く。
   手掛かりは必ず本当の情報。嘘は書かない。採らなかった人の中身は明かさない。
4. **相手の条件を隠して毎ラン抽選**（任務優先35/休憩中40/気が緩み25）。
   v1 は `?cond=B` でプレイヤーが選んでいたので、酒を出せば必ず飲んだ。それが賭けを殺していた。
5. 応募者を毎ラン抽選、予算6G・枠3、近郊警戒は生還84%の博打、ラン記録が残る。

**測定**（`node tools/sim-hero-arrival.js 300`）: 安く5人83% ／ 火力1枚足して96% ／
4人に減らすと45% ／ 在籍2名のみ0%。店の賭けは 外れ68%→軽い酒77%（65%で通る）→
強い酒97%（25%でしか通らない）。**頭数が一番効く。**

**バグとして直したもの**: xorshift に小さい seed をそのまま入れていたため、
seed 1〜6 で応募者の抽選が偏り、モグが一度も出なかった。混ぜてから空回しするよう修正。

**次にやること**: オーナーの試遊（3ラン続けて）。触り方と未検証事項は
[docs/HERO_ARRIVAL_PROTOTYPE_2026-09-09.md](docs/HERO_ARRIVAL_PROTOTYPE_2026-09-09.md)。
**ここで面白くならなければ、人材・仕事を主にする路線そのものを疑う**という合意で進めている。
本編改修・全体仕様化へ自動で広げない。既存ファイルは変更していない。

**GitHub Pages で公開中**。Source は「Deploy from a branch」で
`claude/hero-arrival-tavern-prototype-uy2toh` / `(root)`。`.github/workflows/pages.yml` は
Actions 方式に切り替えた場合用に置いてあるだけで、現状は使っていない。

### 履歴：勇者襲来v0.2へ改稿（2026-09-09・CodeX／文書のみ）

オーナーは第8節の結末をどれも楽しいと評価。文章の堅さは口調・表現で調整する方針。
配置パズルへの不安を受け、[設計書](docs/HERO_ARRIVAL_VERTICAL_DESIGN_2026-09-09.md) の三点を改稿した。

1. 一マス移動とT管理を撤去。仕事の範囲から自動取次・迎撃招集を行う。情報と接触の根拠は残す。
2. プルは聖剣対策の解放キーではなく、魔力を取り込んで状態が残る人物。
   同じ摂食を戦闘外の魔力灯と聖剣へ適用する比較を用意。一般魔法迎撃や研究全体は追加しない。
3. 通常戦闘は送り出し→育成・帰還→勇者戦→戦果の体験と、既存数値での比較観点を具体化。
   CHAIN・新戦闘ギミックは追加していない。勝率・手応えは未測定。

B「酒場の客が勇者」、D「勇者が出勤」、E「門番が指示どおり働く」は維持。
**次にやること**：小さい独立試作の入口・状態・既存計算との接続を決め、通常迎撃と提供をまず触って検証する準備。
同じ六結末の好みを再度すべて聞く必要はない。本編改修・全体仕様化へ自動で広げない。
今回の作業は文書のみ。ゲームコード・数値・第2節の保存/API契約は変更なし。

### 履歴：勇者襲来の縦切り設計v0.1を作成（レビュー反映済み）

オーナーの「進めよう」を受け、[設計書](docs/HERO_ARRIVAL_VERTICAL_DESIGN_2026-09-09.md) を作成。
**文書のみ。ゲームコード・数値・保存/API契約は変更していない。試遊・勝率測定は未実施。**

- 在籍2名＋候補6名の計8名。通常迎撃、提供、会話、雇用、門番、聖剣吸収を一局面で検討。
- 接点・動機・実行可能性・権限から行動候補を作り、相手の応答と情報伝達を分けた。
- 六つの紙上進行と境界表を用意。個別イベントを六本実装する指示ではない。
- 三準備区間、接触上限、予算、酔い数値は比較用の仮設定。本編仕様には採用していない。
- 現行には聖剣の独立した物体・覚醒の供給元がない。吸収は既存処理だけでは実装できない。
- 主要懸念：移動が手順パズルになること、吸収が覚醒解除の補正に見えること、
  通常戦闘の数値校正と育成の手応えが未検証なこと。

**当時の次タスク（完了）**：オーナーが第8節を確認し、結末を評価。三点の修正をv0.2へ反映。
数値や全データ項目の承認は求めていない。今後の順序は上のv0.2項目を参照。
CHAIN追加・全体仕様化・本編実装へ自動で進まない。第2節の契約変更なし。

### 最優先：設計指針を改定。次は勇者襲来の縦切り設計（2026-09-09・CodeX）

オーナー依頼により GAME_DESIGN_PRINCIPLES.md を改定した。**文書のみ。実装・数値・保存API契約変更なし。**
主は採用・仕事・軍団から生まれる全体の面白さ。従は戦闘・CHAIN・戦闘シナジー。
普通の軍事攻略を成立させ、人材の動機・能力・接触機会等から異常が自然に派生する方向。
予定や強さ、戦闘形式、短い特殊ENDは崩れてよい。毎回の対処可能性・見せ場の補填は要求しない。
因果の信頼を守るが、因果があるだけで面白いと判断せず試遊で見る。

**完了した設計作業**：指針第19節に沿う勇者襲来一本の設計書v0.1を作成済み。上の最新項目を参照。
6〜8名程度で標準戦闘と、接点・動機・実行可能性・権限に基づく介入を同時に検討。
誰がなぜ行動するか、他状況へ再利用できるか、複数人の競合、時間、状態と因果、通常決着まで明記する。
専用イベントの大量執筆、CHAIN追加、新戦闘ギミックの追加提案、全体仕様化は保留。
時間方式・ラン長・頻度・属性スキーマ・計算式は未確定。コード着手はこの指針改定の範囲外。

**旧タスクの扱い**：下記の連鎖研修T1〜T3と、旧CHAIN中心ロードマップの「次」は保留。
新指針と第19節を読み、古い「上から着手」「設計確定」を現在の着手指示として実行しない。
旧資料・実装履歴・テスト契約は保存する。第2節のAPI・保存契約は今回変更していない。

### 保留：タイトルから遊べる「連鎖研修」1戦（2026-09-08の旧計画）

試遊で「戦闘速度が速く、連鎖の理解が追いつかない」と確認。ただし通常戦闘を一律に遅くせず、
採用2回→編成→初回CHAIN停止説明→戦果答え合わせまでを約3分で通す任意研修をタイトルから出す。
仕様は [`docs/CHAIN_TRAINING_DESIGN_2026-09-08.md`](docs/CHAIN_TRAINING_DESIGN_2026-09-08.md)。
通常ランの応募抽選・セーブ・魔界史・KPIを汚さない独立1戦とし、ゴブリン《追い剥ぎ》＋《強欲》を
研修内だけ固定する。Opusの戦闘中モルモ大表示は統合済み。旧計画はT1→T2→T3の順。**2026-09-09時点では着手保留**。

### simの未完ラン問題は解消済みと確認した（2026-09-08・CodeX／測定のみ）

バックログに残っていた「`Game.deploy()` が falsy を返して約5%が未完になる」を
`node tools/deploy-falsy-probe.js`（全15戦略×200ラン＝3,000ラン）で再確認し、**未完0件**。
ゲーム本体は変更していない。現在の `tools/sim.js` には、精鋭戦略・中盤転換戦略とも
軍が空のときは最低1名を採用して続行する対策が既に入っている。過去の約5%という記録を
現行simの母集団欠落として扱わないこと。再発時は同probeで `active_empty` と
`payroll_rejected` を切り分けてから本体を疑う。

### 死んでいた「連鎖に出遅れ」を到達可能にした（2026-09-08・CodeX）

18,000ランで成立0件だった `chain_stagefright` を、戦闘式・発生確率・ペナルティを変えずに
到達可能にした。現行で連鎖中の追加行動を得られるのは《強欲》持ちなので、対象を
「《強欲》かつ《臆病者》」へ変更。同じ追撃を先に取っていた `chain_receipt` は
「《強欲》かつ《臆病者》ではない者」とし、2事件を排他的にした。

これにより《強欲》の追撃が事件抽選に当たった場合、臆病者なら「連鎖に出遅れ」、それ以外なら
従来どおり「追撃より小銭」になる。どちらも確率0.3%、攻撃を1回休む効果は同じなので、
事件総数と戦闘上の損失は増やしていない。`tools/test-battle-happenings.js` で実際の
`追い剥ぎ → 金貨 → 強欲` の因果上に発火し、小銭事件に先取りされず、追撃が止まることを固定した。
`node tools/sim.js 50` は全15戦略で完走し、クリア率16〜68%。通常想定帯を維持した。

### 試遊フィードバック反映：モルモの一言を大胆にした（2026-09-08・Opus）

**同日・CodeXの追加調整**: オーナーの「口元が下へ寄り、頭頂の跳ね毛まで入る。跳ね毛は切って
首元を見せたい」を反映。丸枠と3.3倍ズームは維持し、画像の縦位置だけ `top: -9.4% → -30%` へ
移動した。ブラウザテストは枠高の25%以上を上へ送ることも固定し、元の中央位置へ戻る退行を防ぐ。

**同日・CodeXの確認待ち化**: さらに試遊で「出てすぐ消えるため何が起きたか分からない」と確認。
戦闘中の一言は自動消去を廃止し、表示したイベントでタイムラインを停止するよう変更した。
表示中は速度・通常停止・スキップ・結果ボタンをロックし、帯の大きな「戦闘を再開」ボタンを
押したときだけ次のイベントへ進む。全滅後は戦闘が終わっているため「戦果を確認する」と表示し、
確認後に結果ボタンを解放する。通常の「読むために停止」とは別の `mormoAwaiting` 状態で管理する。
`tools/browser-tests/mormo-aside.js` は800ms待ってもイベント番号が動かず、確認後に進むことまで固定。
全Node 53本とブラウザ52本を通過。戦闘計算・速度・出現頻度は変更していない。

オーナー試遊の当初3件のうち、見た目の2件を反映した。後続の確認待ち化でも
**ゲームの計算・速度・出現頻度・データは変更していない**。

- **「いいんだけど目立たない」** → 隅の小さな野次から**戦場の下半分を使う帯**へ。
  実測でスマホ50%・PC48%。1戦闘に1回しか出ないので、出るときは大胆に出す。
  読み終えたら確認ボタンで戦闘へ戻す（全滅時は戦果へ進む）。
- **「丸アイコンに全身は見にくい、顔だけでいい」** → 立ち絵は512x512の全身像なので、
  枠で切り抜いて顔だけを見せる。3.3倍に拡大し、左 -115% / 上 -30% へずらす。
- 狭い画面では横並びだと吹き出しが数文字で折り返すので、**縦積み**にして台詞に幅を使わせた。
- 表示中は `pointer-events: auto` で戦場操作を遮り、下の戦闘操作も無効化する。
- `z-index` を 9 → 11 へ上げた。決着表示（`.scene-result`・z-index 10・全面に薄い幕）の
  下に沈むと、**全滅の最後の一言が読めなかった**。決着の文字は画面中央、帯は下半分なので
  重ねても文字は隠さない。

**縮んで元に戻らないよう、`tools/browser-tests/mormo-aside.js` に下限を入れた**
（下側40%以上・顔110px以上・立ち絵2.5倍以上・台詞18px以上・操作を遮ること／スマホとPC）。

#### まだ直していない指摘（オーナーが「あとの課題」と明言）

> バトルスピード早くて理解はやはり追いつけない感はあるが、そのへんは
> チュートリアルとUIを徹底的に磨きこめば解消すると思うからあとの課題かな

**速度そのものを落とす対処はしないこと。** 2026-09-05 に「全段等速3秒以上」へ倒した版は
オーナーが否定しており、いまの「強弱＋各段に下限」方式へ戻した経緯がある
（設計憲法 第3節）。次に触るなら**チュートリアルと導線**が入口で、尺ではない。

### R2: 前回出撃との差分を作り、戦果の1文へ渡す（2026-09-08・Opus）

設計書 6.2 / チケットR2。これで **B1 の優先順1番目「今回変えた人の確実な働き」が動く**。
**戦闘計算・数値・ハプニング条件・演出閾値は変更していない。**

#### 契約（HANDOFF 第2節へ足す新フィールド）

```
st.lastBuildSnapshot = null | { deployed:[{uid,name,tplId,rankId,traits}], roster:[uid],
                                departments:{uid:deptId}, mercenaries:[name],
                                facility, payroll, merge, mission }
lastBattle.buildChanges = { first, hired[], deployed[], benched[], reassigned[],
                            reordered, mercenaries[], facility, payroll, merge, mission,
                            changedUids[] }
lastBattle.spotlight.changedActor = bool   // 今回動かした人が実際に働いた回だけ true
```

- **結果を一切見ない。** `buildChanges` が答えるのは「何を変えたか」だけで、
  「変えたから勝ったか」ではない（設計書6.2「効果を捏造する材料にしない」）。
  勝敗・タイムラインを参照していないことを**ソースの静的検査としてテストで固定**した。
- **比較する前が無いとき（ランの初戦・旧セーブ）は差分なし。** `first: true` を立てるだけで、
  「全部変えた」と読み替えない。
- **並び順も見る。** 顔ぶれが同じでも順番が変われば `reordered`。先頭ほど狙われるので、
  同じ5人でも別の編成である（V0で踏んだ「配置で結果が割れるのに画面に出ない」の続き）。
- **再起では比較の基準もチェックポイントへ戻る。** `st` の中にあるので自動的にそうなるが、
  テストで固定した。

#### KPI との関係（設計書の「KPIの比較処理を再利用できるか確認」への答え）

**流用しない。** `KPI.fingerprint()` は「何か変わったか」の真偽を返す文字列指紋で、
**誰が変わったかを持たない**。目的が違う。ただし**見る次元は揃えた**
（出撃隊・配属・傭兵・施設・給与方針・合体・作戦）。片方だけ次元が増えると
「試行として数えたのに差分は空」という食い違いが静かに生まれるので、
**両者が同じ判定を返すことをテストで固定**した。次にどちらかを触る人は両方を直すこと。

#### 戦闘中IDと永続uidの橋渡し

`Spotlight.of(timeline, { highlightIds })` に**戦闘中ID**を渡す。対応表は
`simulate()` が id を埋めたあとの `playerUnits` から run.js が作る。
**battle.js の `battle_start` スナップショットには uid を足していない**
（そこを触らずに済ませられたので触らなかった）。

#### 表示

戦果の1文の見出しだけが変わる。**「変えたから勝った」とは書かない。**

```
今回動かした人が、こう働いた
ネルがガロを蘇生。ガロは復帰後に2回動き、15ダメージを与えた
```

印が立つのは、動かした人が**実際に働いた**候補だけ。動かしただけでは立たない。

検証: 新規 `node tools/test-build-changes.js`（結果を見ない・前が無ければ空・並び順・
KPIとの次元一致・選び方への影響・再起で戻る）と `tools/browser-tests/spotlight.js` へ追加。
Node 53本・ブラウザ52本すべて通過。`node tools/sim.js 50` のクリア率 42〜66% で帯の中。

### CHAIN V2 ハプニング条件を全15戦略でA/B測定（2026-09-08・Gemini／測定と推奨案のみ・本番切替なし）

結果は [`docs/CHAIN_V2_HAPPENING_GATE_MEASURE_2026-09-08.md`](docs/CHAIN_V2_HAPPENING_GATE_MEASURE_2026-09-08.md)。
**測定と推奨案のみ。本番切替・採用判断はしていない。`src/`・倍率表・演出閾値・UI・`RECORDED_VERSION` は未変更。**

```bash
node tools/chain-happening-ab.js 200 --json happening-ab-1000.json
CHAIN_SEED_BASE=2000 node tools/chain-happening-ab.js 200 --json happening-ab-2000.json
CHAIN_SEED_BASE=3000 node tools/chain-happening-ab.js 200 --json happening-ab-3000.json
```

- **測定器の規律**: `tools/chain-happening-ab.js` を新設。`tools/sim.js` の15戦略定義・ゲーム進行・母数保証方法（未完ランの追加seed補充）を直接再利用し、複製や余分な状態変更を行わない。
- **透明性assert**: `PROD_VERIFY`（パッチなし素の本番）と `PROD`（計測フック入り）が全15戦略×各30ラン（450ラン）で不一致0件。3 seed基すべてで通過。
- **入力指紋assert**: ラン開始時の魔王・初期金貨・食料・応募者一覧が `PROD` と `V2GATE` で全戦略・全seed完全一致。
- **未完ラン**: seed 1000 は 0件、seed 2000 は 1件（中盤で精鋭に転換）、seed 3000 は 1件（精鋭3体）。すべて両モード同一seedで発生し、追加seedで補充して有効200ランを厳密に保証。

#### 結果（15戦略 × 有効200ラン × seed基3本 ＝ 計18,000ラン）

| 項目 | PROD（現行・raw条件） | V2GATE（比較案・V2条件） | 差分 |
|---|---|---|---|
| クリア率 | 39.9% / 41.2% / 39.1%（平均 40.1%） | 39.9% / 41.2% / 39.2%（平均 40.1%） | -0.03 / +0.03 / +0.07pt（**平均 +0.02pt**） |
| 平均勝利数 | 8.30 / 8.33 / 8.31戦（平均 8.31戦） | 8.30 / 8.33 / 8.31戦（平均 8.31戦） | **0.00戦** |
| 敗北時攻略度 | 6.13 / 6.13 / 6.17（平均 6.14） | 6.13 / 6.13 / 6.17（平均 6.14） | **0.00** |
| `chain_receipt`（追撃より小銭）実発火 | 30 / 30 / 31件（計 91件） | 30 / 30 / 31件（計 91件） | **91 → 91件（発火率 0.011% 完全一致）** |
| `chain_stagefright`（連鎖に出遅れ）実発火 | 0件 | 0件 | **0 → 0件（成立0件）** |

#### ハプニング別の観察

1. **`chain_receipt`（追撃より小銭）**:
   - 全834,689回の判定機会中、raw成立 28,909件に対し、V2成立は 28,816件。
   - 条件真偽が変化したのはわずか **93件（0.32%）**。
   - 抽選後の実発火数は **91件 → 91件 で完全一致**。
   - 真偽変化は純粋連鎖ビルド（ゴブリン統一系）では 1〜3件（0.05%以下）しか起きず、三部門均衡（36件）や未払い搾取（24件）に集中。これは倍率測定と同様、`death` / `overkill` の重複計上を除外したことで生じたものであり、設計原則に合致する。
2. **`chain_stagefright`（連鎖に出遅れ）**:
   - 全18,000ランで成立0件、実発火0件。
   - `duringChain: true` で追加行動できる味方が現行エンジンでは greedy（強欲）のみのため、`!greedy && coward` を満たす者が連鎖中に行動機会を得られないという**既存の構造的課題**（V2切替とは無関係）。
3. **その他8種ハプニング**:
   - `chainDepth` 非依存のため真偽変化は 0件。

#### 推奨案と未確認事項

- **推奨**: ハプニング条件のV2切替は **安全に実施可能（低リスク）**。クリア率差 +0.02pt、平均勝利数・敗北地点は一致（0.00）。実発火数も 91→91件 で維持されるため、ゲームバランス・事件体験を損なわない。本番切替を行う場合はハプニング条件のみをV2へ接続する。
- **未確認事項**:
  - 演出閾値（稲妻強度・モルモ台詞等）は未測定。演出頻度への影響は別タスクで測る。
  - `chain_stagefright` の未発火問題（出遅れ事件が起きない仕様）の改善は別タスク。

### U2: 戦果に「この戦いの一手」を1文出す（2026-09-08・Opus）

設計書 6.1 / チケットU2。B1（`lastBattle.spotlight`）の事実を読んで文にするだけで、
**戦闘計算・数値・保存契約・既存パネルの構成は変更していない。**

- 出る場所は既存の「今回の大暴れ」パネルの中。**パネルは増やしていない**
  （設計書「パネルをさらに積み重ねない」）。最大CHAINと最大OVERKILLの2記録もそのまま。
- 日本語を組み立てるのは `UI.spotlightSentence()` **1か所だけ**。core は事実しか返さない。
  モルモ（D1）や魔界史（R3）で同じ事実を使うときも、言い方の管理はここへ寄せること。
- **撃破まで届いた回だけ「撃破した」と書く。** 届いていない回は実際のダメージを書く。
  ここを共通文にすると、戦果が静かに嘘をつく。
- `spotlight` が無い戦果（証拠が揃わなかった戦闘・この機能より前のセーブ）では
  **1文ごと出さない**。推測で書くと「変えたから勝った」という反実仮想になる。

実際に出る文:

```
この戦いの一手
グルグの追い剥ぎを受け、ボルが追撃した。衛兵を撃破した      （35文字）
ミミの料理がドンを強化（+24%）。騎士を撃破した
ネルがガロを蘇生。ガロは復帰後に2回動き、15ダメージを与えた
```

検証: 新規 `tools/browser-tests/spotlight.js`（3種それぞれ・撃破していない回の言い方・
証拠が無い戦果・パネル数と2記録が変わらないこと・390pxで70文字以内）。`run-all.sh` へ登録。
Node 51本・ブラウザ50本すべて通過。

**次**: D1（モルモが spotlight の事実に反応する）→ R3（魔界史へ記憶1件）。
どちらも文言を作り直さず `UI.spotlightSentence()` の言い方を共有するか、
同じ事実から別の言い方を1か所で持つこと。R2（前回出撃との差分）が入れば、
B1 の優先順1番目「今回変えた人の確実な働き」が使えるようになる。

### R3/U3: 魔界史へ「記憶」1件を残す（2026-09-08・Opus）

設計書 6.2 / チケットR3・U3の記憶表示ぶん。**戦闘計算・数値・既存の記録項目は変更していない。**
保存と表示を同じコミットに入れたのは、設計書の「未接続のデータだけを本番へ積まない」に従ったため。

- `Game.rememberSpotlight()` がランを通して**1件だけ**保持し、`endRun()` が `record.memory` へ載せる。
  選ぶ基準は B1 と同じ `Spotlight.reach()`（結果がどこまで届いたか）。同じなら大きいほう。
- **戦闘限りのIDを魔界史へ持ち込まない。** 設計書6.2の「戦闘中IDと永続uidは別」。
  `p0`/`x0` のような人物IDも根拠 `eventId` も、戦闘が終われば何も指さない。保存時に落とし、
  **名前と数値だけで自立して読める形**にする。ここが `rememberSpotlight()` の責任。
- `st.memory` はラン状態の中にあるので、**再起で巻き戻せば記憶も戻る**
  （やり直した歴史の出来事は残さない）。テストで固定した。
- 表示は終了画面（廃業届）と魔界史一覧の2か所。**統計の行は1つも増やしていない**
  （第11節・数字を並べるほど数字のゲームに寄る）。記憶が無い記録では1行ごと出さない。
- 言い方は `UI.spotlightText()` を戦果と共有する。**同じ事実の言い方を増やさない。**

```
魔界史に残った出来事　第6戦・対 王国巡回隊
グルグの追い剥ぎを受け、ボルが追撃した。衛兵を撃破した
```

契約: `record.memory = null | { kind, origin:{name}, originAbility, actor:{name}, ability:{name},
target:{name}, sameActor, numbers, turn, army, region, victory }`。
**旧魔界史にこの鍵は無い。無ければ表示しないのが正しく、推定生成してはいけない。**

検証: 新規 `node tools/test-memory.js`（選択・IDを持ち込まない・魔界史へ載る・
証拠が無いランには足さない・再起で戻る）と `tools/browser-tests/memory.js`
（終了画面と一覧・統計行が増えないこと・戦果と同じ言い方を通ること）。`run-all.sh` へ登録。
Node 52本・ブラウザ52本すべて通過。`node tools/sim.js 50` のクリア率 50〜74% で帯の中。

#### 設計書のチケットで、まだ手を付けていないもの

- **R1**（初回の応募補助）: 要否は「U1後の短い試遊で判断」とされたまま。**試遊待ち。**
- **R2**（前回出撃との差分 `buildChanges`）: これが入ると B1 の優先順1番目
  「今回変えた人の確実な働き」が使えるようになる。`Spotlight.reach()` の手前へ差し込む。
- **U3の残り**（モルモの状況反応・ツケの元選択表示）: 記憶表示だけ先に入れた。
- **P1**（初見試遊）→ **E1**（敵2隊列の相性試作）は試遊が前提。

### D1: モルモが spotlight の事実に反応する（2026-09-08・Opus）

設計書 6.3 / チケットD1。B1の事実を読んで台詞にするだけで、
**戦闘計算・数値・保存契約・既存の台詞データは変更していない。**

- 語彙は `MORMO_SPOTLIGHT_LINES`（data）に3種×2文＋fallback。差し込みは
  `MormoScene.spotlightLine()` **1か所だけ**。
- 戦果の1文（`UI.spotlightSentence`）とは**別の声**であって焼き直しではない。
  戦果は記録として事実を書き、モルモは現場から野次を飛ばす。ただしどちらも
  **同じ spotlight の事実からしか作らない**ので、片方だけ嘘になることはない。
- 出るのは**根拠の最後のイベント**の位置。そこまで来れば全部起きているので、
  「起きる前に結果や将来の死亡をしゃべらない」を構造で守れる。
  実測: 着地から決着までにイベントが4件以上残る戦闘が99%（決着の片付けで即消えない）。
- 起点と反応が同じ人の回は **黙る**。「AがBを動かした」と言えないため。

#### 絞った理由（実測つき・ここが一番大事）

300戦の実測で **spotlight 自体は79%の戦闘で成立する**。そのまま出すとモルモが
5戦に4戦しゃべることになり、「1戦闘に1回」を守っていても一言が事件ではなく
**毎回のナレーション**になる（第12節・ゲート8）。そこで出すのは
**撃破まで届いた接続で、かつ起点と反応が別人**の回だけにした。

| | 一言が出る戦闘の割合 |
|---|---|
| 従来（全滅／初発見／CHAIN5段） | 36〜40% |
| 無条件に spotlight を足す | **79%**（採用しない） |
| 撃破まで届いた回だけ | **48%**（採用） |

増えるが、増えるぶんは名前と行動が入る。**戦果の1文（U2）はこの制限を受けない。**
あちらは記録で、こちらは事件。

優先順は `wipe`（全滅）>`discovery`（初めての接続）>`spotlight`>`chain`（5段）。
spotlight は汎用の `chain` を**置き換える**位置に置いた（設計書6.1の順に合わせている）。

検証: 新規 `tools/browser-tests/mormo-spotlight.js`（行動に反応するか・起きてから出るか・
3種が同じ台詞にならないか・言えない回は黙るか・優先順・絞りが効いているか・
実際に画面へ出て1戦闘1回で終わるか）。`run-all.sh` へ登録。
Node 51本・ブラウザ51本すべて通過。

### B1: 戦果1文の材料を導出する（2026-09-08・Opus）

設計書 6.1 / チケットB1。**表示はまだ入れていない（U2で入れる）。**
戦闘計算・数値・発火順・保存済みデータの意味は変更していない。

新規 `src/core/spotlight.js`。`Spotlight.of(timeline)` がタイムラインを読み直して、
「誰の能力が、誰の何を動かし、結果どうなったか」を**根拠イベントID付きで最大1件**返す。
`st.lastBattle.spotlight` へ保存する（旧セーブにこの鍵は無い。無ければ出さないのが正しく、
**推定生成してはいけない**）。

#### 契約（HANDOFF 第2節へ足す新フィールド）

```
lastBattle.spotlight = null | {
  defVersion: 1, candidateCount,
  kind: "loot_relay" | "meal_boost" | "revive_return",
  origin: {id,name}, originAbility, actor: {id,name}, ability: {id,name},
  target: {id,name}|null, sameActor, numbers: {...}, evidence: [eventId]
}
```

- **日本語の文はここで作らない。** 引き継ぎメモ 2-1 の「計算」と「見せ方」の分離どおり、
  返すのは名前・数値・根拠IDだけ。文字列を作ると同じ事実の言い方が戦果・モルモ・魔界史で
  三重管理になる。組み立てるのは `ui.js`（U2）。
- **証拠が揃わなければ候補にしない。** 設計書の「該当する証拠がなければ文を出さない。
  反実仮想を断言しない」。金貨が出ただけ／反応の宣言だけ／料理したが殴っていない／
  蘇生したが復帰後に動いていない、はすべて null。自力復活（執念）も
  「人材どうしの接続」ではないので出さない。

#### 実装していない優先順（次に触る人へ）

設計書の優先順は「**今回変えた人の確実な働き** → 初めての接続 → 既存の代表経路」。
1番目には前回出撃との差分（**チケットR2の `buildChanges`**）が要るが、まだ無い。
**無いものを推測で埋めていない**ので、いまは2番目以降だけが動く。
実装している範囲の順序は `Spotlight.reach()` で、「結果がどこまで届いたか」
（撃破まで届いた > 追加の行動が起きた > 効果量だけ）。強さの順ではなく、
第4節「接続数」を見せるための順序。R2 が入ったら `reach()` の手前に差し込むこと。

検証: 新規 `node tools/test-spotlight.js`。手組みタイムラインで「証拠が無ければ出さない」を
6通り、3種の kind をそれぞれ、選択順序、根拠IDが実在イベントを指すこと、保存可能性、
タイムラインを書き換えないこと、実戦タイムラインで動くことを固定した。
Node 51本・ブラウザ49本すべて通過。`node tools/sim.js 50` のクリア率 42〜74% で帯の中
（読むだけの追加なので数値は動かない）。

### 落ちていたテスト2本を直した（2026-09-08・Opus／**現在このリポジトリで赤いテストはゼロ**）

**ゲーム本体は1行も変えていない。** 直したのはテストの作り方だけ。

- **`tools/browser-tests/synergy-pool.js`「1つだけでは魔王軍完成にならない」**
  （2026-09-06 から既知の失敗）。ゴブリン2体出撃で測っていたが、あとから
  《追い剥ぎコンビ》（ゴブリン2体で成立）が増えて非メタが2つになり、
  **overload が設計どおり正しく立つのに落ちる**テストになっていた。
  出撃1体へ変え、さらに **「非メタがちょうど1つ」という前提そのものを assert** した。
  同じ腐り方（あとから増えたシナジーで前提が崩れる）を繰り返さないため。
- **`tools/test-chain-api.js`「V2倍率が実際に発火する戦闘を検証している」**
  素の `Math.random` で戦闘を作っていたため、CHAIN 3段へ届くかが運任せで、
  **8回に3〜4回落ちていた**（今回のファンファーレ修正の有無に関係なく同じ頻度）。
  乱数を種で固定したうえで、**倍率が発火する戦闘に当たるまで種を引き直す**形にした
  （60通りまで、実際は13通り目で確定）。種を1つ決め打ちにするとバランスが動いた
  ときに同じ形で腐るので、そうしていない。これで「倍率が一度も発火しなくなった」
  という本物の異常だけがこのテストを赤くする。10回連続で通過を確認。

**赤いテストが常態化していると、新しく壊したときに気づけない。** 次に何かを足す人は、
まず全部緑であることを確認してから始めること。

### 連鎖を理解できる最小UIを試作した（2026-09-08・Opus／**ここで試遊して止まる**）

試遊の「何が起点で、なぜ次へ連鎖し、どうすれば意図的に伸ばせるのかがほぼ伝わらない」
「よく分からないけどつながった。これでいいのか？」への試作。**倍率・戦闘式・ハプニング条件・
演出閾値・CHAIN記録・保存契約は一切変更していない。** 足したのは表示と、その材料だけ。

**対象はゴブリンの略奪連鎖（信号「金貨獲得」）1本だけ。全連鎖へは広げていない。**
次に進む前に、**表示量と理解度を実機で試遊して判断すること**（設計憲法 第14節）。

#### 足したもの

1. **編成画面の「連鎖の見取り図」**（`UI.chainMapPanel`）
   起点（金貨を出す）→ 金貨獲得 → 反応（金貨獲得に反応する）→ 回り続けるか、の順で出す。
   起点も反応も無く、手持ちで埋まる欠けも無い編成では**パネルごと出さない**。390pxで20行。
2. **戦闘中の答え合わせ**（`BattleScene.pickChainAnswer` / `sayChainAnswer`）
   「《追い剥ぎ》の金貨に、《強欲》が反応した。」を**1戦闘に1回だけ**、実際に起きた
   最初の反応で出す。追加攻撃を生む反応（強欲）を、倍率だけの反応より優先して選ぶ。
   その段は `ANSWER_READ_MS = 2200ms` を下限に保護し、畳み掛けと総尺圧縮から外す。
3. **`Synergy.signalChain(signal, squad, options)`**（core・新API）
   1本の信号について起点・反応・反応が次に出す信号・再発火の可否・欠けを埋める案を返す。
   **効果量は返さない**（`preview` / `traitEffects` の担当のまま）。
4. **`Game.facilityReady(id)`**（run.js・新API）
   施設がこの出撃で実際に働けるかの判定。`deploy()` と見取り図が同じ条件を読む。
   判定内容は従来と同じで、置き場所を1か所へ寄せただけ。

#### 守った原則（次に触る人が壊しやすい2点）

- **説明文から推測しない。** 語彙は data 側の `links` と、シナジーの実際の `check` から組む。
  `desc` の日本語を解析すると、効果を変えたときに表示だけ古いまま残る。
  そのため `links` へ **`on`（発火の言い方）と `once`（1戦闘1回か）** を足し、金貨を
  出す／受けるシナジー（`goblin_horde` / `goblin_pair` / `martyr_allowance`）にも `links`
  を持たせた。**`emits` / `reacts` の語彙自体は増やしていない**ので、採用カードの
  `Synergy.connections()` の出力は変わらない。
- **完成レシピを全公開しない**（第7節）。「いまの手持ちで繋がるもの」に出すのは、
  控えに実在する軍団員と、実測であと何体か分かるシナジーだけ。
  **持っていない能力の名前は出さない。**

#### 検証

- 新規 `node tools/test-signal-chain.js`（起点・反応・再発火・手持ちだけの案・施設の可否）
- 新規 `tools/browser-tests/chainmap.js`（390px・編成を変えると答えが変わる・20行以内）
- 新規 `tools/browser-tests/chain-answer.js`（名指し・1戦闘1回・読む尺・出さない条件）
  いずれも `run-all.sh` へ登録済み。
- Nodeテスト全49本通過。ブラウザ49本中47本通過。落ちた `music` と `synergy-pool` は
  どちらも fad9df7 由来の既知失敗（変更前後で同じ）。
- `node tools/sim.js 200` のクリア率 18〜70%。表示専用の変更で数値は動かしていない
  （同条件で2回走らせた差の範囲内）。
- 答え合わせの尺のぶん、`loot-relay` の総尺は 30.4s → 31.5s へ伸びた（意図どおり）。

#### 次にやること（試遊のあと）

1. **まず遊んで、表示量と理解度を見る。** 見取り図は多すぎないか、答え合わせは
   1回で足りるか、「どうすれば伸ばせるか」が伝わったか。
2. 伝わったら、同じ形を別の信号（味方死亡＝死霊の縦、食料消費＝食事の縦）へ広げる。
   **`signalChain` は信号名を引数に取るので、UI側の対象を増やすだけで届く。**
   ただし1画面に2本以上並べると情報量が跳ねるので、出す本数の上限を先に決めること。
3. 伝わらなかったら、広げる前に**この1本の言い方**を直す。本数を増やして解決しない。

### 勝利ファンファーレが鳴らない原因を切り分けて直した（2026-09-08・Opus）

試遊の「勝利時のファンファーレが鳴らない」。**原因は2つあり、どちらも実測で確認した。**
CHAINの倍率・ハプニング条件・演出閾値・BGMの仕様・他の効果音の水準は**触っていない**。

測り方が要点。`Sound.cue` を差し替えて「呼ばれたか」を見るテストは既に通っていて、
それでも実機は無音だった（落とし穴 3-7 の再発）。そこで `AnalyserNode` を
`Sound.master` と `destination` の間へ挟み、**実際に出ている波形の振幅**を測った。
新テスト `tools/browser-tests/fanfare.js`（`run-all.sh` に登録済み）がこの方式。

**原因1: 合成音の水準が実録WAVより約23dB下だった。**
打撃音は 2026-09-04 に実録WAVへ差し替わり、`Audio` 要素で `Sound.volume * 0.9` まで出る
（`master` を通らない）。一方ファンファーレは合成音で `gain: .065` のまま据え置かれていた。
勝ち確の連打の直後に鳴るので、埋もれて「鳴っていない」と受け取られる。
**凱旋句の gain だけ**を打撃と同じ土俵へ上げた（`.065→.30`、和音 `.018→.085` / `.022→.10`）。
合算ピークは素で 0.70、実測 0.543 で歪まない。**他の合成音（lose・promotion・synergy・final）も
同じだけ下にいるが、この commit では触っていない。合成音全体の水準合わせは別タスク。**

**原因2: 決着表示のあとに「▶▶ 最後まで飛ばす」を押すと、自分でファンファーレを切っていた。**
`BattleScene.skip()` が無条件に `Sound.stopAll()` を呼び、かつ `announced` が true なので
`win` を鳴らし直さない。決着後の「飛ばす」は**残りの戦闘ではなく勝利の余韻を早送りしているだけ**
なので、`announced` のときは `stopAll()`・スキップ音・`KPI.logSkipped()` のいずれも行わないようにした。

実測（`tools/browser-tests/fanfare.js`・`Sound.volume` で正規化した実効ピーク）:

| 経路 | 修正前 | 修正後 | 判定基準 |
|---|---|---|---|
| そのまま見届ける | 0.121 | 0.543 | 打撃WAV基準 0.9 の1/4＝0.225 以上 |
| 決着後に飛ばす | 0.064 | 0.541 | 同上 |

**第3の要因も直した（2026-09-08・追加コミット）**:
`finish()` の `Music.update(scene: victory)` が、決着後に「飛ばす」を押したときに即座に走り、
**鳴り始めたファンファーレの上へBGMを重ねていた**。実機で確認した値は
`raiders-march.ogg` が音量1.0で再生開始、そのときファンファーレは残り3151ms。
BGMは実素材、ファンファーレは合成音なので完全に埋もれる。オーナーが2度目の試遊でも
「鳴らない」と言ったのはこれ。

`Sound.CUE_LENGTH` / `Sound.cueLength(name)` を音の側に置き、`BattleScene.playSettleCue()` が
鳴り終わる時刻 `settleCueUntil` を控える。`finish()` はその残り時間だけBGMの鳴り直しを待つ。
`tools/browser-tests/music.js` の2件はこれで通るようになった（**fad9df7 から落ちていた既知失敗が解消**）。

**踏んだ落とし穴**: 待ちタイマーを最初 `this.timers` へ積んだら `battlefield` と
`vfx-lifecycle` が落ちた。`this.timers` は「まだ描画が残っている」ことを表す枠で、
**スキップ直後に空であること**を両テストが契約として見ている。BGMの予約は
別枠 `this.musicTimer` に置き、`stop()` で畳む。画面を離れると `App.render()` が
先に場面曲を鳴らし、そのあとの `UI.set()` → `stop()` が予約を捨てるので二重に鳴らない。

**別途みつけた既存のフレーク**: `node tools/test-chain-api.js` の
「V2倍率が実際に発火する戦闘を検証している」が8回中3〜4回落ちる。
**私の変更の有無に関係なく同じ頻度**（修正あり 4/8、修正なし 5/8 通過）で、
乱数で連鎖が3段に届かない回があるため。seedを固定するか、届くまで引き直す形にすべき。未修正。

検証: 新テストは fad9df7 で**落ちること**を確認済み（常に成立するテストではない・落とし穴 3-4）。
`sh tools/browser-tests/run-all.sh` は 47件中45件通過。落ちた `music` と `synergy-pool` は
どちらも fad9df7 由来の既知失敗。Node は `test-sound` / `test-battle-pacing` /
`test-chain-live-ui` / `test-kpi` を通した。データも数値バランスも触っていないので `sim.js` は不要。

### CHAIN V2ハプニング条件の予備測定（2026-09-07・CodeX／採用なし）

結果は [`docs/CHAIN_V2_HAPPENING_GATE_PROBE_2026-09-07.md`](docs/CHAIN_V2_HAPPENING_GATE_PROBE_2026-09-07.md)。
汎用戦略200ラン×seed基3本では、条件の真偽が変わる候補は25/840件（3.0%）あったが、
productionとV2条件案のラン結果は全600件で一致した。全15戦略の母集団ではないため、
**影響なしとは判断せず、ハプニング条件はrawのまま維持する。** 次は対象別発火率を15戦略で測るか、
先に実機試遊で事件として認知できるかを見る。演出閾値も未変更。

### CHAIN V2倍率を本番接続（2026-09-07・CodeX）

新規V2ランは、戦闘中に逐次算出する `v2Depth` をCHAINダメージ倍率へ接続した。
進行中・再起・ロードしたV1ランは保存済み `chainDefVersion` を正本にし、raw V1倍率を維持する。

- 採用方式は決定どおり `V2same`。3段×1.25、4段×1.75、以後+0.25、上限×2.50
- raw `chainDepth`、`chainSummary`、ハプニング発火条件、演出閾値は変更なし
- 逐次 `v2Depth` と `Chain.summarize()` の後読み結果は400戦18,513イベントで不一致0件
- A/B測定器を本番後へ追従。production と V2same の透明性検査を維持

次は実機試遊で「連鎖の頂点が鈍いか」を確認する。鈍さが確認できた場合だけ、閾値ではなく
倍率表の刻みを別タスクで測る。ハプニング・演出をV2へ一括変換しない。

### CHAIN V2倍率方式を決定（2026-09-07・Astra／判断のみ）

決定は [`docs/CHAIN_V2_MULT_DECISION_2026-09-07.md`](docs/CHAIN_V2_MULT_DECISION_2026-09-07.md)。
**`V2same`を採用する。上記の本番接続で実装済み。**

- 倍率が読む段数だけV2へ切り替える
- 閾値は3段、倍率表は現行のまま（3で×1.25、4で×1.75、以後+0.25、上限×2.50）
- `V2low`（2段発火）は、適用率が約2.4倍になり非連鎖戦略へ倍率を配るため不採用
- 倍率表の強化案は未測定なので不採用。実機で頂点が鈍いと確認されてから別測定する
- 次はproductionの逐次V2段数を実装し、**倍率だけ**に接続する。ハプニング・演出は触らない

### CHAIN倍率のV2切替 A/B測定（2026-09-07・Opus／測定と提案のみ）

結果は [`docs/CHAIN_V2_MULT_AB_2026-09-07.md`](docs/CHAIN_V2_MULT_AB_2026-09-07.md)。
**採用・本番切替はしていない。`src/`・倍率・閾値・ハプニング・演出・UI・
`RECORDED_VERSION` は未変更。**

```bash
node tools/chain-mult-ab.js 200 --json out.json   # CHAIN_SEED_BASE で seed基を変更
```

- **倍率は記録と違って戦闘へ跳ね返る。** 1回の戦闘から両方は出せないので、
  同じseedで別々に走らせ、**A/B以外の入力が一致していることをassert**する方式にした。
- 差し替えるのは「倍率が読む段数」だけ。timeline の `chainDepth` は **production のまま**にし、
  V2の段数は `v2Depth` へ**並記**する。記録・ハプニング条件・演出閾値は全モードで production と同一。
- **透明性assert**: `production` と `V1` モードが450ランで不一致0件（3 seed基とも）。
  ここが崩れたら以後の差は差し替えの副作用かもしれないので、必ず先に確認する。
- 未完ランは単純除外せず追加seedで補充。件数と戦略別内訳を別途出す
  （seed基2000/3000 で各1件。既知の「生活部門」ケースで、倍率測定とは分離）。

#### 結果（15戦略 × 有効200ラン × seed基3本）

| モード | クリア率 | 倍率適用率 | 平均最大倍率 |
|---|---|---|---|
| V1 現行 | 40.6/42.6/40.2% | 4.4/4.4/4.2% | ×1.45 |
| V2same（閾値・表そのまま `>=3`） | −0.7/−1.4/−1.1pt | 3.6/3.5/3.4% | ×1.17 |
| V2low（閾値を `>=2` へ） | +1.6/+1.2/+2.3pt | 10.0/10.2/10.1% | ×1.42 |

差は小さいが**3 seed基で符号が揃う**（V2same は一貫して弱く、V2low は一貫して強い）。
平均勝利数・敗北地点はどのモードもほぼ動かない。

#### いちばん重要な観察: 頻度と到達倍率は別々にずれる

**「V1と近い発生頻度になるよう閾値を下げる」は成立しない。下げると外れる。**
頻度がV1（4.3%）に最も近いのは **`>=3`＝V2same そのもの**（3.5%）。
`>=2` へ下げると 10% で2.4倍になる。
一方、到達倍率はV2で圧縮される（×1.45 → ×1.17）。
**頻度を合わせると倍率が下がり、倍率を合わせると頻度が上がる。片方の閾値では両立しない。**

#### 提案（採用していない）

**`V2same`（無調整）を推す。** 測定値（頻度が最も近い・クリア率 −1.0pt・変更が最小）と
設計原則の両方で同じ結論になる。
V2は `death` / `overkill` の重複計上を取り上げるので、**倍率が乗るのは
異なる能力が実際に繋がったときだけ**になる（第4節）。
結果として連鎖ビルドと非連鎖ビルドの差が**広がる**（ゴブリン統一+求人は適用率24.6%・
×1.91を保ち、三部門均衡・未払い搾取は×1.06へ落ちる）。第5節の「狙って組んだ人のところに
壊れる瞬間が残る」に合う。
`V2low` は逆に倍率を非連鎖戦略へ配り直す（魔法職寄せの適用率が15倍）ので**推さない**。

弱点は到達倍率の圧縮。直すなら**閾値ではなく倍率表の刻み**を触る筋
（頻度は `>=3` で合っているので閾値を動かすと壊れる）。**この案は未測定。**

#### 未確認事項

- 総与ダメージは未測定（勝敗への影響で代替）
- 表の刻みを変える案は未測定
- ハプニング発火条件と演出閾値は全モードV1のまま。同時切替の相互作用は未測定
- 実機の体感（連鎖の頂点が鈍るか）は測定範囲外。試遊が要る

### CHAIN V2を新規ランへ本番切替（2026-09-07・CodeX）

`Chain.RECORDED_VERSION = 2`。**切替後に始める新規ランだけ**、戦闘中表示・戦果・
ランの `maxChain`・魔界史・KPIを正規化V2で揃えた。進行中・再起・ロード・再起動した
V1ランは保存済み `chainDefVersion` を正本にして、V1の値・表示・KPIを維持する。

- ラン記録はV2なら `Chain.viewOf(timeline).maxDepth`、V1なら従来の
  `result.chainSummary.maxChain` を読む。raw `chainSummary` 自体は互換用に変更していない。
- KPIはV2だけ `Chain.summarize(timeline)` と結合済みstepから深さ・能力列を作る。
  `triggerKinds` は従来どおり版に依存しないrawイベントから集計する。
- 教訓は決定どおりV1/V2とも `<=2`。ビルド名はV1 `>=6`、V2 `>=4`。
- 倍率、ハプニング条件、演出閾値はraw V1のまま。今回の切替へ混ぜていない。
- Node回帰49/49、期待経路9件・分岐20件、固定24戦、400戦18,513イベント一致。
- ブラウザ回帰はPlaywrightモジュール不在で47本すべて起動前停止。Chrome操作面も利用不可、
  in-app browserのローカルファイル表示はセキュリティ方針で拒否されたため実機未確認。

次は**倍率・ハプニング・演出閾値を消費者ごとに測定するタスク**。V2切替済みという理由だけで
一括変換しない。まず倍率の勝率影響、次に戦闘内ハプニング発火率、最後に演出頻度を別々に測る。

### CHAIN V2記録閾値を決定（2026-09-07・Astra／判断のみ）

決定は [`docs/CHAIN_V2_THRESHOLD_DECISION_2026-09-07.md`](docs/CHAIN_V2_THRESHOLD_DECISION_2026-09-07.md)。
**ゲーム本体・表示・倍率・ハプニング・演出・`RECORDED_VERSION` は未変更。**

- V2の教訓「未完の記憶」は **最大CHAIN `<=2`**。V1の死んだ提示率を移植せず、
  敗北を次ランの方向転換へつなぐ役割を優先する（V2実測提示47.5〜48.1%）。
- V2のビルド名「N連鎖を通した」は **最大CHAIN `>=4`**。V1 `>=6` とほぼ同じ希少度になる。
- 進行中V1ラン・V1魔界史は従来閾値を維持し、保存済み `chainDefVersion` で分岐する。
- `>=8` の追加称号や教訓の第2条件は、実機試遊なしに増やさない。
- 次は独立した本番切替タスク。新規V2ランだけ記録・表示・KPIをV2へ揃え、
  倍率・ハプニング・演出閾値は混ぜない。

### CHAIN V2表示モデル一本化（2026-09-07・Sol）

V2の構造化stepから表示文言を作る処理を `src/ui/chain_view.js` の
`ChainViewUI.stepLabel()` へ集約した。戦闘中・明示スキップ・戦果は同じ関数を読み、
片方だけ文言が変わる状態を防ぐ。V1表示、倍率、閾値、raw因果グラフは変更していない。

回帰 `tools/test-chain-ui-formatter.js` は期待効果9種と分岐2経路について、共通文言、
行為者／宣言者／対象、召喚の戦没者／召喚個体の分離、raw `text` 非依存を固定する。

### CHAIN V2切替前統合レビュー（2026-09-07・Sol）

結果は [`docs/CHAIN_V2_INTEGRATION_REVIEW_2026-09-07.md`](docs/CHAIN_V2_INTEGRATION_REVIEW_2026-09-07.md)。
準備層は接続済みだが、**`RECORDED_VERSION` を2へ変えるだけでは切替不可**。

- `run.js` の `st.maxChain` と `KPI.battleFinished()` は現在も意図どおりV1要約を読む。
  切替時は保存済みラン版でV1/V2を選ぶ実装が必要。
- V2ラベル生成が戦闘中UIと戦果UIに複製されている。共通化し、期待経路9件・分岐の
  戦闘中／スキップ／戦果一致を固定してから切り替える。
- 次の順序は **全15戦略×200測定（Opus）→ 表示モデル一本化（Sol）→ 記録/KPI版別読出し（Opus）
  → 記録閾値をAstra判断 → 切替**。
- 倍率・ハプニング・演出閾値はさらに後の別タスク。測定前に変更しない。

### CHAIN V2切替前の測定（2026-09-07・Opus／測定のみ・**2度やり直した**）

結果は [`docs/CHAIN_V2_MEASURE_2026-09-07.md`](docs/CHAIN_V2_MEASURE_2026-09-07.md)。
**測定だけ。実装切替・バランス変更・閾値の採用はしていない。`RECORDED_VERSION` は 1 のまま。
`src/` は1行も変更していない。**

```bash
node tools/deploy-falsy-probe.js 200          # 未完ランの原因切り分け
node tools/chain-v2-measure.js 200 --json out.json
```

**旧数値はすべて無効。** `5d2ff5a`（監査初版）／`23059c3`（未完ランを普通のランとして
数えていた）／`07cb1a9`（未完ランを単純除外していた）の数字は引用しないこと。

#### 踏んだ3つ目の罠: 未完ランの単純除外で母集団が壊れていた

`Game.deploy()` が falsy を返すと `runOnce` が break し `endRun()` に到達しない。
これを除外していたが、**戦略ごとに偏っていた**（seed基1000で精鋭3体 147/200＝73.5%）。
精鋭3体は**最も浅い戦略**なので、落とすとV2の低CHAIN率を過小評価する。
実際、母集団を揃えたらV1の教訓該当率が **5.1% → 1.5%** へ下がった。

**除外で母集団が変わる集計は、率を出す前に必ず戦略別の除外数を見ること。**
全体の除外率（5.2%）だけ見ても偏りは分からない。

#### 原因は本体ではなく `tools/sim.js` の精鋭戦略

`deploy()` の falsy 経路は run.js に2つだけ（出撃者0 / 給与不能）。
`deploy()` を包んで呼ぶ前のスナップショットで分類したところ、**全件が「出撃者0」**。
給与不能・phase不整合・その他は0件。
詳細は「stage1 turn1 day1 に roster 0体」。`newRun()` の初期 roster は `[]` で、
**一人も採用しないまま出撃していた**。空の軍で出撃させない本体の判断は正しい。

`elite` / `pivot` 戦略が「給与5G以上の応募者が居なければ見送る」だけで、
**軍が空のときの例外を持っていなかった**。
意図（高給3体だけ）は保ったまま `st.roster.length > 0 &&` を条件へ足して修正。
`pivot` 側は高給だけを `reduce` する採用行が空配列で落ちるため、
高給が居ない回だけ `chooseIndex` へ落とし、採用が通らなければ止める（無限ループ回避）。

**73.5% → 0.03%**（3 seed基 × 3000ラン中 未完は 0 / 1 / 1 件）。

#### 母集団の保証を測定器へ入れた

未完が出たらseedを追加採番して補充し、**各戦略とも有効200ランで必ず揃える**。
揃わなければ集計せずに `exit(1)`。**未完の件数と戦略別内訳は補充しても別途出力する**。

#### 結果（3 seed基とも有効3000ラン）

最大CHAIN 平均 V1 4.8/4.9/4.8 → V2 3.1（中央 4→3、P90 8→5、最高 22→12）
教訓 `<=2` 提示 V1 **1.5/1.3/1.1%** → V2 **47.5/48.1/47.6%**
ビルド名 `>=6` 命名 V1 19.3/20.0/19.4% → V2 3.9/4.4/3.6%
KPI `chainAbilityMax` 平均 3.4 → 1.5〜1.6

**ビルド名は `>=4` でV1と揃う**（25.1/25.5/25.3% ↔ V1 27.4/28.0/27.6%）。

**教訓は「V1と同じ当たり方」に寄せる値が存在しない。**
V2の分布は `1` が25件・`2` が1402件で断崖。`<=1` は0.5〜0.8%、`<=2` は47.8%で、
その間に置ける値が無い。そもそもV1には `0` も `1` も無く `2` が44/3000しかないので、
**教訓「未完の記憶」はV1の時点で死んでいる**。`<=1` を選ぶのはその状態の移植になる。
閾値ではなく「`maxChain` の少なさで敗因を語る」設計の論点。**案は出したが採用していない。**

#### 未確認事項

- 残る未完2件は形が違う（roster 1体だが唯一の生存者が生活部門で combat 0）。
  3000ランに1件なので補充で対応し、件数と原因だけ残した。直すなら別タスク
- **勝率影響は未測定。** 倍率は一切動かしていない
- **`tools/sim.js` の戦略修正で 精鋭3体 と 中盤で精鋭に転換 のバランス数値が変わる。**
  過去の sim 出力のこの2戦略は信用しないこと（他13戦略は影響なし）
- 教訓の選択率は測定範囲外（提示率まで）
- ハプニング発火条件と演出閾値は未集計（戦闘中の値でランの `maxChain` とは別）
- `chainAbilityMax` はV1とV2で数える単位が違う（生イベント vs 結合済み step）

### KPIのCHAIN観測を定義バージョン別に分離（2026-09-07・Opus／切替計画E・実装済み）

[`docs/CHAIN_V2_UI_PLAN_2026-09-07.md`](docs/CHAIN_V2_UI_PLAN_2026-09-07.md) 第3節E。
**`maxChain`・魔界史・倍率・演出閾値・ハプニング条件・教訓・ビルド名は変更していない。
`src/ui/` は未編集。**

- 分離は `KPI.chainStatsByVersion(runs)` の1か所だけ。版の読み出しは `Chain.versionOf()` に集約し、
  写しを持たない（`tools/kpi-report.js` が本体を `require` する）。
- **`Chain` の解決は `KPI.chainApi()` 経由にすること。** ブラウザはグローバル、Node は
  `require("./chain.js")`、取れなければ例外。`typeof Chain !== "undefined"` の三項演算子で
  自前の版判定へ落とす書き方をすると、**CommonJS では `Chain.versionOf()` が一度も呼ばれない**
  （`const Chain` は chain.js のモジュール内に閉じてグローバルには出ない）。
  同じ理由で `tools/chain-audit.js` の vm 読み込みリストにも chain.js が要る。
- **混在時の判定は2つに分ける。** トリガー種類は版に依存しないので全体で1つ、
  CHAINの判定は版ごとにその版の `chainAbilityMean` だけから作る。
- `chainMax` / `chainAbilityMax` / `chainSample` の平均・最大・代表値は**その版のランだけ**から作る。
- **`triggerKinds` は分離しない。** 段数の数え方ではなく「どの能力が連鎖に参加したか」なので
  版をまたいでも意味が変わらない。ここを分けると理由なく比較できない指標が増える。
- **KPIランの版は「渡されたラン状態」を正本にすること**（`chainApi().versionOf(state)`）。
  `RECORDED_VERSION` を入れると、切替後に既存V1途中ランを**再起動**したときに壊れる。
  `KPI.current` はメモリだけなので再起動で消え、次の戦闘で `battleStarted()` が
  `runStarted()` を呼び直す。そこで定数を読むとラン状態はV1のままKPIだけV2になる。
  **`RECORDED_VERSION=1` の間はブラウザテストでも踏めない**（切替境界に入らないため）。
- **`KPI.battleFinished()` は `Chain.RECORDED_VERSION` を読み直さない。** ラン途中で切替コミットを
  跨いでも1ランの中でV1とV2が混ざらない。混ざった時点でその `chainMax` はどちらの定義でもない
  値になり、後から分離できない。
- レポートは**版が1つなら従来どおりの見た目と数字**。混在時だけ警告＋版別ブロックにし、
  統合した平均・最大・代表CHAIN・判定を出さない。
- 回帰は `tools/test-kpi-chain-version.js`（47本目・44件）。
- `src/core/kpi.js` に `module.exports` を追加した（`typeof module !== "undefined"` ガード付き。
  ブラウザでは従来どおりグローバル）。

### 戦果へV2要約を保存（2026-09-07・Opus／切替計画A・実装済み）

[`docs/CHAIN_V2_UI_PLAN_2026-09-07.md`](docs/CHAIN_V2_UI_PLAN_2026-09-07.md) 第3節A（Astra承認済み）。
**加算保存だけ。表示・`maxChain`・KPI・倍率・閾値は切り替えていない。`src/ui/` は未編集。**

- `Chain.viewOf(timeline)` を追加。`lastBattle.chainView` に
  `defVersion / maxDepth / rawMaxDepth / deepest.steps` だけを保存する。
  **なぜ要るか**: 戦闘中はタイムラインから何度でも正規化できるが、`lastBattle` は
  タイムラインを保存しない。これが無いとロード後の戦果でV2代表経路を作り直せない。
- 関数は保存しない（`pathTo` は含めない）。最後に JSON 往復を通し、関数や循環が
  紛れ込んだら**保存されてから気づくのではなくその場で落とす**。
- **`migrateState()` に `chainView` の既定値は足していない。** 旧セーブに要約は生えない。
  読む側は「無ければV1表示」。**ロード時に推定生成しないこと。**
- **バージョンを2つ混同しないこと。** `chainView.defVersion` は API出力契約の版(2)、
  `state.chainDefVersion` はそのランが記録している値の版(1)。別物として共存する。
- 回帰は `tools/test-chain-view.js`（28件・43本目）。実戦から
  2段以上かつ宣言結合を含む代表経路の戦果を作り、保存→ロードで各 step の
  行為者・宣言者・能力・効果・分岐印が一致すること、再起でチェックポイント時点の戦果へ
  戻ること、旧セーブに要約を作らないことを固定。
- 不変性: `a44a152` と比べて300戦のタイムライン・CHAIN値・倍率タグ・勝敗がバイト単位で一致。
  120ランの `maxChain`・教訓の提示・ビルド名・V1 `chainSummary`・OVERKILL・KPI も完全一致。
- **次は Sol の B/C/D（UIのバージョン分岐）。** 保存側は揃っているので、
  V2戦果は `lastBattle.chainView` を読むだけでよい（UIで再計算しない）。

### CHAIN 正規化APIを本体へ追加（2026-09-07・Opus／①のみ実装済み）

**Astraが `ebc40a1` のAPI契約を承認。承認範囲は「正規化APIと定義バージョン保存」だけ。**
下の「推奨」の①に当たる。②記録のしきい値と③倍率は**まだ未承認・未実装**である。

- `src/core/chain.js` を新設（`Chain.summarize()` / `Chain.versionOf()`）。
  戦闘計算にもラン状態にも触らず、**タイムラインを読み直すだけ**。
- **追加しただけで表示・記録・戦闘結果は何も変わっていない。**
  倍率・連鎖ハプニングの発火条件・記録の閾値・`Battle.summarizeChains()` の戻り値は
  すべて現行(V1)のまま。`src/ui/` は未編集。
  300戦のタイムライン・CHAIN値・倍率タグ・勝敗が追加前と**バイト単位で完全一致**することを確認済み。
- **バージョンは2つある。混同すると「将来の予約」を実績として保存してしまう。**
  `Chain.DEF_VERSION = 2`（APIの出力契約の版）と
  `Chain.RECORDED_VERSION = 1`（**いま実際に記録している CHAIN 値**の版）。
  保存するのは後者。`st.chainDefVersion` / 魔界史 record / KPI entry に **V1 として**入る。
  V2へ切り替えて記録する値が実際に変わったときに初めて 2 にすること。
- バージョン不明・欠落・壊れた値は `Chain.versionOf()` がすべて **V1** として扱う。
  旧セーブ・旧魔界史・旧KPIの `maxChain` を**推定変換しない**。
- `tools/chain-audit.js` は写しを持たず `src/core/chain.js` を読み込んでassertする
  （監査ツールと本体が静かに食い違うのを防ぐため）。**契約を変えるときは本体側だけ直す。**
- 検証: `node tools/test-chain-api.js`（新規/途中/ロード/再起/再起動/旧セーブの6状況）、
  `node tools/chain-audit.js --paths`、`tools/test-*.js` 42本、ブラウザ回帰。
- **次にやるときの注意**: 段数を読むときに `parent.chainDepth || 1` と書かないこと。
  新定義では0段の親が出るため、0 が falsy で 1 に読み替えられ子が水増しされる。`?? 1` 相当にする。
- **ブラウザ回帰の既知失敗**: `synergy-pool` と `music` の2本。**この変更の前（`ebc40a1`）でも
  同じ内容で落ちる**ことを確認済みで、正規化APIの追加とは無関係。残り38本は通過。
- **Sol第三者レビュー（2026-09-07）**: 正ブランチを `75ed826` へfast-forwardし、
  Node回帰42/42、期待経路9件、分岐契約20件、固定24戦A/B不一致0、
  400戦・18,513イベントの逐次/正規化不一致0を再確認した。WindowsのCRLFでは
  `chain-audit.js` のLF固定置換が起動前に失敗したため、監査入力だけLFへ正規化した。
  ゲーム本体・CHAIN契約・数値は変更していない。ブラウザ回帰はSol環境にPlaywrightの
  Chromium実体が無く再実行未確認。上の38/40はOpus報告、過去の40/40は統合前の別実行として
  区別し、ブラウザ結果の食い違いは次の実行可能環境で再確認する。

### CHAIN V2 表示・記録切替の実装順（2026-09-07・Sol／保存契約承認済み）

依存関係とV1不変条件を [`docs/CHAIN_V2_UI_PLAN_2026-09-07.md`](docs/CHAIN_V2_UI_PLAN_2026-09-07.md)
へ整理した。**まだ表示・記録・倍率・閾値は切り替えていない。**

- 戦闘中UIはtimelineからV2を導出できるが、戦果の `lastBattle` はtimelineを保存しない。
  Astra承認済みの `lastBattle.chainView` をOpus側で先に保存する。
- `chainView` は `defVersion / maxDepth / rawMaxDepth / deepest.steps` のJSON値だけを持つ。
  既存V1 `chainSummary` は保持し、旧セーブから推定生成しない。保存→ロードの帰属一致と
  再起で対応する戦果へ戻ることを回帰テストにする。
- その後の順序は、戦闘中UI → 戦果UI → 魔界史の版表示 → KPI版別集計。
- 演出尺、稲妻、モルモ台詞、倍率、ハプニング、教訓、ビルド名の閾値は別タスク。
  全15戦略×200の測定前に変更しない。

#### 戦果UIのV1/V2分岐（2026-09-07・Sol）

`UI.battleChainView()` を追加し、ランの `chainDefVersion` がV2かつ有効な
`lastBattle.chainView` がある場合だけ正規化要約を表示する。V1途中ランは `chainView` が保存されていても
既存 `chainSummary` を使い、旧セーブ・欠損・未知の契約版もV1表示へ戻す。
V2経路の文言は保存済みstepの構造データから作り、raw `text` は解析しない。
戦闘中カウンタ・魔界史・KPI・倍率・閾値は未変更。

#### 戦闘中UIのV1/V2分岐（2026-09-07・Sol）

`BattleScene.prepareChainView()` がV2ランだけ `Chain.summarize(timeline)` からevent→step対応表を
再生開始時に作る。固定帯・CHAINカウンタ・履歴は数える実効果stepでだけ進み、宣言・補足単体では
増えない。通常再生とスキップは同じ対応表を読む。V1ランは従来分岐をそのまま通り、raw因果グラフも
変更しない。演出尺・稲妻強度・モルモの5段台詞・倍率・ハプニング閾値は未変更。

#### 魔界史のV1/V2識別（2026-09-07・Sol）

魔界史にV1/V2が混在するときだけ、各カードの最大CHAINへ「旧定義 V1／新定義 V2」を表示し、
一覧冒頭に世代間で直接比較しない旨を出す。バージョン欠落の旧記録はV1。
単一定義だけの履歴は従来表示を維持し、主要記録も最大CHAIN・最大OVERKILLの2項目から増やさない。
並べ替え・歴代最高・合算は追加していない。KPI・倍率・閾値は未変更。

### CHAIN再定義の契約案と影響監査（2026-09-07改訂・Opus／契約のみ。①は上で実装済み）

設計書 第4節の独立タスク。結果は [`docs/CHAIN_CONTRACT_2026-09-06.md`](docs/CHAIN_CONTRACT_2026-09-06.md)。
測定は `tools/chain-audit.js` の1本だけで、**`src/` は1行も変更していない**。
**2026-09-07にAstraレビューを受けて監査を修正し、測定値を全面的に取り直した。
初版（5d2ff5a）の数値は引用しないこと。**

- **監査側の欠陥を修正**: 事件選択がホストの `Math.random`（seed管理外）を踏んでいた。
  ラン間の保存状態も持ち越していた。いまは乱数をすべてコンテキストのseed付き乱数へ通し、
  1ランごとに `localStorage` を空にする。**A/B不一致は原因を推測せず検証失敗で停止**する
  （実測: 200ラン×3seed基すべて全件一致）。
- **分類の基準を統一**: 「どこで計算しているか」ではなく **「同じ実効果を一度だけ数える」**。
  これで `death`＝補足（一撃そのものの結果）、`survive`＝実効果（別能力の介入）、
  `demon_cook`＝実効果（計算場所はrun.jsだが効果の表現はこの1件だけ）、
  `glutton_feast`・`incident`＝宣言（効果は子が担う）へ変わった。
  未分類のイベントは例外で停止する。
- **子のない宣言（2026-09-07・最終）**: 「子が無ければ自動的に1段」は廃止。
  そのイベント自身が実効果を表すときだけ1段（《戦場ストライキ》の行動中止＝`kind:'skip'`）、
  予告しただけで実行されなかったものは**0段**（《暴食の宴》の未実行。混合編成1500戦で127件出る）。
  分類の `selfEffect` で区別する。この規則にしたことで、数える対象がイベント単体で決まり
  **契約が逐次実装できるようになった**（400戦18,513イベントで逐次と正規化APIの段数が完全一致）。
- **期待経路（assert済み・生→正）**: 略奪 4→3、食料 2→1、死霊 4→3、召喚 4→2、OVERKILL 4→2。
  `node tools/chain-audit.js --paths` が一致しなければ監査を中止する。
- **単一APIを設計**: `Chain.summarize(timeline)` が `defVersion` / `maxDepth` / `rawMaxDepth` /
  各イベントの正規化段数 / 結合後の代表経路を返す。生の因果グラフは捨てない。
  **結合は親子関係だけで判定**（時間的な隣接では結合しない）。
- **結合の向き（2026-09-07・改訂2）**: 宣言は**効果を担う直接の子の step へ所属**させる
  （親側ではない）。「《強欲》による追加攻撃」で1つの step。
  分岐して実効果の子が複数あるときは**同じ宣言を各子の step へ複製**する
  （集合stepにしない・最初の子を選ばない）。複製側は `sharedDeclaration: true`。
  `steps[]` は actor・能力名・効果イベントを持ち、UIは `text` を解析しなくてよい。
- **期待経路は段数だけでなく step の構成（効果の種類＋能力名）までassert**する。
  略奪／食料／暴食／死霊／墓地／OVERKILL／仲間割れ／ストライキ／暴食未実行の**9件**。
  加えて分岐契約20件（合成タイムライン。実戦では発生しない形なので合成で固定している）。
- **行為者（actor）の規則**: `actorId` は**実効果を起こした人物だけ**。人物が起こしていない
  効果（施設）は `null`。宣言者は `declaredBy` へ。**`summon.sourceUnitId` は戦没者であって
  召喚者ではない**ので行為者に帰属させない（戦没者と召喚個体は `effect.fallen*` / `effect.summoned*`
  に別の役として持つ）。
- **影響（消費者ごとに分離して測定）**:
  ①倍率だけ変える → 倍率が下がる判定は 1.1〜1.6%、クリア率の差は +4.0/±0/−1.5pt で seed差に埋もれる。
  ②ハプニング条件だけ変える → **3seed基すべてで現行と完全に同一**（真偽が変わるのは追加行動の3〜4%）。
  ③記録 → 平均最大CHAIN 3.7→2.5。教訓「未完の記憶」は**条件一致0%→約64%、実際の提示も約63%**。
  ビルド名「N連鎖を通した」は命名 11〜14%→ほぼ0%。
  （条件一致・提示・命名を区別して数えた。教訓の選択率は人の判断なので測っていない）
- **推奨（①は実装済み。②③は未承認）**: ①`Chain.summarize()` だけ先に入れる（値は動かさない）→ ②記録のしきい値を置き直す
  （`<=2`→`<=1`、`>=6`→`>=4` あたりから）→ ③倍率は最後。逆順にすると教訓が3分の2のランで提示される。
- 未確認: 勝率影響は「無い」と証明していない。測定戦略は最強優先1本のみ。教訓の選択率は測定範囲外。

### 次にやること（2026-09-06・オーナー採用済み設計）

オーナーがレビューの改善方針を全面採用。実装担当へ渡す正の設計書は
[`docs/BUILD_EXPERIENCE_DESIGN_2026-09-06.md`](docs/BUILD_EXPERIENCE_DESIGN_2026-09-06.md)。
基準コードは `1d984a6`。今回は文書のみで、ゲームコード・数値・保存契約は変更していない。
設計書第6.2節のフィールドは**実装予定であり現行契約ではない**。

- **V0は完了（2026-09-06・Claude／同日 V0補正で結論を訂正済み）**。
  結果は [`docs/V0_BASELINE_2026-09-06.md`](docs/V0_BASELINE_2026-09-06.md)。
  測定ツール2本（`tools/vertical-v0.js` / `tools/reachability-v0.js`）だけで、`src/` は未変更。
  要点: **略奪＝充足／食料＝発火するが帰属データが無い（V2aが必須）／死霊＝充足（配置が正しければ）**。
  接続を狙う方針なら83%のランで2人の接続が成立する。最初の3戦以内は25%。
- **⚠️ 初版の「死霊は不足」は測定側の欠陥だった。引用しないこと。**
  `st.activeUids` の並び順が戦場の配置で、`battle.js` は先頭を60%で狙う。
  初版は死霊術師を先頭に置いていたため術師が先に落ちていた。**後衛へ置くと
  他者蘇生 0%→90%、復帰後行動 →73%、魂の徴収 0%→98%、殉職手当 0%→33%**。
  同じ5人でも並び順で結果が割れるのに、その因果は画面に一切出ていない（U1/U2の最優先課題）。
- **蘇生タイミング（`onRoundEnd`）の変更提案は撤回・保留。** 現行に問題は確認できていない。
- **R1（初回の応募補助）の要否も保留。** 提示は最初の3面接のどこかで54〜69%起きており、
  初版の「機会が足りない」は測り方の欠陥（合法性・食料語彙・事件のseedを欠いていた）。
- 採用カードは `Synergy.connections()` だけでは食料系も《追い剥ぎコンビ》も0件になる。
  `Synergy.preview()` の need/needRace と併用すること（U1の前提）。
- **V1は「検証で充足」として完了（2026-09-06）。実装は行っていない。**
  略奪は入口〜完成が現行コードで成立しており、対照で強欲追撃が 3.65→0.00 に消える。
- **V2aは完了（2026-09-06）**。`Game.mealPlan()` を run.js へ追加し、食事強化の
  起点・対象・効果量を1か所で決めて、予告・本番の倍率・戦闘入力・戦果が同じ伝票を読むようにした。
  契約は第2節「食事強化の伝票 `mealPlan`」。**戦闘式・蘇生タイミング・共通CHAIN倍率は未変更**で、
  seed固定300件のA/B比較で倍率の差分0件（挙動は完全に同じ）。battle.js は触っていない。
- **V2bは完了（2026-09-06）**。battle.js が `rations.meal` を読み、
  料理人イベントへ対象と効果量、強化された者の最初の有効打へ `mealBoost` を書き添えるようにした。
  新しいイベントは増やさず、**ダメージ・発火順・回数・chainDepth は不変**
  （実戦200件のA/Bで差分0件）。契約は第2節「食事強化の因果イベント」。
  実例: 「ザグの【魔界料理人】ゲンコへ+24%」→ ゲンコの最初の有効打が65ダメージで大盾傭兵を撃破。
  **食欲0への強化は現行維持**（`targetEatsNothing` は印だけ）。
- **V3は「検証で充足」として完了（2026-09-06）。ゲーム側の修正は不要だった。**
  `tools/test-death-vertical.js` を追加し、後衛配置での成立を**恒久的な回帰テスト**として固定した。
  seed固定40試行での実測（V0補正と同じ値を再現）:
  他者蘇生 90% / 復帰後に行動 73% / 魂の徴収 98% / 召喚 100% / 殉職手当 33%。
  術師を先頭に置くと他者蘇生は 0%。**蘇生タイミング・戦闘式・共通CHAIN倍率は未変更。**
  設計書3.3の必須仕様（召喚物は永続軍団に増えない・戦没者名簿に載らない・魂の生成除外・
  回数上限＝施設Lv.・4ラベルの区別）もこのテストで固定した。
- **U1は完了（2026-09-06・Sol）**。採用カードを能力→接続1件→代償の順へ変更し、残りは詳細へ畳んだ。
  接続は起点／反応者を分離し、採用・出撃・入れ替え・部門配属の必要条件を表示する。
  `links` 一致だけでなく、現在の出撃者、施設要件、食料と食欲最大対象、実際のシナジー `check` を確認する。
  略奪は戦闘中の所持金増加と誤認させないよう「1Gを略奪予約」。既存「死亡反応」には
  《死霊術》保持者本人の生存条件と、最前列時だけの注意を追加した。戦闘式・応募補助・保存構造は未変更。
  検証: `node tools/test-synergy-preview.js`、`node tools/test-recruitment.js`、
  `node tools/test-death-vertical.js`、ブラウザ回帰40件通過。`synergy-pool` だけは下記既知失敗のまま。
  **同日予告精度修正**: 出撃者を `activeUids` 順に構築し、本番同様に最初の料理人・食欲同値時の
  出撃順先頭を採用する。応募者が関与しない既存食事接続は除外し、応募料理人を既存料理人より
  優先しない。満員時は各1名を入れ替えた合法編成だけを判定し、相手や配置で結果が割れる場合は
  対象・成立を断定せず「入れ替え相手と配置を確定」と出す。戦闘式・食事倍率は未変更。
- **テスト強化は正ブランチへ統合済み（2026-09-07・CodeX）**。
  `346ee67` / `bfa9828` / `dab701d` / `14f5bdc` 相当を取り込み、固定待ち・旧BGM仕様・
  他シナジー混入を見ていた検査をゲーム上の契約へ合わせた。統合後、将軍テストの対照2体が
  《追い剥ぎコンビ》、次に《精鋭主義》を発火する測定ミスを発見したため、異種族・給与4Gの
  中立編成へ補正。**ゲーム本体は変更なし**。Nodeテスト41本は全通過。ブラウザ40本はOpus環境で
  全通過済みだが、CodeX環境ではPlaywrightブラウザ未配置のため第三者再実行は未確認。
- 次は **R1（Claude）→ R2/B1 → U2（Sol）**。U2の戦果1文は `result.mealSummary` / `st.lastBattle.mealPlan` を根拠に書ける。
  死霊の1文は `revive` の `traitId`/`sourceId` と `summon` から書ける（既に区別できる）。
- **V系統（V0〜V3）はすべて完了。** 3系統とも現行コードで入口〜完成が成立しており、
  V2a/V2bで足したのは食事の帰属データだけ。残る課題は表示側（U1/U2）と到達性の補正タスク。
- **到達性（レポート第2節）は暫定。** 食料の正確な帰属判定は V2a/V2b 後の別タスクで補正する。
  R1の要否は **U1後の短い試遊**で判断し、**最終P1を待って後続作業を止めない**。
- その後、採用表示・必要時の初回応募補助・根拠付き戦果・モルモと魔界史へ進む。
- 第9節に担当層・依存関係・AI向け依頼文、第10節に自動検証と初見試遊の条件がある。
- 既存機能を再実装せず、不足だけ埋める。run.jsとbattle.jsは同時に触らない。
- 過去のA〜J表は完了履歴。今回の順序は上記設計書が優先。継承・長編化・新種族量産は後段。
- **既知の失敗テスト（V0/V2aで発見・未修正・いずれも基準コミット由来）**:
  `sh tools/browser-tests/run-all.sh` の **synergy-pool**（「1つだけでは魔王軍完成にならない」）も
  c8a9f1c で既に落ちる。V2aの変更前後どちらでも同じように落ちることを確認済み。
- **既知の失敗テスト（V0で発見・未修正）**: `node tools/test-promotions.js` が c8a9f1c で既に落ちる。
  最後の assert が 1.15 を期待しているが実測 1.38。見本がゴブリン2体なので《追い剥ぎコンビ》と
  《魔王軍完成》も立つのが原因で、**ゲームは設計どおり・テストの期待値が古い**。
  見本を別種族2体にするか期待値を実測へ合わせるだけで済む。触る担当は先に宣言すること。

### いまここ（2026-09-05 時点の要約・まずこれを読む）

- **2026-09-06 序盤CHAINの着地を試作（CodeX）**: ゴブリン2体で《追い剥ぎコンビ》が成立し、
  金貨獲得ごとに次の味方攻撃+25%。さらに全ダメージ系統共通でCHAIN 3は×1.25、CHAIN 4は×1.75、
  以後1段ごと+0.25（最大×2.50）の連鎖倍率を追加。強欲・暴食の宴・OVERKILL伝播へ適用し、
  「つながったこと自体」に戦況上の価値を持たせた。蘇生・召喚は既存の生存者増加と累積強化を着地とする。

- **Solへの次の作業**: [`docs/SOL_HANDOFF_2026-09-05.md`](docs/SOL_HANDOFF_2026-09-05.md)。
  最新ブランチでC・F・Eは統合済み。**Jも全11種×3表情を2026-09-06に本番接続して完了**。生成済み原画の対応表、
  透過失敗2枚、ローカルstashから画像だけ復元する方法を記載。古いstash内READMEで現行コードを上書きしない。

- 正のブランチは **`claude/owner-playtest-tuning-3vr0tt`**。着手前に必ず `git pull`。
  CodeX が同じブランチへ push してくるので、pull せずに始めると弾かれる。
- 進行中の作業表は **`docs/WORK_SPLIT_2026-09-05.md`**（オーナー試遊の不満点6つ → A〜J の分担）。
- Claude 分の進捗: **A・D・I・G・B すべて完了**（詳細は下の各ブロック）。Claude 分の残りは無い。
  - B = 「今回の大暴れ」パネルの先頭に1行サマリ `.breakthrough-headline` を足した。
    `⛓ CHAIN 4 ・ ⚡《魔王軍完成》《ゴブリンの群れ》 ・ 🔥 戦意 ×1.80` の形。
    シナジー名は4つ以上で `ほかN種` に畳む。戦意は新フィールド `lastBattle.momentumPeak`
    （タイムライン導出・第2節に契約を追記）。**試遊で「達成感が残るか」を確認**。
- **E 完了（同日・Claude）**: 戦間イベント 16→30。Gemini の台本（依頼書 `docs/GEMINI_BRIEF_E.md`）を
  そのままデータ化しようとしたら、**14本すべてが「N戦後に自動で祟る」前提**で、その仕組みが
  エンジンに無かった。先に **ツケ（`st.debts`）** を run.js へ入れ、その上で14本を追加した。
  詳細は下の「ツケと戦間イベント14本」ブロックと README「ハプニング」節。
- 他担当の **Jは完了**（表情差分、CodeX/Sol。全11種×驚き・にやり・涙）。C・Eも下記の調整・実装を経て採用済み。
  Hの新規場面台詞の呼び出し確認は別残件。Jの表情選択・受け渡し、通常絵/絵文字fallbackも実装・検証済み。
- 検証の型（毎回これ）: データを触ったら `node tools/sim.js 50`（クリア率がおおむね5〜50%の帯）、
  UIやフローを触ったら `sh tools/browser-tests/run-all.sh`。
  **playwright はコンテナに入っていないことがある**ので、その時は
  `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install --no-save playwright` を先に打つ
  （Chromium は `/opt/pw-browsers/chromium-*/chrome-linux/chrome` にあり、run-all.sh が自動で探す）。

### ツケと戦間イベント14本（2026-09-05・Claude / 作業表 E）

**踏んだ落とし穴**: 依頼書に「選択肢のうち1つは必ず『得だが後で祟る』」と書いて Gemini に投げたが、
返ってきた台本は全14本が「3戦後に忠誠-30」のような**遅延効果**で書かれていた。
`events.js` の `apply(st, c)` は即時にしか効かず、関数を state に置くとセーブできない。
そのまま CodeX へ渡していたら、祟りが落ちて**全部が「ただ得なだけの選択肢」**になっていた。

そこで **ツケ** を入れた。契約は `st.debts = [{ id, battlesLeft, kind, uid?, dept?, amount?, text }]`。
種別と数値だけを持つ伝票で、`Game.oweDebt()` で積み、`Game.settleDebts(notes)` が
**戦闘のたびに1つ期限を縮めて満期を支払う**（`deploy()` の `st.lastBattle` を組む直前）。

**壊してはいけない約束**:
- **勝敗を問わず取り立てる。** 負ければ消えるなら、ツケは判断ではなく「わざと負ける」抜け道になる
- **プレイヤーに見せる**（編成画面・戦果画面の `UI.debtPanel()`）。理由の分からない不幸は理不尽になる
- **祟りの種別を増やすときはエンジン側（`settleDebt()`）に1ケース足す。**
  イベント側に関数を書くとセーブが壊れる
- 稼働中の施設は `facilityLevel` を 0 まで落とさない（選んだ施設が宙に浮く）

台本からの変更点（実装できなかったもの／数値の調整）は下記。
- 「次戦は出撃不可」の仕組みが無いので、有休は金貨と忠誠へ置き換えた
- 勤続年数を持っていないので、退職の相談は忠誠だけで発火させる
- 施設レベルは施設ごとではなく共通の1つなので、「◯◯のLv-1」は `facilityLevel-1`
- 「生活部門の生産量が半減」は仕組みが無いので、生活部門の忠誠-15へ置き換えた
- モルモは roster にいないので、`cast` の actor は同席するモンスターにした（モルモは地の文）
- 前借り55G・裏金40Gへ減額（原案は80G・60G。序盤の金策として強すぎた）

検証: node テスト38本（`tools/test-debts.js` を新規追加）、`sh tools/browser-tests/run-all.sh` 全通過。
`node tools/sim.js 200`: クリア率 **10.0%〜56.0%**（直前は 12.5%〜54.5%。事件が増えた分の揺れの範囲）。

### ブランチ一本化とテンポの戻し（2026-09-05・Claude）

`claude/owner-playtest-tuning-3vr0tt` と `…-3vr0tt-b03d6e` が 764f2c2 から分岐していたので
b03d6e（CodeXの強欲連鎖・履歴・勝利ファンファーレ）を 3vr0tt へマージした（44618d0、衝突なし）。
以後は **3vr0tt を正**とし、b03d6e は同じコミットを指すだけにする。

オーナーが試遊で「全段を等速3秒以上」を否定。**「強弱を付ける」方式へ戻した**うえで、
連鎖の各段に「止まって見える」下限 `CHAIN_STEP_FLOOR`（攻撃560／OVERKILL800／その他520ms）を置いた。
残したもの: 固定帯の因果説明、連鎖履歴と一時停止、勝利の一拍とファンファーレ。
設計原則第3節・README・テスト（test-battle-pacing / pacing / loot-relay）を同期。
固定入力CHAIN12: 29,013ms → 24,350ms（等速版は伸びていた。畳み掛け版は23,884ms）。
sim 200: クリア率 12.5%〜54.5%（マージ前の3vr0tt側と同水準。戦闘式の変更なし）。

**A・D 完了（同日・Claude）**: A は固定帯の説明を「誰が・何で・誰に・いくら」の4スロット固定語順にし、
段の深さを `data-depth`（1〜6）で色分けした（連鎖履歴の段表示も同色）。
D は `.chain-story` の先頭に予告帯 `#chain-forecast` を足した。タイムラインの `synergy` イベント数を
戦闘開始時に「今日の発動 0/3」と点で予告し、発動のたびに点が灯って `3/3 すべて発動した` になる。
**名前は伏せて数だけ約束する**のが要点（何が起きるかは伏せたまま期待だけ先に作る）。
`play()` で数え、`render()` と `skip()` の両方で加算するので倍速・スキップでも一致する。
戦闘式・数値・保存契約の変更なし。

**I 完了（同日・Claude）**: 戦闘中のモルモを `MormoScene.aside()` として足した。
全画面の `show()` と違い操作を奪わず自動で消える「野次」。台詞は `src/data/mormo_lines.js` の
`MORMO_BATTLE_LINES`（chain / discovery / wipe、各3本）。
**1戦闘1回**を守るため、後追いの判定ではなく `play()` でタイムラインを先読みして出る場面を1つだけ決める
（`BattleScene.pickMormoAside`）。優先度は 全滅 ＞ 初めて見るシナジー ＞ 5段以上の連鎖。
全滅の一言だけは `finish()`（`stop()` の後）で出す。`stop()` より前に出すと自分で片付けてしまう。
`result.wipe`（"player" / "enemy" / null）を新設。`permanent` / `reversal` / `firstDiscovery` と同じ
**重要度の印**で、勝敗確定後に導出して付け、描画側だけが読む（2-1節に追記済み）。
テストは `tools/browser-tests/mormo-aside.js`。

**G 完了（同日・Claude）**: イベント本文を立ち絵＋吹き出しに割った。
**`events.js` の契約（`text` は1本の文字列）は変えていない**。UI 側（`UI.eventScript`）が本文の 「…」 を
台詞とみなし、直前に名前が出ている登場人物へ割り当てる。同じ行に2人いれば台詞に近い方、
名前が無ければ直前の話者、登場人物が1人ならその者。**誰にも割り当てられない台詞は地の文のまま残す**
（嘘の話者を作らない）。モルモは cast になくても話者になれる。
つまり E・H の台本側は**本文に「」を書くだけ**で吹き出しになる。これが H の受け皿。
`cast` の面々は忠誠つきで並べ、選ぶ前に誰の話か分かるようにした。
結果画面でも立ち絵を出すため `st.eventCast`（uid だけ・表示専用）を追加。`eventOutcome` と同じ寿命。
テストは `tools/browser-tests/eventcast.js`（割り当て規則を直接検証）。

**次にやること**: `docs/WORK_SPLIT_2026-09-05.md` の分担表。Claude の残りは B（戦果にシナジー名と戦意倍率）。
E（イベント16→30）と J（表情差分）が入れば G の器がそのまま効く。C（シナジー母数8→16）は下書きのまま未採用。

**C 混成シナジー 8→15（同日・CodeX 下書き → Claude 調整）**
異種を混ぜると既存特性を別種族へ貸す7件（`src/data/synergies.js` 末尾、`grant: true`）。
CodeX の下書きはゴブリン統一+求人が16%→66%へ跳ねて止まっていた。原因は、新7件のうち3件が
ゴブリン3〜4体を条件に持ち、ゴブリン軍だけ《魔王軍完成》の段が+3（+60%）されること。
→ **grant 型は《魔王軍完成》の発動数に数えない**（`Synergy.active/applyAll/measure` の3か所）。
必要数は「2体＋2体」へ下げた（8体前後の軍団で狙える大きさ）。骨炭魔術だけは魔法職1＋アンデッド2だと
全戦略で毎回出たので魔法職2へ。sim 200: クリア率 15.5〜56%（変更前 13〜59.5%）。
テストは `tools/test-mixed-synergies.js`。下書きの計測は `docs/drafts/cfj-2026-09-05/measure/`。

**壊してはいけない前提**: grant 型を《魔王軍完成》に数え始めると、ゴブリン系が再び60%超へ跳ねる。
倍率を足す型と特性を貸す型は別物として扱う。

**試遊指摘: 最上段のダメージ数字が見えない（同日・Claude）**: `battlefield.css` が数字の出発点を
立ち絵の頭上65pxに置き、そこから更に34px浮くため、最上段の者では戦場の上端・VS帯に重なって読めなかった。
出発点を胸元（-26px）へ下げ、`z-index: 8` で隣の立ち絵より前に。数字自体も一回り大きく（17/24px）。
`.bu-vfx-anchor` の -65px は斬撃VFXの位置なので触っていない。

**F の取り込み（同日・CodeX → Claude）**: CodeX が制限直前に `codex/f-wip` へ退避した分を cherry-pick。
`battle.js` の unit に `starved` `feast` `chainDepth` を持たせ（保存しない）、追撃中は `duringChain: true` の
ハプニングだけ判定するようにした。7件追加、確率は0.3〜0.4%。sim 50 で不祥事は1.06倍、クリア率 8〜48%。
C（シナジー追加）は `docs/drafts/cfj-2026-09-05/` に下書きと計測のまま。ゴブリン+求人が68%へ跳ねたので未採用。

**H の流し込み（同日・Claude）**: Gemini の台詞325本を `monsters.js` の `voices` へ入れた。
既存7場面は各3本追記（4→7本）、`hungry` `feast` `chain` を新設（各3本、**まだどこからも参照されていない**）。
モルモ25本は `src/data/mormo_lines.js` の `MORMO_LINES`（report/panic/joy/worried/angry）。
これも未参照。`main.js` の `report()` 呼び出しが固定文なので、I で置き換えるときに使う。
ゲーム用語寄りの7本は流し込み時に感情表現へ書き換えた（`docs/GEMINI_LINES_H.md` に記録）。

- **尺テストの注意（2026-09-06・Opus）**: `ranged`/`species` は攻撃動作の上限を
  「等速・scale1で1秒以内」としか見ていない（`min(950, …)` の値そのものは見ていない）。
  上限を1秒より長くする変更をするなら、この2本の上限検査も一緒に見直すこと。

### 読める戦闘と勝利の区切り（2026-09-05・試遊後の最新指示）

**（同日夕方に撤回。等速化はオーナーが再試遊で否定し、上の「強弱＋下限」へ戻した。履歴・一時停止・勝利の区切りは残っている）**

オーナーが前回の速さを試遊で否定。以下は過去の「中間を畳み掛ける」方針を上書きする。
等速は遅めの読者が基準。連鎖各段は最低3秒、文章量で最大6.5秒まで延長し、自動圧縮を廃止。
攻撃動作自体は短く保ち、読む待ち時間を作る。起点と「なぜ誰が何をしたか」を固定帯に残す。
連鎖履歴は全イベントを保持。展開時に進行を止め、「読むために停止／再開」でも制御できる。
勝利は戦闘BGMを止めて0.9秒待ち、勝利表示とオリジナルの約3.2秒のファンファーレ。
表示後3.5秒で次へを有効化。勝利の間は倍速で縮めない。明示スキップは即完了できる。
戦闘式・数値・保存契約の変更なし。設計原則第3節とREADMEも最新方針へ更新。
検証: nodeテスト36本、実戦CHAIN12のPC x1／スマホx2／低モーションx4、通常再生とskipの一致。
reading-victoryで履歴展開中の停止・再開、勝利の一拍・一度だけの合図・倍速でも4.4秒の区切りを確認。
pacing・spectacleも通過。音のテストで約3.2秒の曲長・倍速不変・ミュートを確認。
第15節ゲートでは3・5・7を重視：自分が作った連鎖を理解して次の編成へ活かすための修正。

**次にやること**: 等速で初見でも発火理由を読み切れるか、履歴の説明と勝利の区切りを試遊評価。
食料・死霊系の縦検証もこの読める速度を基準にする。

### 略奪の人材間連鎖と演出の緩急（2026-09-05・Codex）

オーナーは「起点を読ませる→畳み掛ける→決着を読ませる」方針で実装を依頼。
Claudeの `owner-playtest-tuning-3vr0tt`（764f2c2まで）を統合して続行した。
以下が過去の「CHAINの各段を延長」「尺の変更は相談待ち」より新しい方針。

- 《強欲》は本人だけでなく、同陣営の金貨獲得にも反応。盗む者と追撃する者を別々に採れる。
  各人・同一chainIdで1回、70%追撃は維持。追撃で新しい金貨が出ると未反応の仲間へ続く。
  死亡者・敵がもういないときは追加行動しない。勝利時のみ略奪予約確定の契約も維持。
- 描画側のplanが一つの鎖の緩急を決定。起点・初めての条件・最後の追撃・最大OVERKILL・
  蘇生・召喚・永久戦死は読む尺を残し、中間反応だけ短くする。イベントの省略はしない。
  同一鎖の全画面OVERKILLは最大のものへ集約。設計原則第3節をオーナー合意に合わせて更新。
- 戦場外の固定帯で起点と直近の因果を表示。人材間の反応は金色の線で受け渡す。
  強欲は事件の赤縞でなく金色。既存打撃08/09を使用し、新しい合成音は追加しない。
- **契約追加**: `chainSummary.deepest.steps[].actorName`（名前またはnull）。
  タイムラインから導出し、戦果で起点の人材を示す。古いセーブは名前表示だけ省略できる。
  イベント種別・報酬・戦功・出撃上限の変更はなし。run.jsの差分はClaudeからの統合のみ。
- スキップでも戦意メーターと因果表示を最後まで適用。ゲージはwidth遷移からscaleXへ変更。
- 試写室の「💰 略奪から仲間へ連鎖（実戦計算）」で本物のBattle.simulateを再生。
  固定入力のCHAIN12を全イベント保持で29,013ms→23,884msにした（x1、従来の深度延長を
  外した基礎尺との比較）。`battle-preview.html#loot` でも直接開ける。セーブは変更しない。

検証: nodeテスト36本。新規loot-relayブラウザテストはPC x1・スマホx2・低モーションx4で
HP・生死・戦意・因果表示の通常再生/skip一致、起点表示、横幅、JSエラーを確認。
旧テストの人数・倍率・忠誠ペナルティの期待値を、取り込んだClaudeの仕様へ同期した。
sim.js 50（各戦略50ラン）の参考値: 最強優先38%、ゴブリン統一24%、魔法職52%、
三部門均衡48%、未払い搾取44%。ゴブリン統一の最大CHAIN平均4.9、代表能力数3.2。
旧版との同一乱数比較ではないため改善率とは扱わない。

**次にやること**: オーナーが実戦計算の試写と通常ランで、起点が分かるか・追撃の勢い・
決着の重さを評価。次の縦検証は食料の蓄積→大技、死霊の崩壊→反転。
新種族量産・継承・長編化の前に、この3系統の戦い方の違いを確認する。


### 残タスク監査（2026-09-04・Codex）

最新をpullして残件を確認。味方11種・敵10役の6ポーズ制作と物理音08/09の選定は完了済み。
骸骨兵idleの右上に混入した隣コマの槍先を内蔵image_genで除去する編集を2回試したが、
どちらも1254×1254のRGB画像でチェック柄背景が焼き込まれたため不採用。本番画像とCSSクリップは維持。
次にやること: 骸骨兵の透過を維持した局所修正、初見導線・5対5・増援多数時の試遊評価。
生成を再試行する場合は見た目だけで判断せず、RGBAと背景alpha=0を確認すること。
今回の変更は引き継ぎ整理のみ。ゲームコード・数値・保存契約の変更なし。
### 連鎖の爆発化と戦意（2026-09-04・Claude）

演出を派手にしても「爆発力が上がったふうに見えない／ふつうの戦闘が淡々と」という
オーナー指摘が残った。原因は演出ではなく設計だった。

**1. 連鎖は進むほど弱くなっていた**
伝播ダメージが「余剰×0.22」固定で、余剰は撃破のたびに小さくなる。
段が進むほど威力が落ちるので、演出だけ盛り上がって数字はしぼんでいた。
→ 段で倍率を伸ばす（1段目35% → 2段目60% → 3段目85%）。

**2. OVERKILLに見返りが無かった**
伝播の入口になるだけで、それ自体には何の得も無い。「ボーナスを明示」しようにも
中身が存在しなかった。
→ **戦意（momentum）** を追加。余剰を出すほど味方全員の与ダメージ倍率が
   その戦闘のあいだ積み上がる（上限×2.2）。**`momentum` イベントを戦闘タイムラインへ追加（契約の追加）**。

**明示のしかた**
- 戦意メーターを戦場へ常設。開始から ×1.00 が見え、上がるたびに叩かれ、
  +N% が浮き、ゲージが伸び、色が上がる（白→橙→赤）
- 連鎖のキャプションに「連鎖N段目！ 余剰の◯%が流れ込む」
- OVERKILLの全画面に「余剰 N ダメージ → 魔王軍の戦意へ」と行き先を書く
- 連鎖の段と戦意の高さで**ダメージ数字そのものを最大2.2倍の大きさで出す**

**連鎖しやすく**: 必要余剰率を `125-25×段数`（下限40）→ `60-15×段数`（下限15）へ。
ただし OVERKILL イベント自体が `OVERKILL_MIN_PERCENT: 40` 以上でしか出ないため、
実効の下限は40%。ここを下げたいなら battle.js の定数側を触ること。

**壊してはいけない前提**
- **伝播の倍率は段で増やす。** 減衰させると演出が何をしても「淡々」に戻る
- **戦意は戦闘中に下がらない。** 単調増加であることを spectacle.js が縛っている
- 戦意の上限は ×2.2。青天井にすると1戦目から壊れる

**現在のバランス**: sim 200ランでクリア率 12.5%〜52.5%。
経営が回る2戦略（三部門均衡・未払い搾取）が50%をわずかに超えるが、
戦意を切っても同水準なので食料改修由来の水準であり、戦意が原因ではない。

**次にやること**: 実プレイで「連鎖が爆発に見えるか」。まだ足りなければ、
残っている手は連鎖の尺（設計憲法 第3節で伸ばすと決めてあるので方針変更の相談が要る）。

### シナジーと連鎖の演出（2026-09-04・Claude）

オーナー指摘「シナジーと連鎖の演出をもっとド派手に。今はあっさりしすぎ」から着手。
実際、シナジーは細い帯が1本流れるだけ、連鎖は数字が一瞬光って消えるだけだった。

**オーナーと合意した方針**
- 大きいものだけ画面を止める（通常のシナジーは帯のまま、テンポを殺さない）
- CHAINカウンタは積み上がって残る（点滅ではなく居座る）

**入れたもの（9fe8ccd / 05b847c）**
- 全画面演出 `BattleScene.burst()`。《魔王軍完成》と余剰100%以上のOVERKILLだけ。
  集中線→二重の衝撃波→巨大な文字→到達に使ったシナジー名が1枚ずつ立つ
- CHAINカウンタを作り替え。同じ鎖のあいだ数字が残り、段ごとに叩かれ、
  色温度が上がり（白→黄→橙→赤）、横に目盛りが積まれ、途切れたら「×N」で締める
- 伝播の稲妻 `bolt()`。2体のあいだに実際に線を渡す
- 連鎖が深いほど戦場が焼ける（heat-1〜3）、全画面演出中は戦場ごと寄る（zoomed）
- OVERKILLと伝播に音を追加。**選定済みの打撃原音08/09のみ**を、段に応じて
  強く・重ねて鳴らす。新しい合成音は作っていない

**壊してはいけない前提**
- **通常のシナジーで全画面演出を出さない。** 同時発動5のとき5回止まるとテンポが死ぬ。
  `spectacle.js` がこれを縛っている
- **CHAINカウンタは単調増加。** 途中で戻ったり点滅に戻したりしない
- **見せ場の音に新しい合成音を作らない。** 過去に何度も失敗しているオーナー方針。
  既存の thwack 08/09 を重ねる・音量を上げるだけにする
- 稲妻・フラッシュ・ズームは低モーションでは出さない。ただし何が起きたかは読める

**確認の仕方**: 試写室（`battle-preview.html`）の「🔥 シナジーと連鎖の演出を確認」。
積み重ね→魔王軍完成→OVERKILL→伝播→CHAIN6→締め、を通しで再生する。

**次にやること**
- 実プレイでの体感確認。特に「連鎖の尺」。設計憲法 第3節でCHAINは「縮めず、
  大きさに応じて伸ばす」と決めてあるため今回は触っていないが、
  実際に6段の連鎖を見ると間延びする可能性がある。畳み掛ける方向へ倒すなら方針変更の相談が要る
- 敗北の継承（ロマサガ2式）。オーナー選択は「敵は強いまま、継承で追いつく」


### シナジーの爆発化と指名求人（2026-09-04・Claude）

オーナー試遊の指摘「シナジーのインフレ具合がまだ快感まで結びつかない」から着手。

**実測でわかったこと（sim 200ラン・戦闘ごと）**
- 戦闘あたりの同時シナジーは平均0.07〜0.54、**最大2**
- **7〜8割の戦闘が最大CHAIN深度2**（攻撃→死亡）で終わり、連鎖していない
- OVERKILLは正常（平均42〜79%・最大867%）。壊れているのは連鎖のほう

原因は倍率ではなく構造。シナジーは8個しかなく、大半が「同種3体」で**出撃5枠を奪い合う**。
ゴブリン3体と魔法職3体は5枠に同居できず、組み合わせが原理的に成立していなかった。

**入れたもの**
1. 発火条件と効果対象の分離（57a4748）。`Synergy.active/applyAll(units, ctx)` の
   `ctx.pool`（軍団全体）で数え、効果は `units`（出撃した者）だけに乗せる。
   部門へ回した者も条件に参加でき、枠の奪い合いが消えた
2. meta 型シナジーと《魔王軍完成》。通常型を数えた後に判定・適用され、掛け算の2段目になる。
   2つ以上同時発動で、発動数-1につき出撃隊全員+20%
3. 指名求人（04c7374）。`RECRUIT_BRIEFS` 6種を monsters.js へ末尾追記。
   条件はシナジーの発火条件と同じ語彙にしてあり、爆発を自分で狙う手段になる

**壊してはいけない前提**
- **軍団全体で数えるシナジーは、出撃枠を入れ替えても総数が動かない。**
  編成画面の案内を「入れ替える」から「軍団にあと1体（出撃枠はそのままで）」へ直したのはこのため。
  `preview()` の試算も pool 側に足さないと「あと何体」が永久に出なくなる（一度踏んだ）
- **指名求人を確定にしない。** 「出したのに来ない」が消えると採用が作業になる
- **指名求人の求人費は6G。** 9Gも試したが全戦略で損になり罠だった。
  いまは「払える経営を作った者だけが狙い撃ちできる」形で、金の無いビルドでは未払いが増えて不利になる
- 母集団が5→最大20に広がったので、必要数と刻みを下げてある
  （ゴブリン4体+12%／魔法職4体+18%／アンデッド3体+22%）。ここを戻すとクリア率が跳ねる

**現在のバランス**: sim 200ランでクリア率 10.5%〜54.5%。同時シナジーは最大2→最大5。

4. 積んだ枚数を連鎖に変えた。《魔王軍完成》が立っているあいだ、味方のOVERKILL撃破は
   次の敵へ伝播する。深さは同時発動数（最大4）、必要な余剰率は `125 - 25×段数`（下限40%）。
   積むほど深く、かつ点きやすくなる。倍率だけだと数字が増えるだけで爆発に見えないため

**壊してはいけない前提（追記）**
- **伝播は「深さ」だけ上げても伸びない。** 最初は上限深度だけ発動数で伸ばしたが、
  余剰100%（相手の最大HPの2倍を一撃）が滅多に起きず、深度4以上は0%→7%までしか動かなかった。
  必要な余剰率も一緒に下げて初めて0%→18%になった
- 伝播は味方側だけ（`attacker.side === "player"`）。魔王軍の編成が起こすものなので

**次にやること（オーナーと合意済みの順番）**
1. **敗北の継承（ロマサガ2式）。** オーナー選択は「**敵は強いまま、継承で追いつく**」。
   全滅→軍団だけ組み直し、王国攻略度・施設・魔界史・発見済みシナジーは維持。
   前代の将軍の能力を継ぐなどで追いつかせる。いまは「再起＝巻き戻し」と「代替わり＝全リセット」の
   2つしかなく、継承はその中間に入る新しい層
2. シナジーの母数を8個から増やす（data層・CodeX向け。排他性は上で壊してあるので個数が効く）
3. サキュバス・ミノタウロス・ドラゴンなど魅力的なモンスターの追加（CodeX向け）。
   指名求人に「ビジュアル重視」を足すには、テンプレ側に見た目の属性が要る（今は無いので未実装）


### 食料経済の作り直し（2026-09-04・Claude）

オーナー試遊の指摘「食料がいったん不足すると修復が難しい／必要食糧がはっきり書かれていない」から着手。

**実測でわかったこと（tools/sim.js 200ラン）**
- ほぼ全ランが3〜9戦目に食料不足へ落ち、**以後の完全回復は0〜3%**。片道ドアだった
- ところが「不足したら戦闘力の低い者を生活部門へ回す」だけで**クリア率27%→76%**。
  解法は一手で存在するのに、判断材料が画面に無かっただけ
- 余剰食料は死に資源で、黒字ランの平時備蓄は16.8まで積み上がっていた。
  そのため食料を消費する選択肢は常に損になり、判断が生まれなかった

**入れたもの**
1. 収支の開示（ae85963）。`Game.foodBalance()` に計算を集約し、HUD・応募者カード・
   編成画面が同じ数字を見る。適性チップの単位を明示（調達 食料+N／食う量 N口・3口＝食料1）
2. 備蓄上限 `foodCapacity()`（消費×4・最低8）と宴 `holdFeast()`（8863cd5）。
   上限超過は決算時に傷む。宴は食う者の忠誠+6と与ダメージ+15%（次の1戦のみ）
3. 飢餓適応 `starved`（3223116）。3戦続けて飢えを生き延びた者は食料を消費しなくなる。
   代償は最大HP-15%。不足の一方通行を出口へ変えた

**壊してはいけない前提**
- **上限が無いと宴は罠になる。** 上限を入れる前は宴あり27%→20%で明確に損だった。
  余剰が「保険」として無限に強く、それを上回る使い道が作れなかったため。
  食料を消費する要素を足すときは、必ず上限とセットで考えること
- **食わない者は飢えても忠誠が下がらない。** アンデッドと飢餓適応者。
  ここを戻すと骸骨・飢餓ビルドが成立しなくなる
- 宴は「宴のあとに2戦ぶんの糧食が残る」ときだけ開ける。連打しても飢えない設計

**現在のバランス**: sim 200ランでクリア率 5.5%〜53.0%（三部門均衡 40.0%）。

**次にやること**
- オーナーが実際に遊んで、飢餓適応が「発見」として機能するか（3戦耐える判断が起きるか）
- 宴のタイミング判断が実戦で成立しているか。simでは無条件連打で中立なので、
  「ボス前に開く」等の選択で勝てるかは未検証
- 巨大厨房に備蓄上限を上げる効果を付ける案（施設と食料の接続が今は薄い）
- `starved` の絵・台詞・イベントは未着手（CodeX向け）


### 効果音のオーナー選定（2026-09-04）

「鈍い打撃の8か9、基本全部それ。魔法は別」の指示を反映。
物理攻撃は AntumDeluge thwack 08/09 の原音を交互再生。武器別・重量別の加工はしない。
魔法通常攻撃は描画側の attackKind を渡して既存の魔法音へ分離。防御音は維持。
次にやること: 実戦で打撃08/09の音量と連打感を試遊。魔法音の外部素材選定は今後の候補。
検証: test-sound（交互再生・魔法分離）、browser sound/ranged。core・バランス変更なし。

### 初見導線と実録効果音の試遊準備（2026-09-04・Codex）

第1代・最初の2作戦に、能力条件・軍団との接続・配置・CHAIN経路の短い案内を追加。
編成の最前列バッジは折り返し、名前幅を確保。戦場事件バナーは上部へ移して敵名と分離。
オーナーの最新指示で合成SFXの改善を中止。既存CC0実録12音を整形し物理攻撃・防御へ仮接続。
役割別割当・接触同期・倍速で音高を上げない・同時4音まで。試聴室 `tools/sfx-lab.html`。
過去の「実録の接続保留」は今回の試遊用接続依頼で更新。最終音選びはオーナー試聴待ち。
次にやること: オーナーが採用→編成→戦闘を試遊し、迷った点と音の好みを返す。
検証: test-sound、ブラウザ first-play / report / sound / battlefield。
core・数値・保存構造・戦闘タイムライン契約は変更なし。

### 戦闘アート制作の引き継ぎ（2026-09-04・CodeX）

最新オーナー指示でGemini分担を中止し、Codexが生成・整形・実装・検証を担当する。
既存剣士を基準に5差分を単体生成し、6ポーズを接続。試写室の「剣士の斬撃を確認」で再生できる。
PNG原画と512px透過WebPは `assets/battle/units/swordsman/`。チェック柄RGBの初稿は不採用。
単体用 `scripts/prepare_single_motion.py` は身体基準の寸法補正後、共通倍率・足元492で整形する。
弓兵・聖職者・賢者も6ポーズ接続済み。遠隔は22%発射姿→62%命中→68%戻り。
味方魔法系（魔法使い・インプ・死霊術師）も6ポーズ追加。インプは小跳躍、死霊術師は静かな腕の詠唱。
コボルト（警棒の小跳躍）・ゾンビ（引きずり体当たり）も6ポーズ接続済み。
オーガ（棍棒の重量打撃）・キングスライム（三つの塊の圧縮/突進）も6ポーズ接続済み。
盾兵・投石兵・斧兵も6ポーズ接続済み。盾打撃・投げ・斧の振り下ろしを区別。
騎兵（馬の踏み込み）・指揮官（短い突き）・勇者（大きな斬り下ろし）も6ポーズ接続済み。
味方11種・敵10役の全126ポーズ対応。通常編成とvariantsの全49敵エントリに割当漏れなし。
**次にやること**: 試写室で実際の見やすさを試遊評価。量産の残りはなし。骸骨兵idleの原画槍先修正は残件。
全役の左右・390/1280幅・x1/x2/x4・圧縮・低モーション・skip・フォールバックを検証。
最終回帰: species通常/orc、battlefield、ranged、arrival、vfx-lifecycle、art-coverage。
モルモ・core・数値・戦闘タイムライン契約は変更していない。
下のGemini制作待ち・全種族量産保留は過去の方針であり、今回の明示依頼により更新された。

### Gemini向け敵剣士制作キット（2026-09-04・CodeX）

オーナーと「敵剣士1体で画風確認→差分を1枚ずつGemini制作→組み込み」を合意。
`docs/gemini-enemy-swordsman/` に既存待機WebPの参考コピー、6依頼文、受け入れ基準を用意。
Geminiには未送信。まず参考画像と01-idleの本文を送り、生成PNGをオーナーから受け取る。
**次にやること**: 待機絵を確認後、同じidleを毎回添付して5差分を作る。画像承認まで本番は変更しない。
右向き素材→敵配置時に左右反転。共通倍率・足元で512WebP化し、単体PNGをシート分割へ渡さない。

### オークの重量級モーション（2026-09-04・CodeX）

オークに6ポーズの戦闘専用絵と、溜め→斧の振り下ろし→踏ん張り→復帰を追加。
命中38%・総尺を維持して、重さはポーズと停止区間で表現。待機も重い呼吸へ分岐。
試写室の「オークの斧撃を確認」で確認可能。素材は `assets/battle/units/orc/`、
生成プロンプト/再生成手順は `docs/BATTLE_SPECIES_MOTION.md`。数値/契約/履歴書は変更なし。
**次にやること**: 次の種族候補はコボルト。骸骨兵の原画コマ境界改善は引き続き残件。

### スライム・骸骨兵の戦闘モーション（2026-09-04・CodeX）

2種に戦闘専用6ポーズを追加。スライムは圧縮→跳躍→体当たり→弾性復帰、骸骨兵は
身を引く→槍突き→引き戻し。被弾したスライムも潰れる。HP/音の接触38%と総尺は維持。
スライムに斬撃を表示しない。待機も種別に変え、低モーションでは停止する。
履歴書絵・データ・戦闘式は未変更。tplIdなしの召喚物は従来の絵文字（推測マッピングしない）。
`battle-preview.html` に比較ボタン、`tools/browser-tests/species.js` に12画像・左右・倍速・
HP反映・skip・低モーション・フォールバックの回帰試験。既存4関連テストも通過。
素材とプロンプト、再生成手順は `docs/BATTLE_SPECIES_MOTION.md`。
**次にやること**: オーク/コボルトの専用絵、骸骨兵の槍のコマ境界修正を次回原画更新で行う。
現在は待機コマの余白に入った隣コマの槍先をCSSクリップで隠している。

### 召喚・復活とスキップ整合性（2026-09-04・CodeX）

召喚追加を `addSummon`、生死表示を `setLife` に集約し、通常再生とskipで共有。
従来skipがsummonを無視し、増援と後続HP更新が消える不具合を修正した。
召喚は浮上、復活は立ち上がりをactorだけに適用。名前・HPは動かさず、
「召喚」「倒れた」「戦死」を状態ラベルで区別する。6体以上は戦場を縦に拡張。
画像未登録の召喚物は絵文字フォールバックを維持。ルール・数値・イベント契約は変更なし。
試写室に「召喚と復活を確認」を追加。arrivalテストは通常/途中skipの一致、二重召喚防止、
永久戦死、8体配置、低モーションを検証。battlefield/effects/vfx-lifecycle/ranged、
test-summon-chain/test-battle-pacingも通過。
**次にやること**: 増援多数時の縦長戦場を実プレイで評価。専用モーション絵の拡充は別タスク。

### 遠隔攻撃の飛翔体（2026-09-04・CodeX）

`BattleScene.attackKind` で表示だけを分類し、弓は矢、投石係は石、mage/necromancer/impと
敵の✨/📖は魔法弾を飛ばす。遠隔役は最大6pxの構えだけで敵へ突進しない。
動作枠の62%で到達し、そこでHP・数字・音を同期。近接の38%接触とイベント総尺は維持。
投石は中点を28px持ち上げ、弓・魔法は直線で対象へ向かう。飛翔体はルートの一時DOMなので
カードの重なりで隠れず、stop/skipでタイマーとともに除去する。低モーションでは出さない。
既存の画像・写真・絵文字フォールバックは変更なし。新規画像生成・数値バランス変更なし。
`battle-preview.html` の「弓と魔法を確認」で試写でき、`ranged` ブラウザテストで回帰確認。
**次にやること**: 剣士・魔法使い・弓兵の専用モーション絵とゴブリンの中割りを磨く。

### 横向き2D戦場への改修・第1段階（2026-09-04・CodeX）

オーナーは「履歴書絵を動かす方式では足りない。戦闘専用2Dとして一から作ってよい」と指定。
カード枠を外し、左の魔王軍と右の敵が向き合う戦場へ移行した。ゴブリン6ポーズを描き直し、
剣アイコンの敵に共通剣士の全身絵、低コントラストの回廊背景を追加。HP・名前は定位置に残す。
攻撃は溜め→踏み込み→接触→戻り。HP・VFX・効果音は接触時へ同期した。
内部のpendingHitsを停止時に確定するため、命中前にスキップしても表示HPが欠落しない。
**timeline・セーブ・数値バランス契約は変更なし**。`src/core` とモルモは変更していない。

`battle-preview.html` でセーブに触れず1対1／5対5を試写できる。生成プロンプト、素材再生成、
動作レビューと未完部分は `docs/BATTLE_MOTION_REVIEW.md` に記録。
**次にやること**: まずこの試写をオーナーと確認し、ゴブリンの中割り・剣士の攻撃差分・
遠隔攻撃の飛翔体を優先する。全種族の量産はまだしない。6ポーズ＋補間を完成品質とは扱わない。

### 女の子版モルモを正式採用（2026-09-03・CodeX）

オーナー提供の画面参考を基準に、`assets/mormo/female/` へ `welcome`、`report`、`worried`、
`panic`、`angry`、`joy` のPNG原画を生成した。オーナー承認後、背景を除去し、既存契約と同じ
512×512の透過WebPとして `assets/mormo/*.webp` へ正式採用した。旧男の子版は
`assets/mormo/male/` に保存している。`scripts/prepare_mormo_female.py` で原画から再生成できる。
ブラウザテスト `mormo` で全画面表示・画像読込・報告遷移を確認済み。

### オーナー決定: 長編化はUI全面改修の後に扱う（2026-09-03）

現行の正規攻略8拠点・最大5体は、面白さを検証する最小単位としては機能したが、
製品版の最終的なボリュームとして固定しない。オーナーは、数日に分けて軍団の編成・成長・愛着を
楽しめる一周と、将来的な5→7→8体（特殊条件で10体）の段階的な出撃規模を志向している。

ただし、現在は発火が《将軍の号令》《精鋭主義》へ偏り、同じ壊れ方が早く収束するため、
戦数だけを先に増やさない。Fableレビューも踏まえ、次の順で進める。

1. Claudeの最新ブランチを取り込む（`8e97a3e` まで取り込み済み）
2. UI全面改修と戦闘の可視化を先に行い、Webブラウザ風のカード／パネル感をなくす
3. その後、`GAME_DESIGN_PRINCIPLES.md` の最大5体・60秒リトライ契約を正式に改訂する
4. 現在の短いランへ3幕だけを試験導入し、幕間でビルドが変質するか測る
5. 密度を維持できた場合だけ戦数と実時間を伸ばし、段階的敗北を再起の拡張として導入する

長編化の暫定目標候補は3幕・合計3〜5時間だが、これは先に固定しない。
幕ごとの編成指紋変化率、発火種類、新規シナジー、最大CHAINの伸び、特定シナジーへの偏り、
翌日継続率を測り、結果として適切な長さを決める。現行の短いルートは当面sim・回帰試験用に残す。

**重要**: 現行の設計原則第3節は最大5体を維持し6体目以降を標準化しない契約である。
長編化・枠拡張の実装は、必ず設計原則を先に改訂してから行う。UI改修では将来8〜10体を
収容できる画面構造を意識してよいが、ゲームルールの上限はまだ変えない。

### UI全面改修の進捗（2026-09-03・CodeX）

- 第1段階: 共通シェルを魔王城の執務卓へ変更。採用は羊皮紙の履歴書、編成は石板、作戦は作戦令として分離
- 第2段階: PCの部門編成を「左＝作戦・給与／中央＝配置盤／右＝発火予測・敵情・出撃」の三面作戦卓へ変更
- 第3段階: 戦闘を大きな戦場＋右側の紙の戦況記録へ再構成。ログを主役から補助へ下げ、ユニットと発火表示を大型化
- 第4段階: 作戦会議を三本の進軍路が並ぶ軍議卓、戦果を報告書、魔界史を縦につながる年代記へ変更
- 戦闘演出: 陣営方向の攻撃軌道、CHAIN段数の戦場内表示、蘇生と召喚で異なる登場アニメーションを追加
- 第5段階: 施設選択を築城現場と設計図、事件をモルモ提出の緊急案件、ラン終了を「魔王軍事業終了届」へ変更
- スマホは同じ情報を一列へ戻す。既存の `data-action` とテスト用DOM契約は維持
- 将来8〜10体を収めるためPC幅を1180pxへ広げ、中央配置盤は2列の人材札を基本にした

UI全面改修の主要画面は一巡した。次は実機で文字密度とスクロール量を見て微調整し、
その後モルモ男女差分か、長編化前の3幕設計へ進む。

### 採用履歴書の可読性調整（2026-09-03・CodeX）

オーナーの実機スクリーンショットで、羊皮紙上の階級（兵卒）、戦功、適性タグが暗背景用の淡色を
引き継いで読みにくいことを確認した。`game-scene-recruit` 内だけ濃いインク色・薄い色面・太字へ
上書きし、紙の縦罫線も不透明度を下げた。暗背景側の共通色と他画面の表示契約は変更していない。

### 画像VFX＋戦闘立ち画パイロット（2026-09-03・CodeX）

戦闘ルールとタイムライン契約は変えず、対象カードへ一時画像を重ねる `.bu-vfx-anchor` を追加した。
`slash`、`impact`、`guard`、`revive`、`overkill` の透過WebPを生成・圧縮し、攻撃・耐え・蘇生・
OVERKILLへ接続済み。画像読込失敗時は既存CSS演出だけが残り、スキップ時は残ったVFXを除去する。

戦闘専用立ち画はゴブリンだけをパイロット実装した。待機は
`assets/battle/units/goblin/idle.webp`、攻撃時は `attack-windup.webp` へ短時間切り替わる。
履歴書写真 `assets/monsters/goblin.png` は変更していない。他種族へ展開するときは一括生成せず、
同一性・透過・96px表示を1種ずつ確認して `BattleScene.BATTLE_SPRITES` へ登録する。

### 戦闘画像のライフサイクル修正（2026-09-04・CodeX）

画像VFXと浮遊数字のCSS再生時間・除去タイマーを同じ速度／イベント圧縮倍率へ同期した。
ゴブリンの攻撃ポーズも圧縮倍率へ追従。停止／スキップでは字幕・カットイン・数字・一時クラスを
除去し、死亡状態と決着表示は残す。画像スキルの契約に従い、戦闘絵→履歴書写真→絵文字の
二段階フォールバックを修正し、失敗した立ち画を攻撃／停止時に再要求しない。
`vfx-lifecycle` テストでx1/x2/x4、圧縮倍率0.45、スキップ、二段階読込失敗を確認。
次は他種族の立ち画を1種ずつ試す。戦闘ルール・バランス・モルモは変更なし。

### この区切りまでに完了・push済み

| コミット | 内容 |
|---|---|
| `bbf3b5b` | 初回3日間を撤廃し、編成へ「最前列へ」を追加 |
| `a368ff9` | 略奪＝資金食料、鎮圧＝忠誠建材、侵攻＝決戦進行を作戦カードへ明示 |
| `1ba62ac` | 無料採用枠後も面接を閉じず、紹介料4G→8G→16Gの追加採用と明示終了を導入 |
| `dd00414` | 恐喝帳簿・巨大厨房・墓地を同時1つの選択式大型Joker施設に変更 |
| `ddbf8cc` | 施設3択へ現在の発火可能／不足人材・配置を表示 |
| `3fee470` | 稼働施設の `links` を履歴書の採用接続ヒントへ統合 |
| `4a82605` | 有料追加採用数・紹介料をKPIへ追加し、sim比較戦略を実装 |

Fableによる設計レビューも回収済み。「今すぐ直す」3点（施設選択時の接続状態、施設と履歴書の接続、
追加採用の測定）は上記3コミットで完了した。Geminiには今後、既存語彙を横断する橋渡し能力、
状態連動ハプニング、ビルドへの問いとなる敵編成の**案だけを大量生成**させ、コードは触らせない方針。

### 最新の実プレイ所感

オーナーが施設Lv最大・墓地系のランを1回クリア。2倍速のため施設発火自体は判別しづらかったが、
同じゾンビが二度立ち上がるように見え、「死の軍勢は粘り強くて強い」「壊れすぎず、ちょうどよい」
という感触だった。ここで見えたしぶとさは、次が重なった可能性が高い。

- ゾンビ自身の《執念》による致死耐え
- 死霊術による蘇生と、《死の軍勢》による全快化
- 墓地による**別個体の骸骨従者召喚**
- 施設Lv.3の旧来共通補正（全員HP+12%・防御+2）

墓地は同じゾンビを蘇生する効果ではない。ところが2倍速では、どれが誰の手柄か分からなかった。
数値を強化する前に、戦闘結果へ「墓地：骸骨従者1体／全滅回避」「城郭Lv.3：HP+12%・防御+2」
「執念→戦死→死霊術で全快蘇生」のような**施設・死者CHAINの短い戦果要約**を残すのが次候補。

### 実プレイ進行中のメモ（2026-09-03・オーナー）

スライムを揃えてキングスライム合体で進行中（未クリア）。**施設の恩恵はまだ感じていない**。
スライム統一は sim でも施設到達0%の戦略なので、これは想定どおりの「建設に振らないと施設は存在しない」
状態。試遊後に見るのは「建設に振らなかった理由」（建材が出ない／建設担当を出撃から外したくない／
施設が何をするか分からなかった）のどれか。次のチャットは Opus で継続してよい。
このメモの続きは、ラン終了後に `copy(KPI.export())` → `node tools/kpi-report.js` の結果と一緒に書く。

### 測定済み・まだ変えないこと

有料追加採用の200ラン比較：ゴブリン統一33.0%、追加採用33.5%、求人引き直し22.0%。追加採用は
287人・1196G、最大CHAIN平均は通常3.9→追加4.6。勝率の自動正解ではなく連鎖完成へ効いているため、
紹介料や「採用後に求人費が無料へ戻る」契約はまだ弱体化しない。

施設は多くのsim戦略で平均Lv.0。試遊で以下を確認するまで、到達率・維持深化・共通補正を同時に変えない。

1. 5ラン中、施設選択へ何回到達したか
2. 条件未達の施設を将来の採用目標として選んだか
3. 追加採用を毎戦とりあえず4Gで使ったか
4. 同じ施設を維持する理由があったか
5. 一律HP・防御補正とJoker効果のどちらを体感したか

### 拠点接収を入れた（2026-09-03・Claude / 0節3番の分岐A）

**分岐の根拠**: オーナーの実プレイ結果はテンプレのまま未記入だったため、オーナー確認の上で
A（施設到達率）を選んだ。決め手は sim へ足した施設到達率の行の実測で、到達が
「建設に人を割く2戦略が100%／残り13戦略が0〜2%」という**二値**だったこと。
施設は「届きにくい」のではなく、開始時の部門配分ひとつで決まっていて、途中の判断ではなかった。
原因は `processDepartments` の `materialUsed = min(materials, buildCapacity)`。
建設部門が空だと `buildCapacity` が 0 で、ミッション報酬の建材は備蓄に貯まるだけで
永久に施設へ変換されない（作戦画面にも「施工役なし（N建材は備蓄）」と出ていた）。

**入れたもの**（`a428366`）: 勝利後・施設Lv.0のときだけ結果画面に出る**拠点接収**。
備蓄建材3を投じてLv.1へ届き、大型Joker施設の3択へ入る。**1ランに1度きり**。
代償は王国警戒度+1（`Game.SEIZE_ALERT_COST`。敵スケールが約2%上がる）。
Lv.2/3は従来どおり建設部門の仕事として残してあるので、建設ビルドの価値は削っていない。
API は `Game.canSeizeStronghold()` / `Game.seizeQuote()` / `Game.seizeStronghold()`、
状態は `st.seizeUsed`。sim は条件を満たせば必ず接収し、`拠点接収 N回` として出力する。

**実測（`node tools/sim.js 200`）**:

| 戦略 | Lv1到達 前→後 | クリア率 前→後 |
|---|---|---|
| 最強優先 | 0% → 99.5% | 42% → 41.0% |
| スライム統一 | 0% → 100% | 62% → 53.5% |
| ゴブリン統一 | 0% → 99.5% | 24% → 36.0% |
| 骸骨寄せ | 0% → 100% | 22% → 27.0% |
| 略奪4回→侵攻 | 0% → 100% | 24% → 18.5% |
| 三部門均衡（元から到達） | 100% → 100% | 33% → 31.5% |
| 未払い搾取（元から到達） | 100% → 100% | 37.5% → 37.0% |
| 精鋭3体 | 0% → 24.0% | 12% → 14.0% |

全戦略が9.5〜53.5%の帯内。**到達だけが動き、勝率は動いていない**のが狙いどおり。
警戒度+3・+2も測ったが、+3は全戦略で一律にクリア率が下がった（最強優先28%、スライム統一46%）。
勝率を上げるためではなく選択肢を増やすための入口なので+1を採った。

**注意（次に測る人へ）**: 50ランだと三部門均衡が22%まで落ちて見えたが、200ランでは31.5%で
ゆらぎだった（接収回数は200ラン中6回しかなく因果もない）。**この機能まわりは50ランで判断しない。**

**まだ残っていること**:
- 精鋭3体だけ到達24%。戦闘数が短く建材3が貯まらない。少数精鋭に施設を届けたいなら
  必要建材か `FACILITY_LEVELS[1].buildThreshold` 側を見る（今は触っていない）
- Lv.1の共通補正はHP+5%しかないので、接収の価値はほぼJoker3択の中身で決まる。
  0節4番（共通補正の撤去判断）は依然として墓地単体の深化とセットで行うこと
- **未検証**: 実プレイで接収が「悩ましい選択」になっているか。simは必ず接収するので、
  人間が見送る場面があるかは測れていない。次の試遊で見るのは
  「接収を見送ったランがあったか」「見送った理由は警戒度か、建材を建設に回したかったからか」

### 共通HP・防御補正を撤去し、施設Lv.をJokerの回数にした（2026-09-03・Claude / 0節4番）

**⚠ 契約変更あり。`src/data/departments.js`（CodeX担当層）も触っている。**

**なぜ**: 施設Lv.3の「出撃隊全員のHP+12%・防御+2」は、設計憲法 第9節が名指しで禁じている
「恒久成長を単純な数値上昇にする」形そのものだった。さらに大型Jokerは3施設ともLv.に関係なく
1戦闘1回しか働かず、Lv.を上げても**その軍団の壊れ方は一切濃くならなかった**。施設が個性ではなく
平均値だった。

**何を**（`d679011`）: **施設Lv.N ＝ 稼働中の大型Jokerが1戦闘にN回働く**、へ一本化。

| 施設 | 旧 | 新 |
|---|---|---|
| 墓地 | 最初の戦死者1体を召喚 | 戦死者を**N体**まで召喚 |
| 恐喝帳簿 | 予約金貨3G到達で1回 | 予約金貨3Gごとに**N回**まで発火 |
| 巨大厨房 | 食事強化を一律2倍 | 食事強化を**(N+1)倍** |

**契約（CodeXへ）**:
- `FACILITY_LEVELS` に `works` を追加。`hpMult=1` / `defBonus=0` は旧セーブ互換で残してあるが
  **もう誰も読まない**。施設を強くしたいときは `works` を増やすのではなく、各Jokerの中身を濃くする
- `Battle.simulate` の options に `facilityWorks` を追加（未指定なら1。既存呼び出しは壊れない）
- `Game.facilityWorks()` を追加。戦果 `lastBattle.facility` に `works` を含めた
- 触ったのは `FACILITY_LEVELS` の構造と `FACILITIES` の `desc` 文言のみ。
  絵・モンスター・台詞・イベントには触れていない

**実測（`node tools/sim.js 200`。撤去前＝拠点接収を入れた直後の200ラン）**:

| 戦略 | 撤去前 | 撤去後 |
|---|---|---|
| 最強優先 | 41.0% | 37.0% |
| ゴブリン統一 | 36.0% | 36.5% |
| スライム統一 | 53.5% | 43.5% |
| 骸骨寄せ | 27.0% | 26.5% |
| 魔法職寄せ | 45.0% | **50.0%** |
| 精鋭3体 | 14.0% | **16.5%** |
| 略奪4回→侵攻 | 18.5% | 14.0% |
| 慎重経営 | 13.0% | 7.5% |
| 三部門均衡（建設） | 31.5% | **28.5%** |
| 未払い搾取（建設） | 37.0% | 28.5% |

0節4番が撤去を保留していた理由は「建設ルートだけが一方的に弱くなる（約10pt）」だったが、
**Jokerの深化が返すため三部門均衡は-3ptに収まった**。落ち幅は建設ルート専有ではなく全戦略へ
薄く広がり、厨房が濃くなる魔法職寄せと精鋭3体はむしろ上がった。**保留の前提は解消した。**

**残っていること**:
- **骸骨寄せ+求人が 9.5→4.0% と帯の下限を割った。** 元から最弱の戦略（拠点接収前は2.0%）で
  50ランの揺らぎ幅にも近いが、次に施設か骸骨まわりを触る人は最初にここを確認すること
- `tools/sim.js --no-facility-bonus` は削除した。run.js が `hpMult` を読まなくなり旧仕様を
  復元できないため。撤去前後の数値は上の表が唯一の記録
- **未検証**: 実プレイでLv.2/3が「同じ施設を維持する理由」になっているか。
  simは施設を途中で建て替えないので測れない。次の試遊で見るのは
  「Lv.2へ上がったとき墓地が2体起こしたのが分かったか」「別の施設へ建て替えたくなったか」
- 0節4番はこれで**完了**。次の未着手は0節5番（忠誠・給与を罰からビルド入口へ／CodeXの案出し先行）

### 魔界史にビルド名を残した（2026-09-03・Claude / ロードマップ⑩）

**なぜ**: 魔界史に残るのが「第N代・M戦で敗北・最終施設Lv.2」という台帳の数字だけで、
**負けたランが記録として何も語っていなかった**。設計憲法 第7節（敗北を笑える／人に話したく
なるものにする）・第11節（歴史を残す）に対し、ロードマップ⑩は未着手のままだった。

**何を**（`17144bd`）: ラン終了時に record の中身だけから名前を作り、終了画面と魔界史へ出す。

> 「墓地を三度も回した死の軍勢」「給料を払わなかったゴブリン軍団」
> 「誰ひとり死なせなかったオーガの一党」「特筆すべきことのないスライム軍団」

名前 ＝ 〈修飾〉＋〈中核〉＋〈体裁〉。
- **修飾**は `Game.BUILD_TRAITS`。そのランで起きた**いちばん珍しいこと1つだけ**。
  2つ並べると「墓地を三度も回した屍を積み上げた死の軍勢」となって読めないため1つに絞った
- **中核**は `Game.BUILD_CORES`。発見済みシナジーがあればそれを名乗り、無ければ主力種族
- **KPIには依存しない**。材料はすべて record の中にあるので、simでもテストでも同じ名前が出る
- record へ `seizeUsed` と `buildName` を追加した

**修飾を足すときの唯一の注意**: `BUILD_TRAITS` は**上にあるものほど珍しい**という前提で並んでいる。
最初の実装では拠点接収と再起を中位に置いたところ、**名前の半分以上が「拠点を接収した〜」
「一度死に損なった〜」に占領された**（simはどちらもほぼ全ランで行うため）。
最下位へ落として 4〜19種/50ラン → 10〜24種/50ラン へ改善している。
新しい修飾を足すときは、必ず `node tools/sim.js 50` のビルド名の行で占有率を見ること。

**測り方（新しい物差し）**: `node tools/sim.js 50` の各戦略に
`ビルド名: N種/Mラン　多い順 …` が出る。**1種類が半分以上を占めていたら、その戦略は毎回
同じことをしている**（または修飾の優先順位が珍しさを表していない）。ランごとの差の物差しとして使える。

**残っていること**:
- 精鋭3体は2〜8種/50ラン、骸骨寄せ+求人は1つの名前が35/50。どちらも戦闘数が短く
  そもそも何も起きていない。**ビルド名の不具合ではなく、その戦略にランごとの差が無いことの表れ**
- **未検証**: 実プレイで「人に話したくなる名前」になっているか。特に敗北時に悔しさや笑いへ
  繋がっているかは試遊でしか分からない。次の試遊で見るのは
  「終了画面の名前を見て、そのランを一言で説明できたか」「もう1回やる気になったか」
- ロードマップ⑩はこれで**完了**。⑦（失敗を方向転換の材料に）と⑧（愛着）は未着手のまま

### オーナー実プレイ＋Claude実プレイの結果（2026-09-03）

**オーナー（1ラン）**: キングスライム合体＋ゴブリン4体で勝ち切った。「キングスライムが安定していた」。
**施設は一度も使わなかった。** 試し遊びは十分やったので、以後は計測よりブラッシュアップを優先したい、とのこと。

**Claude（Playwrightで実プレイ4ラン）**: 「施設を使わなかった」の原因が**届かなかったからではなかった**。

1. **接収パネルが結果画面の 2049px 地点にあった**（全2316px・画面1000px）。2画面ぶん
   スクロールしないと見えない。実質「無い」。→ 勝利バナー直下へ移動（374px）し、金枠を付けた（`232e099`）
2. **作戦画面が行き止まりに読めた**。建設担当がいないと「施工役なし（N建材は備蓄）」とだけ出る。
   → 「建材Nで勝利後に拠点接収できる」に変更
3. **略奪は建材0、侵攻は1**。略奪中心のルートは建材3に届かず、**入口自体が出ない**。
   実プレイ4ラン中、入口が出たのは侵攻を優先した1ランのみ
4. 施設3択の初回ボタンが「この施設へ建て替える」だった（何も建っていないのに）→「この施設を建てる」
5. ビルド名は動いていた（「拠点を接収した魔法結社」）

**simと実プレイのズレ**: simは施設到達96〜100%と出るが、これは**simが毎ラン鎮圧・侵攻を混ぜて
建材を貯めるから**。人間が略奪や速攻を選ぶと入口が出ない。**到達率をsimだけで語らないこと。**

**オーナーのビルドはsimとも一致している**: スライム統一は施設なしでクリア率43.5%と最上位。
「施設を使わなくても勝てる」のは壊れではなく、現状の設計どおり。
問題は「施設を使う道が見えないこと」であって「施設が弱いこと」ではない。

### 前代の教訓を入れた（2026-09-03・Claude / ロードマップ⑦）

**なぜ**: 敗北がただの損失だった。魔界史に名前は残るが次のランは毎回まっさらで、
「負けたから次はこうする」がゲームの側に繋がっていなかった。

**何を**（`ecaf804`）: 敗北確定画面で「この敗北から、何を学んだことにするか」を3択。
選んだ教訓は**次代にだけ**引き継がれ、一度使うと消える。

| 教訓 | 前代の負け方 | 次代への影響 |
|---|---|---|
| 石工の記憶 | 城をひとつも建てられなかった | オーク・オーガが応募に来やすい |
| 弔いの記憶 | 戦死者5体以上 | 死霊術師・骸骨兵・ゾンビが来やすい |
| 未払いの記憶 | 未払い3回以上／不祥事10件以上 | オーク・ゴブリンが来やすい |
| 兵糧の記憶 | 食料が尽きたまま終わった | コボルト・スライムが来やすい |
| 未完の記憶 | 最大CHAINが2以下 | 魔法使い・インプ・死霊術師が来やすい |
| 敗走の記憶 | 3勝以下で敗走 | 毎回の応募者が1名増える |

**設計上いちばん大事な点**: 効果を「攻撃力+1%」型にしていない（設計憲法 第9節）。
教訓が変えるのは強さではなく**応募プールの偏り＝次に引ける手札**で、該当種族の出現重みを
3倍にするだけ。確定ではないので「来なかった」も起こる。**ここを数値強化に変えないこと。**

**必ず3つ出す**理由: 当てはまったものだけにすると、綺麗に負けたランで選択肢が消える。
当てはまるものを先に並べ、足りなければ残りで埋めている（`Game.lessonOffers`）。

**契約**: `Game.LESSONS` / `Game.lessonOffers(record)` / `Game.chooseLesson(id)` /
`Game.activeLesson()`、状態は `st.lessonId`、永続は `Storage.LESSON_KEY`（ラン状態の外）。
教訓を足すときは `LESSONS` に `test` と `favor`（tplIdの配列）を書くだけでよい。

**残っていること**:
- **simは教訓を選ばない**ため、教訓を持ったランのバランスは未測定。応募の偏りだけなので
  大きくは動かないはずだが確認していない。次に触る人はここから
- 教訓の選択は Storage にのみ保存し、ラン状態に残していない。敗北画面で選んだ直後に
  リロードすると選択済み表示だけ消える（保存された教訓自体は残る）
- **未検証**: 実プレイで「次はこの方向で行こう」と思わせられているか
- ロードマップ⑦はこれで**完了**。残る未着手は⑧（愛着）

### 推奨する次の順番（2026-09-03 ロードマップ監査後）

1. ~~戦闘結果へ施設貢献・死者CHAIN要約を追加~~ **完了**（4節「戦果に施設と死者の働きを残す」）
2. **実プレイ数ラン**（コード変更なし）。見るのは「施設選択へ何回到達したか」「条件未達の施設を
   採用目標として選んだか」「追加採用を毎戦4Gで使ったか」「戦果の施設行で共通補正とJokerの
   どちらを体感したか」。`copy(KPI.export())` → `node tools/kpi-report.js`
   完了条件: 5ラン分のKPIと所感がHANDOFFに書かれている
3. ~~試遊結果で分岐~~ **A（施設到達率）を実施済み**（上の「拠点接収を入れた」節）。当初の分岐案は以下:
   - 施設到達が5ラン中2回未満 → 建設なしでも一度だけ施設を選べる入口（例: 侵攻の拠点接収）
   - 到達は十分だが「維持」に理由がない → Lv.2/3で同施設を深化、建て替えに代償
   完了条件: `node tools/sim.js 50` でクリア率5〜50%帯、施設到達率の行（追加済み）で到達が動いていること
4. ~~共通HP・防御補正の撤去判断~~ **完了**（上の「共通HP・防御補正を撤去し」節）。当初の保留理由は以下（`--no-facility-bonus` は削除済み、
   施設到達率の行も追加）。**2026-09-03 実測（各200ラン）**:

   | 戦略 | 補正あり | 補正なし | Lv3到達 |
   |---|---|---|---|
   | 三部門均衡 | 33.0% | 24.5% | 19% → 8.5% |
   | 未払い搾取 | 37.5% | 27.5% | 24% → 16% |
   | 施設に届かない12戦略 | ±5pt以内の揺れ | 同左 | 0〜1% |

   共通補正は建設ビルドのクリア率を約10pt支えている。**いま撤去すると建設ルートだけが一方的に
   弱くなる**ので、撤去は「墓地単体で10pt相当を返せる深化」か「建設なしルートの代償」と同時に行う。
   なお sim の施設選択は戦略種別で固定（術者→厨房、ゴブリン→帳簿、他→墓地）なので、
   到達する2戦略が墓地一色なのは sim の癖であり、プレイヤーの唯一解ではない
5. 忠誠・給与を「罰」から「ビルド入口」へ（監査文書2節の行き止まり: 低賃金大量採用・精鋭主義・忠誠）。
   `src/data/` の能力設計なのでCodeXの案出しを先に受け、Claudeは接続と測定を担当

**後回しにするコンテンツ追加**: 新種族、敵編成の3隊列目、事件数の積み増し、2D差分。
いずれも「試せる回数」が増えてから。ロードマップ⑦（新種族）は引き続き最後。

### ロードマップ監査（2026-09-03）で確認したこと

- 設計憲法19節の順で見ると、①採用画面の仮説 ②発火条件の整理 ③連鎖基盤 ④代表ビルド縦検証
  ⑤CHAIN/OVERKILL可視化 は完了。⑥「戦闘・建設・食料を同じビルドへ収束」は施設Joker化で
  **部分成立**（選択はできるが到達率がsimで大半0）。⑧愛着のみ未着手（⑦失敗を方向転換の材料に・⑩魔界史のビルド名は2026-09-03に完了）
- 一つのループとして繋がっているのは 略奪→G→追加採用/傭兵、鎮圧→建材→施設→Joker、戦死→建材(供養)→墓地。
  **最も細いのは 給与・忠誠→戦闘** で、未払いは血の気とストライキだけ、忠誠は罰しか返さない。
  事件も状態から起きるが、ビルドを先鋭化する選択がまだ少ない
- 旧ロードマップ「②③実プレイ計測が終わるまで④以降へ進まない」は、施設Joker化を先に入れたことで
  順序が入れ替わった。これは実プレイで施設Lv最大まで届いたオーナー所感を受けた判断であり、
  以後は上の「推奨する次の順番」を正とする（5節「再設計統合」内の7段階リストは完了記録として残す）

未追跡の `img/` と `シナジーイベント.xlsx` はオーナーのファイルなので触らない。

---

## 1. いま何ができているか

遊べる状態。`index.html` を開くだけで動く（ビルド不要・サーバー不要・`file://` 可）。

1ラン = 王国8拠点の攻略。略奪・反乱鎮圧へ寄り道できるため総戦闘数は可変。
負けると軍団は消滅し「魔界史」に記録が残る。

実装済みの主な要素:

- 応募者の面接・採用・解雇・並び替え（並び順＝配置）
- 最大20体の軍団保有と、作戦ごとの戦闘・建設・生活の三部門編成
- 戦闘部門から出撃隊5体を選抜（出撃給与は全額、建設・生活手当は半額、戦闘控えは0G）
- 種族・前職による部門適性と、通常支給・意図的未払い・前払い厚遇の給与方針
- 食料の生産・消費・不足による忠誠変化、建材投入と施設3段階の出撃補正
- 作戦会議（略奪・反乱鎮圧・王国侵攻の3択）と警戒度
- 作戦別の報酬・給与・差引見込の表示
- 求人の出し直し（1回無料、以降 2G→4G→8G）
- 自動戦闘＋イベントタイムライン方式の演出（踏み込み・被弾・カットイン・画面揺れ）
- ラウンド区切り・攻撃者表示・シナジー別の光・最終決戦演出（x1は観戦用、予算45秒）
- 効果音21種（うち衝撃WAV12種＋モルモ発話音）、音量・ミュート設定
- 通常進行と戦闘の独立BGM（編成・昇進・未払い・忠誠・警戒度の演奏レイヤーは共通）
- 戦果パネル（貢献度の棒グラフ、殊勲/盾役/戦死バッジ、戦闘後の一言）
- シナジー8種（頭数・昇進・死亡と経済の横断条件で発動）
- 戦功による三段階昇進（小隊長→魔将→将軍）と将軍の号令
- 戦闘中ハプニング（未払いストライキ・サボり・低忠誠の仲間割れ）
- 履歴書（前職・志望動機・短所）と証明写真風の立ち絵
- ハプニング14種（戦闘・給与・部門・食料・施設・職歴から発生）
- 給与と忠誠、未払いの累積ペナルティ、離脱
- 再起（1ランに1度だけ一戦手前へ巻き戻し）
- 魔界史（LocalStorage永続）
- 宰相モルモの全画面報告（タイプ表示・発話音・ボタン／キーによる明示送り）

立ち絵は全10種＋キングスライムが揃っている（CodeX作成）。
追加するときは `assets/monsters/{id}.png` を置き、`src/data/portraits.js` の一覧にも足すこと。

---

## 2. 壊してはいけない設計の約束

ここを崩すと後で高くつく。理由つきで書く。

### 2-1. 戦闘は「計算」と「見せ方」を分離してある

`Battle.simulate()` はイベントタイムラインを返すだけで、描画を一切知らない。
`BattleScene`（`src/ui/battle_scene.js`）がそれを再生する。

```
Battle.simulate() → timeline[] → BattleScene.play(timeline)
（ルール・バランス）  （契約）     （DOM/CSS・差し替え可能）
```

**タイムラインが唯一の真実**。ログも、戦果集計も、全部ここから導出している。
新しい情報が欲しくなったら、戦闘中に状態を持ち回るのではなく
「タイムラインから後で導出できないか」をまず考えること。

`emphasis`（0〜3）は「どれくらい重要か」だけを伝え、**尺（何ms見せるか）は描画側が決める**。
この分担を守っている限り、Canvas版やネイティブ版へ戦闘シーンだけ差し替えられる。

`death.permanent` / `result.reversal` / `synergy.firstDiscovery` も同じ性格の**重要度の印**である。
描画側の圧縮判定（`BattleScene.isProtected` / `magnitude`）だけが読み、**戦闘計算には一切使わない**。
`permanent` と `reversal` は勝敗確定後にタイムラインから導出して付け、`firstDiscovery` は
`Game.recordDiscoveredSynergies()` が登録時に付ける。増やすときも同じ作法（計算に混ぜない）を守ること。
`result.wipe`（`"player"` / `"enemy"` / `null`）も同じ印。**判定決着と全滅を見分ける**ためだけにあり、
勝敗そのものは `victory` が持つ。描画側（モルモの一言）しか読まない。

戦果の集計もすべてタイムラインからの導出で完結している。`chainSummary.deepest`（代表CHAIN経路）と
`contribution` の非ダメージ項目（`traitTriggers` / `resources` / `revivesGiven` / `selfRevives` / `healed`）は
`sourceId` と `parentEventId` だけを根拠にする。**誰の手柄か言えないものは個人へ帰属させない**
（施設由来の発火、召喚ユニットの行動）。これらは表示専用で、戦功・昇進・報酬には接続しない。
施設の働きは個人へ付けない代わりに `facilitySummary` / `deathChains`（同じくタイムライン導出）として
戦果へ残す。**新しい施設を足したら `facility_trigger` に `facilityId / name` を付ける**こと。
付いていないと戦果の施設行に出ない。

**契約追加（2026-09-05・Claude / 作業表 B）**: `st.lastBattle.momentumPeak`（数値・戦意の到達倍率）。
`run.js` がタイムラインの `momentum` イベントの `mult` の最大値を取るだけの導出で、戦闘式・数値は変えていない。
戦意は戦闘中の帯にしか出ず、終わると消えていたので戦果に残らなかった。**古いセーブには無い**ので、
表示側は `1` として扱い、`1` のときは1行サマリから戦意を落とす（`UI.breakthroughPanel`）。

### 2-2. アニメーションは `transform` と `opacity` だけ

将来 Capacitor でスマホアプリ化する前提。`left/top/width/height` を毎フレーム変えると
レイアウトが走り、実機で確実にカクつく。HPバーも `width` ではなく
`transform: scaleX()`（`transform-origin: left`）で伸縮させている。

### 2-3. `core/` と `data/` にブラウザAPIを持ち込まない

現在この2層はDOM非依存で、`node tools/sim.js` がブラウザ無しで全ゲームロジックを走らせられる。
`storage.js`（31行）だけが LocalStorage に触れており、ネイティブ保存への差し替え口になっている。
**ゲームロジックからDOMを読まない**（DOMは書き込み専用の出力先）。

### 2-4. コンテンツはデータ追記だけで増える

| 増やしたいもの | 触るファイル |
|---|---|
| モンスター | `src/data/monsters.js` |
| 特性 | `src/data/traits.js`（5種のフック関数） |
| シナジー | `src/data/synergies.js`（`check`/`apply`） |
| 敵 | `src/data/enemies.js` |
| 作戦 | `src/data/missions.js` |
| 昇進階級 | `src/data/promotions.js` |
| ハプニング | `src/data/events.js` |
| 実績 | `src/data/achievements.js` |
| 魔王 | `src/data/demon_kings.js` |
| 立ち絵 | `assets/monsters/{id}.png` ＋ `src/data/portraits.js` |

ロジック側を触らずに済む形を保つこと。

### 食事強化の伝票 `mealPlan`（2026-09-06・V2a で追加した契約）

`Game.mealPlan(rations)` が **食事強化の起点・対象・効果量を決める唯一の場所**である。

- `Game.preparedRoster(rations, plan?)`（本番の倍率）、編成画面の予告、
  `Battle.simulate` へ渡す `rations.meal`、`st.lastBattle.mealPlan` は**すべてこの戻り値を読む**。
  **どこかで倍率を再計算しないこと。** 表示だけが古くなる事故はここから始まる。
- 主なフィールド: `cookUid/cookName`（起点）、`targetUid/targetName/targetAppetite`（対象）、
  `tiedUids`（食欲が同値で並んだ者）、`boost/boostPercent`（効果量）、`kitchenMult`、
  `bigEaters[]`、`hungerUid`、`feast`、`targetEatsNothing`。
- **対象の決定規則: 食欲が最大の1体。同値なら出撃順（`activeUids`）の先頭。**
  `sort` が安定なので並び順だけで決まる。この規則を変えると予告と本番がずれる。
- 効果量が0のときは `targetUid` を `null` にする（0%の強化を誰かに帰属させない）。
- `targetEatsNothing` は「食欲0の者に料理が乗っている」現行挙動の**印**であって修正ではない。
  可否の判断は V2b。ここを直すと数値が動く。
- `rations.meal` は battle.js がまだ読まない追加フィールドである。
  **説明用のフィールド追加で発火順・回数・chainDepth を変えないこと**（`tools/test-food-attribution.js` が検証）。
- 古い戦果には `mealPlan` が無い。表示側は**その表示だけを省略**する。

### 食事強化の因果イベント（2026-09-06・V2b で追加した契約）

**新しいイベント種別は増やしていない。既存イベントへの情報追加だけである。**
伝票（`rations.meal`）が無い戦闘・古いセーブでは何も足さないので、表示側はその表示だけを省く。

1. **`trait_trigger`（`traitId: "demon_cook"`）＝ 起点・対象・効果量**
   追加フィールド: `targetId` / `targetName`（強化された者。効果量0なら `null`）、
   `amount`（0.24 など）/ `amountPercent`（24）、`consumed`、`kitchenMult`、
   `tiedIds`（食欲が同値で並んだ者の戦闘ID。「なぜこの人が受けたか」の説明用）、
   `targetEatsNothing`。`text` も対象と％を含む文言になった。
2. **強化された者の「最初の有効打」＝ 既存の `attack` / `splash` に `mealBoost` が付く**
   `{ first: true, sourceId, sourceName, targetId, targetName, amount, amountPercent }`。
   **1戦闘に1件だけ**。付く条件は **敵へのダメージ** かつ **仲間割れ（incident）でない**こと。
   仲間割れの同士討ちは `unit.atk * 0.7` の生ダメージで `mods.dmgMult` を通らない＝
   食事強化が反映されていないので、印を付けない。**対象外の回では印の権利も消費しない**
   （仲間割れの後に出た本当の初撃へちゃんと付く）。
   一度も有効打が無ければどこにも付かない（嘘の着地を作らない）。
3. **`result.mealSummary`** … タイムラインから導出するだけの要約。
   `{ sourceId, targetId, targetName, amount, amountPercent, consumed, kitchenMult,
   tiedIds, targetEatsNothing, triggerEventId, firstHit: { eventId, type, toId, dmg, dead } | null }`。
   伝票が無い戦闘では `null`。

**壊してはいけない約束**: ここで足したのは説明用フィールドだけで、
**ダメージ・発火順・回数・chainDepth は1つも変えていない**
（`tools/test-meal-attribution-battle.js` が指紋比較で検証。`BATTLE_BEFORE=<旧battle.js>` を
渡すと変更前の実装と直接つき合わせる）。今後ここへ手を入れるときも同じ検証を通すこと。
**食欲0の者へ強化が乗る現行挙動は維持**しており、`targetEatsNothing` は印にすぎない。

イベントチェーンの最小契約は `state.laborDispute`（通常 `null`）。現在は
`{ stage: "march", actorUid, startedTurn }` だけを持ち、給与抗議を無視したときに作成、
ストライキ行進の解決時に必ず `null` へ戻す。汎用フラグ基盤へ抽象化しない。

敵段階は従来の `units` を基本隊列として保ち、任意の `variants[]` に
`{ id, name, hint, units }` を足せる。作戦契約には `formationId / formationName /
formationHint` が加わり、UIは未設定の旧セーブを「基本隊列」として表示する。
`formationId` は同じ作戦会議中の再生成でも保持し、戻る操作を敵の引き直しにしない。

敵ユニットの任意契約 `introQuote` は `Battle.makeUnit()` と開始スナップショットを通り、
`dialogue { unitId, name, side, quote }` として戦闘タイムラインへ出る。DOM側へ敵名を直書きせず、
台詞追加は `src/data/enemies.js` だけで行う。現在は最終戦の勇者アレンに1行だけ設定済み。

### 2-5. 2026-09-02 に増えた契約（データを触る前に読む）

**シナジーの効果量を説明文へ二重に書かない。**
編成画面の「いま ×1.15／あと1体で ×1.30」は `Synergy.preview()` が
**使い捨ての写しへ実際に `apply()` して測った値**である。`desc` は文章としてだけ使う。
したがって `synergies.js` へ新しいシナジーを足すと、**表示は自動で追従する**（追加作業は不要）。
逆に `desc` に数値を書いても表示はそちらを見ない。`apply()` が本体。

同じく `Synergy.traitEffects()` が特性の `modDealt` を中立な状況
（敵HP満タン・ラウンド2・乱数0.5）で呼び、編成で決まる倍率だけを拾う。
**新しい特性も `modDealt` を持つなら自動で編成画面に出る。**
状況で決まるもの（敵HPを見る、ラウンドを見る、乱数を引く）は自然に外れる。

**merge型シナジーは「実際に合体した戦闘」でだけイベントに載る。**
`battle.js` は `type === "merge"` を synergy イベントと `activeSynergies` の両方から除外し、
合体を実行した `run.js` が `addMergeSynergy()` で差し込む。条件を満たしただけで
「合体する！」と宣言しないための分担。**新しい merge型を足すなら `run.js` 側の合流処理が要る。**
キングスライム合体は既定で行うが、編成画面で断れる（`st.kingSlimeMerge`、既定 true）。

**傭兵は軍団員ではない。** `flags.mercenary` を持つ戦闘ユニットで、`contribution` の行に
`mercenary: true` が付く。欠員・戦没者名簿・戦功・昇進・給与・魔界史のいずれにも入らない。
`st.mercenaries`（雇用中）と `st.mercenaryOffers`（候補）は戦闘後に空へ戻る。

**ラン状態へ増えたフィールド**（`migrateState()` の defaults にも入れてある）:
`maxChain` / `maxOverkill` / `mercenaryOffers` / `mercenaries` / `kingSlimeMerge` /
`extraHiresThisPhase` / `activeFacilityId` / `pendingFacilityChoiceLevel`。
**新しい状態を足したら defaults にも足すこと**（旧セーブが壊れる）。

**無料採用枠が0になっても `hire()` は面接を自動終了しない。** 以後は紹介料4Gから倍増し、
`extraHiresThisPhase` が料金段階を持つ。終了は `skipHire()` の明示操作だけ。戦後の
`nextRecruit()` で無料枠と追加料金段階をリセットする。simの基準戦略は従来比較を守るため無料枠だけ使う。
KPIは `paidHires / paidHireGold` をラン状態の外へ記録する。simには狙いのゴブリンが候補にいて、
紹介料を払っても6G残る場合だけ追加採用する比較戦略を持つ。
200ラン比較ではゴブリン統一33.0%、追加採用33.5%、求人引き直し22.0%。追加採用は287人・1196G、
最大CHAIN平均は通常3.9→追加4.6。勝率の自動正解にはならず、連鎖完成へ寄与しているため、現時点で
紹介料や採用後の無料引き直しリセットは弱体化しない。実プレイで「毎戦とりあえず4G」になるかを見る。

**新しい「編成の判断」を足したら KPI の指紋にも足す。**
`KPI.fingerprint()` が「ビルド試行」を数える根拠で、現在は出撃隊・部門配属・施設Lv・給与方針・
雇った傭兵・合体の可否・作戦種別を見ている。足し忘れると、その判断は試行として数えられず、
KPIが実際より少なく出る。

---

### 遅刻（2026-09-09・Claude）── 特性は数値ではなく出来事の順番に効かせる

- 特性フック **`lateArrival(ctx)`**（`ctx.unit`, `ctx.rng`）を追加。返した整数ラウンド数だけ、その味方は開戦時に不在。
  敵側には適用しない（採用していない者に癖は無い）。
- 不在中は `flags.absent = true`。**`battle_start.player` に入らず、口上を言わず、狙われず、行動順にも入らない。**
  戦場判定は battle.js の `onField(u)`（= alive かつ !absent）に一本化してある。新しい「alive で選ぶ」処理を書くときはこれを使うこと。
- 到着はラウンド頭で **`summon` イベント**（`late: true`, `sourceUnitId: null`, `unit.summoned: false`）。
  描画側は `addSummon` が `late` を見て召喚札を付けない。**`flags.summoned` は立てない**ので戦功・欠員・戦没者に数える。
- Snap に **`late`**（boolean）を追加。`contribution` に **`late`**（遅刻ラウンド数、0=開戦から居た）を追加。どちらも表示用の追加フィールドで、計算には使わない。
- 味方が全員倒れたあとに遅刻者が一人で着くことがある。これは仕様。`wiped(playerUnits)` は不在者を alive と数えるので敗北にならない。
- 既知の穴：`summarizeFacility` の「全滅回避」判定は `battle_start.player` から生存表を作るため、遅刻者を数えない。表示専用なので放置。
- データ側：`traits.drunkard`（酒好き、1〜2ラウンド）を末尾追記。オークの `traitPool` に入れた（fixed ではない。出るのはたまに）。
- 見せ方（2026-09-10 試遊で「召喚が付いてきた」「遅刻が明示されない」を受けて）：
  `battle_start.absent`（Snap[]）を追加し、描画側は不在者を薄い枠＋「遅刻中」で最初から置く。
  開戦時に `dialogue`（`late: true`, name "モルモ"）で不在を言う。到着の `summon` は `quote` を持ち、
  描画側は枠へ入れて「到着！」を浮かせる（「召喚！」は出さない）。
- 台詞は `traits.drunkard.lines.{absent, absentSelf, arrive}` のプール。`{name}` が置換される。人物ごとの脚本にはしない。
  開幕は二拍（モルモ→本人が戦場の外から `offstage: true`）。
- 一言の契約：`dialogue` に `late` / `offstage`、`summon` に `quote`、`trait_trigger` に `quote` を追加（表示用）。
  `MormoScene.aside(options)` に `speaker: { name, src }` を追加（省略時はモルモ）。`BattleScene.speakAside()` が
  一言で止める共通経路で、既に止まっていれば false を返して字幕へ落とす（二重に止めて詰まらせない）。
  `asideUsed` で「大食漢の一口」は1戦闘1回だけ止める。
- **落とし穴**：`BattleScene.skip()` は `render()` を通らない独自の早送り経路。到着処理は `clearAbsent()` に共通化して
  両経路から呼ぶこと。最初の実装は render 側だけで、「飛ばしたときだけ遅刻中の枠が残る」バグになった（テストが検出）。
- 検証：`node tools/test-late-arrival.js` ／ `tools/browser-tests/late-arrival.js`。

### 撤退の提案（2026-09-10・Claude）── オートバトルに一度だけ入るプレイヤーの判断

仕様は `docs/SPEC_RETREAT_2026-09-10.md`。battle.js 側の契約はこれだけ。

- **`retreat_offer` イベント**（1戦闘1回、`emphasis: 3`, `cls: "mormo"`）。
  `{ round, downed: Snap[], standing: Snap[], enemies: Snap[], text }`。
  出す条件はラウンドの終わり（`resolveRecoveryHooks(false)` と暴食の宴のあと、勝敗判定の前）に
  **軍団員（`flags.summoned` でない味方）が倒れたまま／敵が全滅していない／立っている軍団員が1人以上**。
  「立っている」は `alive` ではなく **`onField`**（遅刻で不在の者は数えない）。
  蘇生でラウンド終了時に立っていれば、そのラウンドでは出ない。
- **`result.retreatOffer`** = `{ index, round, contribution }`、無ければ `null`。
  `index` は timeline 内の位置。`contribution` は**既存の `summarizeContribution()` をそのまま提案時点で呼んだもの**で、
  軍団員の行だけ `survived: true` に上書きし、倒れていた者へ `injured: true` を足す（担いで帰る＝戦死しない）。
  傭兵・召喚物は今までどおり。**二か所で別々に組まないこと。**
- **`options.noRetreatOffer`**（真なら提案を出さない）。開幕の防衛戦（未決U1の既定＝提案しない）で run.js が渡す。
- `retreat_offer` は `permanent` / `reversal` と同じ**重要度の印**である。
  `simulate()` は提案を出しても止まらず最後まで計算する（＝続けた場合の結末）。
  **`U.rand` / `U.chance` / `U.pick` を新たに呼ばない**こと。呼ぶと sim の数字が全部ずれる。
  回帰は `tools/test-retreat-battle.js` の9番（提案あり／なしで `attack` 列と `contribution` が完全一致）。

## 3. 落とし穴（私が実際に踏んだもの）

同じ轍を踏まないように。全部実際に起きた。

### 3-1. `tools/sim.js` はフェーズを増やすたびに更新が要る

バランス検証用のヘッドレスシミュレータ。`phase` を新しく増やすと、
sim側がそのフェーズを処理できず**無限ループして空回りする**（guard で抜けて記録なしになる）。
`defeat` と `event` を追加したとき2回踏んだ。数値が急におかしくなったらまずここを疑う。

読み込むファイル一覧も sim.js の先頭にベタ書きなので、`src/data/` にファイルを足したら
**sim.js の `files` 配列にも足すこと**（`events.js` で踏んだ）。

### 3-2. 状態オブジェクトの同一性

`Game.retry()` が `this.state` を**別インスタンスに差し替えて**いたため、
`const st = Game.state` で参照を掴んでいた呼び出し側が古い状態を見続けた。
今は「中身だけ入れ替える」実装にしてある。同じことをしないこと。

### 3-3. 配列の並び順に暗黙依存しない

戦果パネルの「最多与ダメ」を `contribution.find(c => c.dealt > 0)` で取っていた。
配列が与ダメ降順にソート済みという前提に依存していて、実プレイでは偶然動いていたが、
合成データで検証したら**被弾最多のはずが殊勲の台詞**になった。
今は `reduce` で明示的に最大値を求めている。

### 3-4. 検証が常に成立するテストを書かない

「敗北後にセーブが消えているか」を**履歴画面**で検査していた。
履歴画面には元々「続きから」ボタンが無いので、この検査は**絶対に失敗しない**空の検査だった。
そのせいで「敗北してもセーブが残り続ける」実バグを長期間見逃した。
テストを書いたら「わざと壊したら落ちるか」を一度確認すること。

### 3-4b. テストは「UIの形」ではなく「守りたい性質」を測る

UIを作り替えたとき、ボタン名を名指ししていたテストが**静かに無意味になった**実例が2つ。

- `tier0` は `[data-action="down"]` と `[data-action="fire"]` の距離を測っていた。
  出撃隊／控えの分離で同じカードに両方が並ばなくなり、要素が見つからず落ちた。
  → 今は「`.danger` のボタンは他のボタンから20px以上離れている」という
  **性質そのもの**を測る。並び替え同士が近いのは押し間違えても取り返せるので対象外。
- `portrait` は「オーガは一覧に無いから絵文字」を対照群にしていた。
  オーガの絵が増えた瞬間にこの前提が壊れた。
  → 今は `PORTRAITS` に載っていないことが保証される架空のidを実行時に作る。

**実在のコンテンツを対照群に使わない。** 増えた瞬間にテストが嘘になる。

もう1点。`ok()` が失敗しても `process.exit(errs.length?1:0)` で握り潰していたテストがあり、
`run-all.sh` が `tail -1` しか出さないため画面上は `✓ JSエラーなし` に見えていた。
今は `ok()` が `process.exitCode=1` を立て、終了コードにも反映される。
**テストを足すときは `ok()` を使い、独自に `process.exit` する場合は
`process.exitCode` を必ず巻き込むこと。**

### 3-5. 立ち絵は一覧方式（404を出さないため）

最初は「PNGがあれば自動で使う」方式にしたが、未収録の7種族に対して
**毎回404が飛び**、コンソールが汚れる上にテストの実エラー検知が鈍った。
今は `src/data/portraits.js` の一覧に載っている種族だけ画像を要求する。
**画像を足したら一覧にも足すこと**（載せ忘れても絵文字のままで壊れはしない）。

### 3-6. 台詞に別言語が混ざる

`voices` を大量に書いたとき、ロシア語の「это」と英語の「next」が紛れ込んでいた。
データを大量追加したら機械的にチェックすること:

```bash
node -e "
new Function(require('fs').readFileSync('src/data/monsters.js','utf8')+';globalThis.T=MONSTER_TEMPLATES')();
for (const t of T) for (const [k,arr] of Object.entries(t.voices))
  for (const l of arr) if (/[a-zA-Zа-яА-Я]/.test(l)) console.log(t.id,k,l);
"
```

### 3-7. 「音を生成したか」だけを測るテストは、実機の無音を見逃す

BGM「魔王軍の行進曲」の初回実装は、headless テスト（オシレーターが`start()`されたか）と
headless ブラウザテスト（`Music.timer`が立っているか）は全部通ったが、
**オーナーの実機（Androidスマホ）では無音**だった。効果音は鳴っていたので
Pagesの反映漏れやキャッシュではなく、BGMのコード側の問題だった。

原因は2つ重なっていた。ノイズ系の音（スネア・ハイハット）の減衰式に、末尾で
もう一段（`*.12`）掛かっていて効果音の実質1/10未満まで沈んでいたこと。
そして常時鳴る足音がほぼ55Hz付近の純音のみで、スマホの小型スピーカーは
この帯域をほとんど再生できないため、理論上鳴っていても耳に届かないこと。

**「音を生成したか」だけを測るテストはこの種のバグを検出できない。**
`tools/test-music.js` に「ノイズ系の実効音量が効果音と比べて懸け離れて
小さくないか」を数値で測るテストを足した。詳細は `docs/BGM_DESIGN.md` 3-1節。
音量まわりを触ったら、まずこのテストを壊せるか確認すること。

---

## 4. 直近で完了したこと

### 戦果に施設と死者の働きを残す（2026-09-03完了）

オーナーの実プレイ「2倍速だと同じゾンビが二度立ち上がったように見え、誰の手柄か分からない」から。
`Battle.summarizeFacility()`（施設ごとの発火回数・召喚数・全滅回避）と `Battle.summarizeDeathChains()`
（倒れた味方ごとの `致死を耐えた → 戦死 → 死霊術で蘇生（全快） → 骸骨従者を召喚` と永久戦死）を追加。
どちらもタイムラインだけから導出し、2-1の約束どおり戦闘計算・戦功・報酬へは接続しない。
`lastBattle` に `facility`（Lv共通補正と稼働施設IDを分けて保持）・`facilitySummary`・`deathChains` が増えた。
UIは勝利・敗北画面の「今回の大暴れ」直下に「🪦 施設と死者の働き」を出し、要約の無い旧戦果では出さない。
数値バランスは不変。検証は `node tools/test-facility-report.js` とブラウザ `report`。

### 施設を選択式の大型Jokerへ移行（2026-09-03完了）

`facilityLevel/buildProgress` は施工規模・実績・魔界史との互換のため残し、同時に1つだけ稼働する
`activeFacilityId` を分離した。Lv.1/2/3到達ごとに恐喝帳簿・巨大厨房・墓地から維持または建て替えを
選ぶ。恐喝帳簿は会計職＋予約金貨3G、墓地は建設部門の死霊術師＋戦死、巨大厨房は戦闘糧食を
追加1消費して大食漢・魔界料理人の食事強化を2倍化する。KPI指紋にも施設IDを追加済み。
旧セーブは既存Lvを維持し、次の戦果確認後に施設選択へ入る。一律HP/防御補正は比較のため当面維持し、
選択制の価値を試遊してから撤去判断する。
施設選択カードには現在の軍団を照合し、会計職・大食漢／魔界料理人・建設部門の死霊術師が
「発火可能」か「不足」かを表示する。条件に合う答え合わせだけでなく、未接続施設を先に選んで
次の採用目標にするための情報である。
`FACILITIES.links.emits/reacts` も特性と同じ表示語彙として扱い、稼働中施設を
`Synergy.connections()` の軍団側要素へ含める。施設効果の計算には使わず、履歴書の接続表示専用。

### 面接の自動終了を廃止し、追加紹介を選択化（2026-09-03完了）

無料採用枠を使い切った瞬間に作戦会議へ送る挙動を廃止。次の応募者群を見たまま、紹介料
4G→8G→16Gで追加採用するか、明示的に面接を終了するか選べる。希望給与は戦後の維持費、紹介料は
即時費用としてボタン上で分けて表示する。人事適性の割引は同時導入せず、候補人数増加だけを維持。

### 3作戦の目的と施設施工見込を明示（2026-09-03完了）

オーナー実プレイで「反乱鎮圧が建設向けなのか分からない」「軍団を整える寄り道が見えない」と判明。
数値は変えず、作戦カードで略奪＝資金・食料補給、鎮圧＝忠誠回復・建材確保、侵攻＝決戦進行と明示した。
施設施工は鎮圧専用ではなく、建設担当と備蓄建材があれば全作戦の勝利後に起きる。各カードには現在の
建材・施工能力から「勝利後の最大投入見込」を表示し、施工役がいなければ建材は備蓄されることも示す。
侵攻には最終決戦までの残り勝数を表示する。戦闘・経済バランスは不変。

### 開幕3日間プロトタイプを撤廃＋最前列への一発移動（2026-09-03完了）

オーナー実プレイの所感により、初回だけ存在した「勇者到着まで3日」の準備日を撤廃した。
新規ランは採用終了後、そのまま通常の作戦3択へ入る。3日間途中の旧セーブも、人材・資源・配置を
維持したまま作戦会議へ移行する。`openingPrototype` 以下の旧フィールドと入口はセーブ互換のため
当面残すが、新規・移行後のどちらからも到達しない。`tools/test-daily.js` とブラウザ `daily.js` は
「3日間が始まらない」ことを守る回帰テストへ反転した。

編成画面には各出撃者の「⏫ 最前列へ」を追加。`activeUids` の対象を1操作で先頭へ移し、通常の
「前へ／後ろへ」と同じくKPIの編成変更へ記録する。すでに最前列なら何も変えない。

### キングスライム合体を断れるようにした（2026-09-02完了）

バックログの「スライム5体が罠」を確かめに行った結果。**罠ではなかった**が、
合体が自動・不可逆で、プレイヤーに何も聞かないのは判断の取り上げだったので選択に変えた。

- 既定は従来どおり合体する。編成画面で断れる（`Game.setKingSlimeMerge(false)`）。
  「3体のまま」と「合体して1体」のHP・攻撃・給与を並べて出す（`Game.kingSlimePreview()`）。
- **既定を「合体しない」にして測ったらスライム統一のクリア率が 48% → 29% へ落ちた**ので、
  既定は合体のままにした。合体は罠ではなく、素直に強い。黙って弱くしないための判断。
- 旧セーブは「合体する」で移行する。

**ついでに直した不具合**: merge型シナジーは条件を満たすだけで
「シナジー発動【キングスライム】 スライム3体が1体に合体する！」のカットインと
`activeSynergies` へ載っていた。合体を選択制にした時点で「合体していないのに宣言する」
状態になったため、battle.js 側で merge型を除外し、実際に合体した戦闘でだけ
run.js が差し込む形へ統一した。

検証: `node tools/test-king-slime-choice.js`（既定・断る・選び直す・対象がいなければ設定しない・
旧セーブ移行・合体していないのにカットインを出さないこと）、
`tools/browser-tests/kingslime.js`（新規・クリック非依存）、`tools/king_slime_smoke.js` を選択制へ追従。
全Nodeテスト32本・ブラウザ26本すべて通過。`node tools/sim.js 100` でスライム統一 56%。

### 種族統一の成績（2026-09-02実測・CodeXへの申し送り）

**オーナー判断（2026-09-03）:** この表を「全種族へ統一シナジーを配る根拠」として使わない。
同一種族5体はフォーカードのような分かりやすく強い入口であって、シナジー設計の中心ではない。
中心にするのは、種族・特性・職業・忠誠・給与・死亡・蘇生・資源・施設など、異なる条件を
横断して初めて成立する組み合わせである。たとえば死霊術とアンデッド、死亡と略奪、暴食と
食料0のように、単一タグの頭数ではなく「起点を作る役／反応する役／別の状態へ変換する役」が
そろうことで連鎖が伸びる形を優先する。統一編成が弱いという測定値だけを理由に、種族ごとの
人数倍率や専用セットボーナスを一括追加しない。新案は、別軸を最低2つ以上つなぎ、完成途中の
各パーツにも単独または別ビルドでの用途があるかを確認してから実装する。

**根本改修の回収（2026-09-03）:** 設計憲法第19節の最優先「採用画面で、今の軍団に入れたら
何が起きるか考えられる」を実装。特性の任意メタデータ `links.emits / links.reacts` は、戦闘計算や
効果量ではなく、公開済みの発火条件を共通語彙で結ぶ表示契約である。`Synergy.connections()` が
応募者と現在の軍団を双方向に照合し、履歴書へ `追い剥ぎ → 金貨獲得 → 強欲` のような経路を
最大3本表示する。直接接続がない場合は捏造せず「まだない」と表示する。新特性で既存能力との
接続を採用画面に出したい場合だけ `links` を追記する。数値や戦闘挙動は変わらない。

種族ごとに5体統一で段階別の勝率を測った（各200戦・基礎値のみ、昇進と施設は無し）。

| 種族 | S5 | S6 | S7 | 発動シナジー |
|---|---|---|---|---|
| インプ | 100% | 100% | **100%** | 魔法結社 |
| 死霊術師 | 100% | 99% | 67% | 魔法結社 |
| オーガ | 100% | 100% | 46% | なし |
| ゴブリン | 100% | 99% | 5% | ゴブリン軍団 |
| 魔法使い | 100% | 100% | 7% | 魔法結社 |
| スライム | 98% | 5% | 0% | キングスライム・低賃金 |
| ゾンビ | 38% | 0% | 0% | なし |
| コボルト | 11% | 0% | 0% | なし |
| オーク | 100% | 7% | 0% | なし |
| 骸骨兵 | 99% | 0% | 0% | なし |

**10種族のうち4種族（コボルト・オーク・ゾンビ・骸骨兵）は、統一しても何も起きない。**
「同じ種族を集める」という最も自然な戦略が、半分近い種族で無報酬になっている。
骸骨兵は既知（死の軍勢は死霊術師が必須）。ここは `src/data/synergies.js` の担当なので
**CodeXへの申し送り**とする。足すなら「頭数で伸びる」形（既存のゴブリン軍団・魔法結社と同じ）ではなく、
**その種族にしかできない別の反応**（例：オークは被弾するほど強い、ゾンビは死んでも減らない）にすると
接続数が増える。倍率を増やす形は避ける。

### 傭兵市場＝稼いだ金貨の出口（2026-09-02完了）

ロードマップ⑥「施設の大型Joker化」に相当する枠だが、**施設ではなく出撃枠を開ける形**にした。
理由は測定にある。略奪ビルドは金を稼ぐのに使い道が無く、ビルドを完成させるほど負けていた
（第5戦時点のゴブリン5体のクリア率12%＝最低）。施設で解禁条件を作ると、
建材を投じないゴブリン経済型が使えず、直したい相手に届かない。

状態契約:

- `st.mercenaryOffers`（候補・作戦ごとに固定）と `st.mercenaries`（雇用中）。
  `Game.mercenaryCost()` は `MERCENARY_COSTS = [10, 20]` を順に返し、上限で `Infinity`。
- `deploy()` で出撃隊の**外から**合流し、`flags.mercenary` が立つ。戦闘後に両方とも空へ戻す。
- 傭兵は軍団員ではない: `contribution` に `mercenary: true` が付き、
  `processCasualties` は欠員・戦没者から除外、`awardMerit` はロスター照合で自然に無視。
  給与・昇進・魔界史にも入らない。旧セーブは空配列で移行。

測定（各300ラン・ゴブリンを狙うプレイ）:

| 方針 | クリア率 | 傭兵/ラン | 5体ビルドのクリア率 |
|---|---|---|---|
| 引き直し全力・傭兵なし（従来） | 30% | 0 | 14% |
| 引き直しは無料分だけ・傭兵なし | 38% | 0 | 26% |
| 引き直しは無料分だけ・同族傭兵へ | **62%** | 2.6 | **61%** |

**「引き直しに使うか、傭兵に使うか」という判断が生まれ、ビルドを完成させたランが
最低から最高へ反転した**（14% → 61%）。これが狙いだった。

**追記（2026-09-02・差別化レバーを3つ測って全部外れた）**:
「稼ぐビルドが報われる」ようにしたくて、次の3つを各300ランで測った。**どれも差を作れなかった。**

| レバー | 略奪ビルド | 稼がないビルド |
|---|---|---|
| 何もしない | 61% | 60% |
| 値段を上げる（15/30・20/40） | 59% → 54% | 55% → 53% |
| 同族割引（1体10%・最大40%） | 61% | 65% |
| 傭兵の取り分（勝利報酬 -4G/-8G） | 59% → 53% | 56% → 53% |

理由は明快で、**傭兵1人の効き目に対してビルド間の収入差（1ラン14.9G vs 4.0G）が小さすぎる**。
値段をいじる限り、両方が同じだけ上下するだけ。差をつけたいなら、価格ではなく
「略奪の収入そのものを大きくする」か「傭兵の質を払った金額に連動させる」しかない。
**同じレバーを再試行しないこと。**

顔なじみ価格（同族割引）は**残した**。クリア率への影響はほぼ0だが、
「能力で選ぶか、安さと種族シナジーの頭数で選ぶか」という小さな判断になり、
「同族はいいことがある」という既に測った事実（混ぜると倍率を二重に失う）とも一致するため。
**インフレを直す手ではない**ことは、はっきり書いておく。

**正直な留保（実プレイで確かめること）**:
稼がないビルド（最強優先・1ラン4.0G）でも 43% → 60% と同じくらい上がる。
傭兵を2.4人雇える略奪ビルドと1.4人の非経済ビルドで差が +20 と +17 しかなく、
**いまのところ「稼ぐビルドが報われる」より「みんな強くなる」に近い**。
値段を 15/30・20/40 へ上げても差は3〜5ポイントのままで、全体が下がるだけだった。
次の一手の候補は**同族割引**（軍団と同じ種族の傭兵は安く雇える＝統一へのコミットが報われる）。
実プレイで「誰でも雇えばいい」になっていたら、そこへ進む。

検証: `node tools/test-mercenary.js`（価格の段、所持金不足、5枠を消費しないこと、
戦闘への合流、契約終了、欠員・戦没・戦功に入らないこと、旧セーブ移行）。
`tools/browser-tests/mercenary.js`（新規・クリック非依存）。
全Nodeテスト31本・ブラウザ25本すべて通過。`node tools/sim.js 50` は傭兵を使わないので従来どおり。

### 編成画面にシナジーの実効倍率と伸ばし方を出す（2026-09-02完了・実プレイ由来）

オーナーの実プレイ「ゴブリン5だと耐久がなくて負けるので3体でやめた」から。測ったら**逆**だった。

| 編成（実データ・各300戦） | S5 | S6 | S7 | 総HP |
|---|---|---|---|---|
| ゴブリン5（純） | 100% | **100%** | 12% | 100 |
| ゴブリン3＋オーガ2 | 100% | **28%** | 0% | 150 |
| ゴブリン2＋オーガ3 | 100% | 47% | 0% | 175 |
| オーガ5（純・耐久型） | 100% | 98% | 29% | 225 |

総HP100の純ゴブリンが総HP150の混成に勝つ。《ゴブリン軍団》が頭数で伸びる（3体+15% / 5体+45%）のと、
《群れの本能》が同族1体につき+10%乗るため、**中途半端に混ぜると倍率を二重に失う**。
つまりプレイヤーは正しく「脆い」と怖がって、より弱い方へ逃げていた。

原因は情報の欠落。編成画面は `desc` の「+15%刻み」しか出しておらず、
いま何倍なのか・あと1体で何倍になるのかが分からなかった。

- `Synergy.preview(units, { slots })` を追加（`src/core/synergy.js`）。
  **効果量は定義に書かせず、使い捨ての写しへ実際に `apply()` して測る。**
  data 層へ説明用の数値を二重に持たせると、効果を変えたとき表示だけ古くなって誰も気づかない。
- 出撃枠が空いていれば「あと1体で ×1.30」、埋まっていれば「オーガをゴブリンに替えると ×1.30」。
  誰を誰に替えるかは総当たり（5×5の適用）で実測する。未発動シナジーの「あと○体」も同じ方式。
- `src/data/synergies.js` は**変更していない**（CodeX の層を触らずに済む形にした）。
  数値バランスも不変で、増えたのは表示だけ。

**追記（2026-09-02）**: 残課題だった「特性の同族ボーナスが見えない」も入れた。
`Synergy.traitEffects(units)` が各特性の `modDealt` を**中立な状況**（敵HP満タン・ラウンド2・乱数0.5）で
実際に呼び、倍率が1を超えたものだけを「いまの並びで効いている特性」として出す。
編成で決まるもの（群れの本能・忠犬・血の気）だけが残り、状況で決まるもの（卑怯者・先制・怪力）は
自然に外れる。これでシナジーと特性の両方が見え、「混ぜると二重に失う」が画面から読める。

**踏んだ落とし穴**: ロスターのモンスターは `alive` を持たない（戦闘ユニットだけが持つ）。
`sandbox()` が `alive` を立てていなかったため、《群れの本能》の「生存中の味方」が常に0体になり、
実機では何も表示されなかった。ところが**テストは作り物に `alive: true` を入れていたので通っていた**
（HANDOFF 3-4「検証が常に成立するテストを書かない」の実例）。
テスト側も実際のロスターと同じ形（`alive` を持たない）へ直してある。

検証: `node tools/test-synergy-preview.js`（実測であること＝定義の apply を差し替えると表示も変わる、
ロスターを書き換えないこと、枠が満杯なら入れ替えを出すこと、伸びないなら何も勧めないこと）。
`tools/browser-tests/synergy.js`（新規・クリック非依存）で実画面の文言を確認。
全Nodeテスト30本とブラウザ24本すべて通過。

**ついでに直したフレーク**: `casualty.js` は味方が強すぎて敵が一度も行動せず、
「捨て駒Aが死なない」戦闘になることがあった（スイート実行で実際に落ちた）。
攻撃力と速度を下げ、敵が必ず行動する状況にした。

### 戦果パネルの言葉を直す（2026-09-02完了・実プレイ由来）

オーナーから「CHAINってなに？」「壊れ方という表現がピンとこない」。両方とも伝わっていない証拠。

- 「💥 今回の壊れ方」→ **「💥 今回の大暴れ」**（オーナー選択）。
  「壊れビルド」は設計文書の語彙で、プレイヤーは設計文書を読まない。
  画面に出す言葉は世界観の口調へ寄せる。**設計語彙をそのままUIへ出さないこと。**
- 「最大CHAIN」の名前は残す（オーナー判断）。代わりに、数字と経路の関係を読ませる一行
  「いちばん長くつながった連鎖（4段）」を経路の上へ置いた。用語の説明画面は作らない。

**あわせて記録しておくべき事実: 最大CHAINは実測で平均2.36段しかない。**
オーナーの見立ては「序盤で2段なのは敵が弱いから。中盤にかけて増えればよく、いきなり
インフレさせなくてよい」。段階別に測った結果（120ラン・1007戦、`tools/sim.js` と同じAI）:

| 攻略段階 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 最大CHAIN平均 | 2.17 | 2.29 | 2.37 | 2.39 | 2.42 | 2.44 | 2.40 | 2.36 |
| 4段以上の割合 | 6% | 13% | 16% | 16% | 12% | 12% | 7% | 4% |
| 事件/戦（発火・資源・蘇生・召喚・事件） | 0.53 | 1.05 | 1.43 | 1.87 | 2.08 | 1.56 | 1.63 | 1.30 |

- **当たっている部分**: 序盤が寂しいのは敵と軍団が小さいからで、事件の数は段階3〜5で
  約4倍（0.53→2.08）に増える。序盤を無理に盛らない判断は妥当。
- **当たっていない部分**: **連鎖の深さは段階が上がってもほぼ横ばい**（2.17→2.44→2.36）で、
  4段以上に至っては中盤16% → 終盤4%へ**減る**。つまり終盤は「事件は増えるが、
  それぞれが独立して起きているだけで繋がっていない」。敵が強くなれば自然に繋がる、
  という形にはなっていない。
- **留保**: この測定は `sim` のAI（強い順に採用）なので、意図的に略奪ビルドや蘇生ビルドを
  組む人間プレイとは違う可能性がある。実プレイのKPI（`buildAttempts` と魔界史の maxChain）で
  確かめてから⑤⑥の設計に入るのが正しい順番。

**方針（オーナー判断）: 数値インフレで連鎖を伸ばさない。** 増やすのは倍率ではなく
「反応の口」（何が何に反応できるか）。現状の深い連鎖は略奪経済と戦死・蘇生の2本しかなく、
どちらも特定の特性が揃って初めて動く。⑤⑥はこの口を増やす作業として扱う。

### OVERKILLの下限を引き上げ、日常を見せ場に戻す（2026-09-02完了）

前項（事件の尺）の測定で見つかった、演出ではなく**ゲーム語彙の側**の問題。

- 旧実装は「少しでも余ればOVERKILL」。実測で **1戦3.94回**、余剰割合の中央値は18%。
  つまり撃破のほぼ全部がOVERKILL判定で、名前が意味を失っていた。
- `Battle.OVERKILL_MIN_PERCENT = 40`（敵最大HP比）を追加し、下限未満の撃破では
  `overkill` イベントを出さないようにした。**1戦0.88回**へ。
  しきい値は40/30/50%を実測して選んだ（30%=1.23回、50%=0.56回）。
- **ランク表（蹂躙100/粉砕300/消滅500/魔王級1000）は下げていない。**
  実測の最大は約320%で上位2つには届かないが、これは「壊れたビルドで初めて届く目標」として
  残す判断。ランクを下げて簡単に到達させると、また名前が安くなる。

副作用として測ったもの:

- 戦闘ごとの最大CHAIN平均 2.49 → 2.36。小さいOVERKILLが連鎖の1段を稼いでいたぶんが消えた。
  CHAINが「本当に反応が起きた回数」に近づいたと見ている。
- 段階別の平均尺は 序盤14.8→13.3秒、終盤46.0→43.4秒。保護占有率 54〜66% → 40〜49%。
  **数字は下がるが悪化ではない**。1戦3回ぶんの「事件のふりをした日常」を分母から外した結果で、
  残っている保護区間は本当の事件だけになっている。
- `node tools/sim.js 50` のクリア率は8〜54%で帯は不変。ダメージ式・勝敗は触っておらず、
  《連鎖虐殺》の発火条件（余剰100%以上）も下限より上なので挙動は変わらない。

検証: `node tools/test-overkill.js` に下限の境界（39%は出ない・40%は出る・基本ランクのまま）を追加。
全Nodeテスト29本とブラウザ23本すべて通過。

### 事件の尺を「読み切れる長さ」へ（2026-09-02完了・実プレイ由来）

オーナーの実プレイ指摘「余剰ダメージや大食漢の演出が一瞬で見逃す」から。測ったら明白だった。

| 事件 | 1戦あたり | 旧尺 | 新尺 |
|---|---|---|---|
| 能力発火（大食漢・魔界料理人など） | 0.13回 | **620ms**（全イベント中最短） | 1150ms |
| 資源獲得（追い剥ぎ +1G） | 0.59回 | 631ms | 917ms |
| OVERKILL | 3.88回 | 1149ms | 1575ms |
| 蘇生 / 召喚 / 耐えた / 魂消費 | — | 1100 / 1150 / 650 / 655ms | 1250 / 1250 / 750 / 754ms |

字幕（`showAction`）の表示時間もイベントの尺に合わせて伸ばした。x1は観戦側に振り、
急ぎたい人は速度x2/x4と「最後まで飛ばす」を使う、という分担にしている（オーナー判断）。

**もう一つの発見: OVERKILLの見せ場が一度も立っていなかった。**
OVERKILLは1戦3.88回も起きるが、余剰割合の中央値は18%、100%以上はわずか2.5%（10戦に1回）、
300%以上は実測0%。つまり旧しきい値（揺れ300%・カットイン500%）は**実プレイで発火していない**。
《蹂躙》以上（100%以上）で揺れとカットインを出すよう下げた。日常の小さな余剰は短いまま流す。
これは第3節「尺は事件の大きさに比例」の意図どおりの姿で、しきい値がデータと合っていなかった。

段階別の平均尺は 序盤13.8→14.8秒、終盤45.3→46.0秒（`node tools/analysis-duration.js`）。
保護占有率は 51〜61% → 54〜66% へ上がった。`node tools/sim.js 50` はクリア率6〜62%で帯は不変
（`battle_scene.js` はシミュレータから読まれないので当然）。全Nodeテスト29本と
ブラウザ23本すべて通過。

### 戦果パネルの行レイアウト（2026-09-02完了）

非ダメージ貢献のバッジを足したことで、**働いた者ほど名前が読めなくなっていた**。
名前とバッジが同じグリッド行を取り合い、実測でバッジ5個のとき名前の幅が **1px** まで潰れていた
（`text-overflow: ellipsis` なので「古…」とすら出ない）。

1行を「アイコン＋名前＋与ダメージ」「バッジ」「バー」の3段に分けた。バッジは自分の行で折り返し、
バーは全幅になる。行の高さはむしろ縮んだ（120px→116px / 92px→88px / 74px→67px）。

守りたい性質は「誰が働いたか読める」ことなので、`tools/browser-tests/report.js` で
バッジ4〜5個の行を描き、`scrollWidth <= clientWidth`（省略されていない）とバーの幅を見る。
旧レイアウトへ戻すと落ちることを確認済み。

### ブラウザ一括回帰の復旧（2026-09-02完了・23本すべて通過）

長らく「一括回帰は `smoke` 等がタイムアウトするので完走対象外」としていた問題を直した。
**環境の問題ではなく、こちらの回帰が2つ重なっていた。**

1. **モルモ報告が下の画面を覆う**。報告を自動で閉じない仕様（意図どおり）にテストが追随しておらず、
   報告が出ている間 `[data-action="hire"]` などをクリックできずタイムアウトしていた。
2. **「次へ」ボタンが無限に動いていた**。`mormoNext` が `transform: translateY(3px)` を
   `infinite alternate` で当てていたため、ボタンの位置が毎フレーム変わる。Playwright は
   要素が静止するまでクリックしないので、報告を送ること自体ができなかった。
   これは実機でも「押す先が動く」ので、`filter` と `box-shadow` の発光へ置き換えた（位置は動かさない）。
3. さらに、開幕3日間プロトタイプの導入前に書かれたテストが `missionpick` を直接探しており、
   `preparation` で止まっていた。`softlock` は魔王が複数になる前の「タイトルの新規ボタンは1つ」を
   見ていた。どちらも実装ではなくテスト側の陳腐化。

対応:

- `tools/browser-tests/helpers.js`（新規）に3つの共通ヘルパーを置いた。
  `dismissMormo(page)`（出ている報告を1つ送る）、`autoDismissMormo(page)`（以後の報告を即送り。
  `goto` の前に呼ぶ）、`silenceMormoFromNow(page)`（報告が出ること自体を先に確認するテスト用）、
  `enterMissionPhase(page)`（開幕3日を終えて通常ループへ入る）。
  `daily.js` が自前で持っていた `dismissMormo` が事実上の正解パターンだったので、それを共有化した形。
- 報告の中身を見ないテストは `autoDismissMormo` を1行足すだけ。`smoke` は「報告が全画面で出ること」を
  確認したうえで以降を黙らせる。`mormo.js` だけは実際のクリック・キー操作をそのまま検証する。

**新しいブラウザテストを書くときは、`helpers.js` を通すこと**（報告を閉じ忘れると必ず30秒待って落ちる）。

検証: `sh tools/browser-tests/run-all.sh` が 23/23 通過。全Nodeテスト29本通過、
`node tools/sim.js 50` も14戦略完走で従来どおり。

### KPI測定契約（2026-09-02完了）

設計憲法 第14節。ロードマップ「仮説試行と『もう1回』を測る」の第1段階。
**外部送信なし・分析画面なし**で、端末内へ行動だけを残す。

- `src/core/kpi.js`（新規・DOM非依存）。LocalStorageキー `maou_kpi` を1つ追加した。
  記録は `buildAttempts` / `formationChanges` / `quickRetry` / `sessionRun` /
  `speedChanges` / `logSkips` / `reportSkips` / `lastScreen`。
- **ビルド試行の定義**: 出撃隊（uid・順序・tplId・階級・特性）、部門配属、施設Lv、給与方針、
  **雇った傭兵（種族）、キングスライム合体の可否**、作戦種別（＝狙う資源）から指紋を作り、
  前戦と違えば1回。初戦は必ず1回。指紋は表示にも計算にも使わない。
  **新しい「編成の判断」を足したら、この指紋にも足すこと**（足し忘れると、その判断は
  仮説試行として数えられず、KPIが実際より少なく出る）。
- 呼び出し口: `Game.newRun/deploy/endRun` と編成操作3つ（`toggleDeploy` /
  `assignDepartment` / `moveDeployed`）、`BattleScene.cycleSpeed/skip`、
  `MormoScene.advance`（タイプ中のみ）、`App.render`（画面記録）。
  `run.js` からは `Game.kpi(method, ...)` 経由で呼ぶので、**kpi.js を読み込まない既存テストは
  そのまま動く**（UI側は `typeof KPI !== "undefined"` で守る）。
- **状態契約**: KPIはラン状態の外に置く。したがって `saveCheckpoint` / `retry` の対象外で、
  再起しても減らない。魔界史の `maxChain` / `maxOverkill`（巻き戻る）とは逆で、意図的。
  ラン状態のセーブへKPIを混ぜないことをテストで固定している。
- 読み方: DevTools で `copy(KPI.export())` → JSONを保存 → `node tools/kpi-report.js kpi.json`。
  「ビルド試行 ÷ 戦闘数」が高ければ足りないのは種族・役割、低ければ試せる回数・組み替えやすさ。
- `tools/sim.js` も同じ計測を通り、戦略ごとに「ビルド試行 平均n回/ラン」を出す。
  現在のAIプレイは 6.8〜14.1回/ラン（戦闘数のほぼ毎回で編成が変わる）。これは基準線であって
  人間の値ではない。次は実プレイ5〜10ランで人間側の値を取る段階。

検証: `node tools/test-kpi.js`（試行の判定7種、編成変更、テンポ操作、画面記録、再起で減らないこと、
60秒判定、ラン状態を汚さないこと、壊れた保存データ、件数上限）。
実際の `deploy()` を1回通して呼び出し経路も固定してあり、`run.js` のフックを外すと落ちる（確認済み）。
`tools/browser-tests/kpi.js`（新規・クリック非依存）で実機の配線を確認。
全Nodeテスト29本通過。`node tools/sim.js 50` はクリア率10〜58%で従来の帯のまま。

### ラン全体の最大CHAIN・最大OVERKILLを魔界史へ（2026-09-02完了）

設計憲法 第11節「主要記録は最大CHAINと最大OVERKILL」の実装。各戦闘の記録は戦果画面で
見えるようになっていたが、ラン終了時に失われていた。

状態契約:

- ラン状態へ `maxChain` / `maxOverkill` を追加。新規ランは0。
  `deploy()` が戦闘ごとに `result.chainSummary.maxChain` と `result.overkillSummary.maxPercent`
  との最大値を取る。**勝敗を問わない**ので、再起せず確定した最終敗北も記録に入る
  （`endRun()` は `deploy()` / `concede()` の後段で呼ばれるため、両方の経路で保存される）。
- **再起の扱い**: `saveCheckpoint()` は state の全キーを写すので、この2つも一緒に巻き戻る。
  巻き戻された戦闘の記録は残らない。KPI用テレメトリのような「巻き戻しても残る別記録」は
  意図的に作っていない（作ると再起の契約が二重になる）。
- `migrateState()` の defaults へ両方を0で追加。旧セーブ・旧チェックポイントも0で移行する。
- `endRun()` の歴史レコードへ `maxChain` / `maxOverkill` を保存。クリア・敗北の両方。
- UI は `UI.recordHighlights(record)` 一つを終了画面と魔界史カードの両方で使う。
  フィールドが無い旧レコードは0として表示する。主要記録はこの2つだけで、総余剰・獲得G・
  召喚数などは並べない。恒久ボーナスへも接続しない（記録は記録のまま）。

検証: `node tools/test-run-records.js`（初期値0、最大値だけが残る、低い記録で上書きされない、
OVERKILL未発生でも安全、再起でチェックポイントへ戻る、クリア時と最終敗北時の保存、
旧セーブ・旧魔界史の0移行、増やした記録が2つだけであること）。
冒頭で実際の `deploy()` を3戦回して記録経路そのものを固定してあるので、
`run.js` の更新行を消すとこのテストは落ちる（確認済み）。
`tools/browser-tests/records.js`（新規・クリック非依存）で終了画面と魔界史カードの表示を確認。
全Nodeテスト28本通過。`node tools/sim.js 50` はクリア率12〜58%で従来の帯のまま。

### 戦果画面のCHAINと非ダメージ貢献（2026-09-02完了）

`docs/SYNERGY_VERTICAL_SLICES.md` 実装順7。CHAINは集計していたのに戦果画面から見えず、
貢献度が与ダメージ中心だったため、略奪・蘇生・能力発火が「無かったこと」になっていた。

- `Battle.deepestChainPath(timeline)`: 最深イベントから `parentEventId` を逆にたどり、
  **代表経路を一本だけ**返す（分岐した枝は並べない）。1段の文言は `Battle.chainStepLabel()`。
  同深度なら先に起きた方を選ぶ。親リンクが壊れていても `seen` で止まる。
  因果メタデータの無い旧データ、単発（深度1）は `null` を返し、表示側は経路を出さない。
  `chainSummary.deepest = { chainId, depth, steps:[{eventId,type,depth,label}] }`。
- `Battle.summarizeContribution()` に非ダメージ貢献を追加。すべてタイムラインからの導出:
  `traitTriggers`（`trait_trigger.sourceId`）、`resources`（`resource_gain` − `resource_forfeit` を
  資源別に純増減）、`revivesGiven`（`revive.sourceId`）、`selfRevives`（`sourceId` の無い蘇生を
  `unitId` へ）、`healed`（`heal.unitId`）。施設由来や召喚物は誰の手柄か言えないので付けない。
- UI: `UI.breakthroughPanel(battle)` を新設し、勝利・敗北・ゲームオーバーの3画面で共通に使う。
  見出し記録は**最大CHAINと最大OVERKILLの2つだけ**。旧 `overkillPanel` と 💀召喚パネルは廃止し、
  OVERKILL回数・総余剰・蘇生回数・召喚数は詳細1行へ落とした。
  個人行のバッジは `UI.contributionExtras()` が組み立て、0のものは出さず最大3つ。

**非ダメージ貢献は表示専用**である。`awardMerit()` は従来どおり `dealt / taken / kills / survived`
だけを見る。評価軸を増やすと「強い編成」がぼやけるので、恒久報酬へは接続しない。
魔界史への最大CHAIN・最大OVERKILL保存は次の独立タスク（バックログ「設計憲法への4契約統合」の契約3）。

検証: `node tools/test-battle-report.js`（代表経路が一本であること、旧データで空になること、
略奪Gの帰属、殉職手当の没収差し引き、死霊術の術者帰属、執念の自己帰属、召喚物を混ぜないこと、
非ダメージ貢献を足しても戦功が変わらないこと）。`deepestChainPath` を「同じchainIdを全部並べる」
実装へ壊すと最初のアサーションが落ちることを確認済み。
`tools/browser-tests/report.js`（新規。主要記録2つ・代表経路・バッジ・OVERKILLパネルの重複が無いこと）。
全Nodeテスト27本通過。`node tools/sim.js 50` はクリア率2〜46%で従来の帯のまま（計算経路は不変）。

### 戦闘の尺は事件の大きさに比例（2026-09-02完了）

`GAME_DESIGN_PRINCIPLES.md` 第3節の契約。仕様は `docs/BATTLE_PACING_SPEC.md`。
旧実装は「総尺45秒超で全イベントを一律加速」で、契約と向きが逆だった（見せ場ほど速く流れる）。

- core は**印だけ**を足した。`death.permanent`（味方が蘇生せず退場）、`result.reversal`
  （総HP3割以下から勝った・`Battle.detectReversal()` がタイムラインから導出）、
  `synergy.firstDiscovery`（そのランで初めて見たシナジー）。計算・乱数・勝敗は不変。
- UI は `BattleScene.plan(timeline)`（純関数）が保護区間と圧縮対象を分け、
  予算 `BUDGET_MS` 45秒を超えた分を圧縮対象からだけ削る（下限 `MIN_COMPRESS` 0.45）。
  保護は `PROTECTED_TYPES`・子を持つ起点・`permanent` な死亡・深度3以上の攻撃。
  `magnitude()` が深いCHAIN・OVERKILL・初発見・逆転・永久戦死を延長する。
- 予算は上限ではない。保護区間だけで45秒を超える戦闘は、超えたまま見せる。

測定（`node tools/analysis-duration.js` 200ラン、x1平均・段階1→8）:

| | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 実装前 | 13.3s | 15.9s | 25.4s | 26.3s | 33.3s | 37.0s | 39.7s | 44.4s |
| 実装後 | 13.8s | 16.1s | 25.4s | 27.7s | 34.0s | 38.6s | 40.7s | 45.3s |
| 保護占有 | 51% | 55% | 46% | 46% | 47% | 49% | 52% | 61% |

序盤の +0.5秒は延長規則によるもので、内訳は OVERKILL +0.37秒・初発見 +0.06秒。
退屈な区間が伸びたのではなく、序盤にも起きる事件が大きさどおりに見えるようになった。
終盤の最長が 45秒 → 65秒 に伸びたのは、保護区間だけで予算を超える乱戦を許した結果である。

検証: `node tools/test-battle-pacing.js`（10項目。`isProtected` を常に false にすると
項目3と6が落ちることを確認済み）、`tools/browser-tests/pacing.js`、全Nodeテスト、
`node tools/sim.js 50`（クリア率 4〜58%の帯に変化なし。計算経路を触っていないので当然）。

### 部門・食料・施設由来の事件（P3・完了）

既存8事件が戦闘・給与・忠誠に偏り、配属結果が勤務報告の数字だけで終わっていたため、
`events.js` へのデータ追記だけで3事件を追加した。新しい状態項目や `core/` 契約は増やしていない。

- **食堂占拠**: 食料不足から発生。4Gで食料を買う／最も大食いの戦闘員を生活部門へ異動し
  給与+1Gで炊事責任者にする／兵糧攻めで忠誠を失う。危機が次の配属変更へ直結する
- **余った食料の行方**: 生活部門が余剰を作ったとき発生。食料を全員の忠誠、5G、将来備蓄の
  どれへ変えるか選ぶ。生活ビルドの成功が単なる備蓄量で終わらない
- **施設完成の功績争い**: 施設完成時、施工適性が最も高い担当者が登場。3Gの報奨、
  給与+1Gの名誉、建材2への再投資から選ぶ。施設・履歴書適性・給与・忠誠を接続する

`tools/test-department-events.js` で発生条件、配属変更、資源変換、忠誠コストを検証する。
`analysis-events.js` は作戦・部門導入後に0戦で終わっていたため、必要データ読込と
mission処理、生活・建設への最低配属を修復。300ランで全11事件が発生・解決した
（食堂占拠14、余剰食料38、施設功績25）。

**測定（`node tools/sim.js 200`）**: 三部門均衡46.0%、未払い搾取50.0%、全戦略10.0〜58.5%。
P2直後の帯12〜57.5%とほぼ同じで、イベント追加によるバランス崩壊はない。

**追記（2026-09-01・イベント案35件の選別）**: 添付企画を既存11事件と現行契約に照らし、
新しい履歴状態なしで編成差が出る3件だけを追加した。まかない失敗は生活適性・食事不要体質、
掃除当番論争は異種族配置・掃除職歴、鉄アリは建材備蓄・建設適性で結果が変わる。
夜宴会は既存の余剰食料事件と重複するため不採用。新人備品、名刺交換、突貫工事はそれぞれ
採用直後・次戦闘効果・建設方式の履歴がないため保留。給与抗議チェーンは既存の賃上げ要求と
戦場ストライキをまとめる後続状態を設計してから実装する。
`analysis-events.js` 300ランでは追加分が味付け54、掃除54、鉄アリ27回発生し、当時の全14事件を解決。
初回は掃除1・鉄アリ2回と狭すぎたため、異種族と建材量を必須条件ではなく内容・選択肢を
変える条件へ移した。

### 給与方針（P2・完了）

固定だった給与処理を、編成画面で毎戦選ぶ3方針へ接続した。

- **通常支給**: 勝利後に通常額を払う。既存挙動を維持し、資金不足なら自動未払い
- **今回は未払い**: 0G。出撃前に勤務者へ `unpaid` を立てるため、同じ戦闘から
  `rage_unpaid`、戦場ストライキ、戦闘後の一言、BGMの不満へ反映する。
  勝利後に未払い回数を進め、忠誠を15、30、45……と段階的に下げる
- **前払い・厚遇**: 通常額の1.5倍（切り上げ）を出撃前に払う。未払い回数を消し、
  勤務者の忠誠+8。敗北・戦死でも返金されず、資金不足なら選択できない

給与額は従来どおり `salaryAssignments()` を正本とし、P1の経理割引も3方針すべてへ効く。
選択回数は `payrollChoices` と魔界史レコードへ残す。旧セーブは通常支給へ移行する。
キングスライムは合体元の未払い状態と最長未払い回数を継承する。

**測定（`node tools/sim.js 200`）**:

- 通常の三部門均衡: クリア率49.5%、不祥事869件
- 血の気持ちがいて忠誠に余裕があると未払いを選ぶ「未払い搾取」:
  クリア率53.0%、不祥事1,396件、未払い487回、厚遇46回
- 全戦略のクリア率帯は12〜57.5%。未払いは少し得だが組織が荒れる別ルートで、唯一解にはなっていない

`tools/test-payroll.js` 17項目で、厚遇の資金不足拒否・先払い・二重請求防止、
未払いの戦闘前接続・連続ペナルティ、通常支給、旧セーブ移行を検証する。

### 部門適性（P1・完了）

`GAME_DESIGN_PRINCIPLES.md` 第5・7・8節に対して、部門が「人数」でしか効いていなかったのを直した。
**種族・履歴書の職業欄が部門効果に接続する**ようになり、「誰を置いても同じ」が消えた。

- `src/data/departments.js` に `RACE_APTITUDES` / `JOB_APTITUDES` / `Aptitude` を追加。
  効きは4本だけ（`food` `material` `wage`% `recruit`名）＋ `appetite`（食う量）。
  **これ以上チャネルを増やさないこと。** 配置の判断が読めなくなる
- 適性 = 種族ベース ＋ 職業欄の部分一致加算。職業欄はこれまで表示用のフレーバーだったが、
  ここで初めてゲーム判断になった（第8節）。未登録の種族は `DEFAULT_APTITUDE` に落ちるので、
  **CodeX がモンスターを追記しても壊れない**
- `Aptitude.contribution()` は配属先で効く適性だけを返す。
  戦闘部門の会計係は給与を下げない ＝ 「どこに置くか」が判断になる
- 生まれた接続（意図的に仕込んだ発見）:
  - **アンデッドは `appetite` 0** → 骸骨・ゾンビ軍団は食料問題が消える。
    死の軍勢シナジーと二重に噛む
  - **オーガ＋前職「重量物運搬」で施工能力7** → 桁が違う。「これ強すぎない？」枠
  - **建設部門の死霊術師 = 供養代行** → 直前の戦没者1名につき建材+2。
    戦死という損失が別部門の資源になる
  - **人事・受付・営業の職歴を非戦闘部門へ** → 応募者が3名から最大6名に増える
  - **会計/取り立て/契約書偽造** → 給与総額を圧縮（合計上限60%。無給にすると経営判断が消えるため）
- `Game.departmentOutput()` が唯一の集計入口。給与・食料・建材・応募者数・UIは全部ここを通す。
  **数値を足したくなったらここに集約すること**（run.js や ui.js に散らさない）
- 食料消費は頭数ではなく `appetite` 合計 ÷ 3。旧仕様（頭数÷3）と平均の尺度は同じで、
  種族差だけが乗るようにしてある

**測定（`node tools/sim.js 200`）**:

| 戦略 | 変更前 | 変更後 |
|---|---|---|
| 三部門均衡 | **68.5%** | 47.5% |
| 中盤で精鋭に転換 | 58.5% | 60.5% |
| 魔法職寄せ | 60.5% | 59.0% |
| 慎重経営 | 11.5% | 19.0% |
| 精鋭3体 | 17.5% | 15.0% |

**狙い通り「三部門に均等に置けば勝つ」という唯一解が壊れた**（68.5% → 47.5%、首位から3位へ）。
帯は 14〜60.5% で、変更前とほぼ同じ広さ。

途中で一度、種族ベースの food 平均が旧仕様の一律2を下回っていたせいで
食料不足が急増した（三部門均衡で 35回 → 884回）。`RACE_APTITUDES` の food を
全体に上げ直して 535回 に収めた。**適性表をいじるときは平均が2前後から
ずれていないかを必ず見ること。**

`tools/test-departments.js` を21項目に拡張（適性・配属先による効き分け・
経理割引・人事の集客・供養代行を検証）。ブラウザ19本は通る。
`smoke` が一度だけ落ちたが単体再実行では通り、`tier0` と同種の間欠と判断した。


### 一本道の8連戦を作戦選択式へ変更（完了）

採用後に「辺境を略奪」「魔界の反乱を鎮圧」「王国へ進軍」の3作戦を提示する。
侵攻勝利だけが王国攻略を進め、8拠点でクリア。略奪は給与を賄いつつ蓄えを作れ、
鎮圧は生存者の忠誠を回復する。寄り道は警戒度を上げ、敵能力へ反映される。

進行状態の本体は `turn`（通算作戦数）、`conquest`（攻略数）、`alert`（警戒度）。
`stage` は既存イベント・テスト・旧セーブ互換のため `conquest + 1` として残している。
作戦画面の収支は `報酬 - 現在の給与`。旧セーブの編成画面は作戦会議へ安全に戻す。

### 最大20体の軍団と出撃隊選抜（完了）

`roster` は軍団全員（最大20体）、`activeUids` は今回出撃する最大5体の順序を表す。
戦闘・シナジー・戦死・キングスライム合体は出撃隊だけが対象。事件・忠誠・離反は軍団全体が対象。
給与は出撃隊だけが全額、控えは0G。弱敵には安い隊を出し、高給の主力を温存する判断ができる。
6体目以降は1体につき敵能力+2%（最大+6%）の「軍勢警戒」が作戦生成時にかかり、カードにも明示する。
旧セーブには先頭5体を自動選抜する。直接テスト用ロスターを差し替える場合は `activeUids` も更新すること。

### 戦功による三段階昇進（完了）

生還で戦功+1、撃破は最大+2、与ダメージ最多+2、別の盾役+1。閾値は小隊長4、魔将10、将軍22。
昇進時の能力・忠誠・給与変化は `src/data/promotions.js` に置く。将軍は
`src/data/synergies.js` の「将軍の号令」へ接続され、出撃隊全員の与ダメージ+15%。
`generalsMade` は一度でも輩出した将軍を保持し、戦死・離反後も魔界史へ残す。

### 殿堂入りと再応募（完了）

ラン終了時、生存者から戦功優先・同点なら現在戦力優先で1名を `record.hallOfFame` に保存する。
契約は `{ name, tplId, race, job, prevJob, motive, flaw, quote, generation, merit, rankId }`。
能力値は保存しない。次代開始時に歴代殿堂入りから25%で1名を抽選し、最初の応募者枠へ混ぜる。
ラン状態の最小契約は `legacyReturn`（候補または `null`）と `legacyOffered`（同一ランの再提示防止）。
再応募者の `legacy` は出身代・以前の戦功・階級を表示するためだけの印で、能力・戦功・階級は新任扱い。
旧セーブと殿堂入り以前の魔界史は、候補なしとしてそのまま読める。

### 魔物採用図鑑（完了）

ラン状態の `recruitedTplIds[]` は一度でも採用したテンプレートIDを保持し、死亡・離反では消さない。
ラン終了時に同名の魔界史フィールドへコピーする。`finalRoster[]` にも今後は `tplId` を保存する。
魔界史画面は全レコードを集約し、登録済み種族だけ名前と職種例を開示する。未登録枠は伏せる。
旧セーブは現在の `roster` から補完し、旧魔界史は `finalRoster.tplId` または `hallOfFame.tplId` が
存在する範囲だけ復元する。

### シナジー図鑑（完了）

ラン状態の `discoveredSynergyIds[]` は戦闘タイムライン上の `synergy.id` から登録する。
表示名ではなくIDを正本にするため、キングスライム合体を含む全シナジーで同じ契約になる。
ラン終了時に魔界史の同名フィールドへ保存し、全レコードを集約して発動済みだけ名前と効果を開示する。
未発見シナジーは条件も名前も伏せる。旧セーブ・旧魔界史は空配列として安全に扱う。

### 魔王実績（完了）

`src/data/achievements.js` の `ACHIEVEMENTS` は `{ id, name, desc, check(history) }`。
保存キーや解除フラグは増やさず、魔界史から毎回後計算する。現在は初記録、征服、未払い通算5回、
施設Lv.3、戦功22殿堂入り、全種族採用、全シナジー発動の7件。報酬による恒久能力上昇は付けない。
新しい実績は既存の遊び方を試す目標になるものだけ追加し、単純な作業回数稼ぎは避ける。

### 複数魔王（最小実装完了）

`DEMON_KINGS` は `{ id, name, icon, desc, start: { gold, food, materials, hires } }`。
新規ゲームのボタンからIDを `Game.newRun(id)` へ渡す。ラン状態は `demonKingId`、魔界史は
`demonKingId / demonKingName` を保存する。旧セーブ・不明IDは `standard`（若き魔王）へ戻す。
人事魔王は7G・食料4・初回採用3名、築城魔王は6G・食料3・建材5。数値ボーナスの上下版ではなく、
人手と給与、建設と手元資金という既存の三部門判断へ接続する。追加魔王も開始時の不足の形を変えること。

### 戦闘中のハプニング（完了）

定義は `src/data/battle_happenings.js`。1体につき1戦闘1回までで、未払いはストライキ、
忠誠45未満はサボり、25未満は仲間割れが候補になる。生存中の将軍がいれば発生率は65%減。
タイムラインの `incident` が演出と結果表示の契約で、仲間割れダメージは戦功集計から除外する。

### 戦闘の見せ場演出・第一段階（完了）

戦闘計算とタイムライン契約は変えず、`BattleScene` の再生だけを観戦向けにした。
各ラウンドは「前ラウンド終了 → ROUND n」で区切り、攻撃者と対象をカード枠と中央字幕で示す。
シナジー・不祥事・蘇生・耐久・最終決戦は専用色で区別。標準速度の実測平均は
序盤11秒、中盤20〜31秒、終盤36〜41秒で、長期戦はおおむね45秒まで自動圧縮する。
急ぐ人のため x2 / x4 / スキップは維持している。`node tools/analysis-duration.js` で再測定できる。

### ニアミスの可視化（完了）

敗北画面と敗北確定画面に、敵耐久を75%以上削れたときは「最も追い詰めた瞬間」、
それ未満なら誇張しない「敗因メモ」を表示する。
敵軍を何%削ったか、敵総HPがいくつ残ったか、決着前の最後の出来事を示すため、
プレイヤーは死因の定型文だけでなく「次の採用・作戦・配置で何を変えるか」を考えられる。
敵が回復／蘇生した場合は、最もHPが低かった時点と最終HPを分けて表示する。

`Battle.summarizeNearMiss()` は開始時スナップショットとHP差分をタイムラインから再生して導出する。
戦闘中の状態やタイムライン契約は増やしていない。検証は `node tools/test-near-miss.js` とブラウザテスト `nearmiss`。

### サウンドの土台（完了）

`src/ui/sound.js` に音声処理を集約。攻撃・防御は `assets/sfx/` のオリジナルWAV
（ばすん・がちゃん・ずし・ずばっ、各3変種）、その他はWeb Audio APIで鳴らす。
ラウンド・シナジー・不祥事・最終決戦・勝敗・昇進・モルモ発話音を含め、キューは全21種。
最初のユーザー操作でAudioContextを解禁し、音量とミュートはLocalStorageへ保存する。
WAVは `tools/generate-sfx.js` から再生成でき、外部素材は使っていない。
研究根拠は `docs/SFX_RESEARCH.md`、比較試聴は `tools/sfx-lab.html`。
実素材へ交換する場合も `Sound.cue()` の名前を契約として保つこと。
検証は `node tools/test-sound.js` とブラウザテスト `sound`。

### 宰相モルモの全画面報告（完了）

従来の各メニュー上部にある小さな案内枠を廃止し、**意味のある画面遷移を区切る全画面シーン**へ変更した。
新規開始・再開、作戦決定→編成、戦闘→戦果、戦果→事件／次の採用、事件の事後報告、再起・終了で出る。
食料不足、施設完成、勝敗などゲーム状態から表情と台詞を選ぶ。

表示制御は `src/ui/mormo_scene.js` に分離した。遷移先画面を先に描画してから固定オーバーレイを載せ、
台詞を27ms/文字（句読点は間を置く）で表示する。2文字ごとの短い三角波が発話音。
ボタン／Enter／Spaceは「全文表示→閉じる」の2段階。読み終わっても自動では閉じず、
プレイヤーの明示入力を待つ。画面の余白やモルモ画像のクリックでは進まない。
`prefers-reduced-motion` では最初から全文を表示する。ゲーム状態と `core/` は変更していない。

検証は `node tools/test-sound.js`、ブラウザテスト `mormo`、`smoke`、および全既存ブラウザテスト。
`mormo` は全画面寸法、画像読込、文字の増加、発話キュー、タップ／キー操作、作戦→編成の節目を検査する。

### 通常進行＋戦闘BGM（完了）

**BGMは軍団そのものが演奏している。** 当初の「場面ごとに曲を差し替えない」は、
実プレイで戦闘まで同じ旋律が続いて単調というフィードバックを受けて更新した。
通常進行と戦闘の2曲に絞って場面差を出し、軍団レイヤーは両方へ共通して重ねる。
判断の根拠と方針変更は `docs/BGM_DESIGN.md`。

出撃隊の頭数が行進ベースの音数になり、将軍がいれば号令ラッパ、アンデッドで不気味な
コーラス、術者で魔法の鈴、スライムで間の抜けた裏拍が鳴る。警戒度でテンポが上がる。
そして**給料未払いの隊員はサボって音を出さず、低忠誠はピッチと拍をよれさせる**。
給料を払わないと行進曲がだれるのが耳で分かる、という不祥事の見せ方になっている。

戦闘と同じく計算と見せ方を分けてある。

```
Music.describe(state) → descriptor → Music.apply(descriptor)
（軍団の解釈・純関数）   （契約）      （CC0実曲＋同期アクセント）
```

**`descriptor` が唯一の契約**（`{ scene, bpm, root, layers:{名前:{gain,...}}, unrest, alert }`）。
`describe()` は DOM も Audio も触らない純関数なので node から検証できる。
現在の `apply()` は通常進行で CC0 の **Raiders March / Bobjt**、戦闘で
CC0 の **Boss Battle Theme / Cleyton Kauffman** を音楽の芯にし、既存の合成レイヤーを
編成差の薄いアクセントとして同期する。出典は
`assets/bgm/LICENSES.md`。別素材へ交換しても、**レイヤー名と gain の意味さえ保てば
`describe()` とゲーム側は一切触らずに済む**。

`core/` と `data/` は触っていない。ゲーム側の接続は `src/main.js` の `App.music()` と
`BattleScene` の2箇所（場面名を渡すだけ）。BGMのオンオフは `maou_bgm` に保存し、
音量とミュートは効果音と共有する（`Sound.master` にぶら下げている）。

**追記（2026-09-01）**: 純合成を主役にする方針を終了し、CC0実曲へ差し替えた。
約62.44秒・123 BPM・E minorのループを約1.2MBへ最適化。計測値は約 -15.9 LUFS /
-1.75 dBTP。実曲の調とテンポへ既存アクセントを合わせ、`Music.MIX` は `.55` → `.2` に
下げた。場面差は実曲の速度を約±8%の範囲で変え、アンデッド・未払い・低忠誠は
フィルター／音量／音抜けへ反映する。音量スライダー、ミュート、BGMトグル、file直開き、
音源欠落時の合成フォールバックは維持。

**追記（2026-09-01・戦闘曲）**: 出撃時に `battle-theme.ogg` へ曲頭から切り替え、
決着表示で通常曲へ戻る。戦闘曲は約114.34秒、Ogg Vorbis 約2.0MB、約 -15.9 LUFS。
`tools/test-music.js` とブラウザテスト `music` は「同じURLを速度だけ変える」退行も検出する。

検証は `node tools/test-music.js` とブラウザテスト `music`。

**追記（2026-08-31）**: 初回実装は実機（Androidスマホ）で無音だった。原因と修正は 3-7節を参照。
`Music.MIX` は `.3` → `.55`、`hiss()` の減衰式から余分な `*.12` を除去し、
足音に中域のクリックを足して小型スピーカーでも輪郭が聞こえるようにしてある。

その修正後、オーナーが実機で再確認 → **「まだ小さい」で保留**にいったんなった。

**追記（2026-08-31・続き）**: `MIX` をさらに上げる対症療法は避け、代わりに
**音域と音の重ね方**を変えた。詳細は `docs/BGM_DESIGN.md` 3-2節。

- 足音・行進ベースの基音を、実際にスマホで再生される帯域まで上げた
  （旧: 32〜110Hz中心 → 新: root付近〜倍音でroot×1.7〜2）。低い音域のまま
  音量だけ上げても、出せない帯域はどのみち出せない。
- オクターブ上の倍音を控えめに重ねた（欠落基音の錯覚）。基音が再生されない
  スピーカーでも、耳は倍音から元のピッチ・低さを補って聞き取る。
- 号令ラッパと魔法の鈴に、わずかにデチューンした2〜3声のユニゾン
  （`Music.unison()`）を入れた。単純な波形1本の単音を、音量ではなく
  質感で厚くする狙い。

`Music.MIX` 自体はいじっていない。`tools/test-music.js` に「足音・行進ベースが
130Hz未満に閉じ込められていないか」を測るテストを追加し、旧実装へ巻き戻すと
実際に落ちることを確認済み（node/ブラウザ両方の回帰テストは全て通過）。

この純合成のみの案は上記2026-09-01差し替えで主役から外れた。次はAndroid実機で
実曲版の音量・ループ継ぎ目・長時間の聴き疲れを確認する。

### 効果音の次段階: 純合成から実録フォーリーへ（調査中・未実装）

オーナー試聴結果: 現在の `basun/gachan/zushi/zuba` は
**「まだ合成っぽい、初期ファミコンのようでちゃっちい」**。純合成をさらに磨く方針は
ここで打ち切り、迫力のある実録素材を核に差し替える。既存12音は比較用・フォールバックとして
残し、採用品が決まるまでは置換しない。

進捗:

1. CC0実録候補を `assets/sfx/candidates/` へ別名で追加し、`tools/sfx-lab.html` に現行12音とのA/B試聴を作成した（ゲーム本体ではまだ未使用）。
   ライセンスは `assets/sfx/LICENSES.md`、調査の判断根拠は `docs/SFX_RESEARCH.md` を参照。
   金属感を抑えた「ズバッ／しゃり」候補として、`candidate-artisticdude-swish-01`〜`13`（衣類ハンガー・木材の実録）も追加済み。
2. **保留（全候補をオーナーが後で確認）**。初見では鈍い打撃の `candidate-antum-thwack-04` が有力。
   剣の候補は金属感が強すぎるため、「ズバッ／しゃり」と聞こえる、現実の金属衝突そのものではないゲーム的な手触りを探す。
   ロマサガ／FFは機能的な参考試聴に留め、音源の抽出・サンプル利用・模倣コピーは行わない。
3. 選定後にだけ、剣の風切り、肉・鎧への衝突、低域の重量音を1音へレイヤーする。
   単体素材のポン置きではなく、実録を中心にEQ・コンプレッサー・短い残響でゲーム向けに整える。
4. オーナーが比較試聴して採用を決めた後だけ、`zuba/basun/gachan/zushi` の3変種へ書き出す。

**優先度メモ（2026-08-31・更新）:** オーナーの言う「サウンド」は効果音ではなく**BGM**を指していた。
BGMは実装済み（上の「BGM『魔王軍の行進曲』」）。
効果音候補の採用は**引き続き全候補の試聴まで保留**で、こちらから先へ進めないこと。

調査済み候補（2026-08-31時点）:

- **第一候補 / 法務が最も簡単**: OpenGameArt「20 Sword Sound Effects (Attacks and Clashes)」
  by StarNinjas。実際のナイフ同士を録音・加工した剣撃10音＋衝突10音、CC0。
  https://opengameart.org/node/122900
  ページ内の `sword_-_starninjas.zip` と `sword_clash_-_starninjas.zip` が個別音源。
- **品質の上限候補**: Sonniss #GameAudioGDC Bundle。ゲームへの組み込み、改変、商用利用、
  無表記が許可されるが、素材単体の配布は禁止。バンドルが巨大なので、必要カテゴリだけ取得できるか
  次回確認する。取得日のライセンス版が適用される。
  https://sonniss.com/gdc-bundle-license/
- **補助候補**: Pixabay の DavidDumaisAudio「Sword Slash With Metallic Impact」等。
  商用利用可能でゲーム内の複合作品への組み込みはStandalone配布ではない。ただし原音をそのまま
  リポジトリへ置くより、加工・混合して完成ゲーム内だけで配る運用が安全。
  https://pixabay.com/sound-effects/film-special-effects-sword-slash-with-metallic-impact-185435/

重要: ロマサガ等の既存ゲーム音源を抽出・模倣コピーしない。「鋭いアタック＋実録衝突＋重量ある
低域」という機能的な参考に留める。今回の調査だけではファイル追加、音声差し替え、コード変更は
行っていない。

### 戦死したモンスターを本当に退場させる（完了）

戦闘で倒れた者はロスターから永久に外れる。給与計算より前に外すので死者に給料は出ない。
次の面接で「通常1名＋欠員数」まで補充できる。詳細は README の
「戦死は本当の退場（欠員募集）」を参照。

判定に `died` ではなく `survived`（最終生存状態）を使っているのが要点で、
これにより蘇生系の特性が「永久退場を防ぐ保険」として機能する。

検証: `tools/browser-tests/casualty.js` と `node tools/sim.js 150`。
クリア率は 4.7〜47% の帯に収まっており崩壊していない。

## 5. バックログ（優先度順）

### 2026-09-09更新：現在の優先順位

1. **v0.2改稿済み**：[勇者襲来の縦切り設計](docs/HERO_ARRIVAL_VERTICAL_DESIGN_2026-09-09.md)。六結末の方向性確認と三点の修正を反映。
2. **次の準備**：独立試作の範囲・状態・既存計算との境界を狭く定める。通常迎撃と提供を同じ入口で触る候補。
3. **その後の候補**：レビューを受けた小さな実装・試遊。範囲と契約はまだ未決。
4. **横展開は保留**：洪水・反乱・研究・委任全体・複数地域・特殊END量産。

以下は既存のバックログ履歴。旧CHAIN優先順位を新指針より上位に扱わない。


- **sim の未完ラン（約5%）は解消済み。** 2026-09-08に全15戦略×200ランを
  `tools/deploy-falsy-probe.js` で再測定し、未完0/3,000。精鋭系が軍を空にしたまま進まない
  対策は現行 `tools/sim.js` に入っている。上の最新節を参照。
- **CHAINの閾値決定（オーナー／Astra待ち）。** 材料は `docs/CHAIN_V2_MEASURE_2026-09-07.md`。
  教訓「未完の記憶」はV1で既に死んでいる（0.3%）ので、V2の閾値は「V1と同じ当たり方に戻す」
  ではなく「この教訓を機能させるか、やめるか」から決める必要がある。

- **現在の優先順位（2026-09-06）**: `docs/BUILD_EXPERIENCE_DESIGN_2026-09-06.md` 第9節。
  V0基準測定 → V1/V2a/V2b/V3の3系統縦検証 → U1/R1の採用と初回機会 → R2/B1/U2の戦果帰属
  → D1/R3/U3の人材記憶 → P1初見試遊 → E1敵相性。全項目未着手。既存仕様で充足なら検証のみ。
- 採用された狙い: 「自分の採用で壊した」と分かること。略奪の手数・食料の集中打撃・死霊の反転を区別する。
- Jは全11種×3表情の接続完了。Sol引き継ぎの未生成枚数は過去の記録で、再制作タスクではない。
  C/F/Eも再実装不要。

- 以下は既存の保留事項・過去の優先順位。上記の新設計と競合する順序は新設計を優先する。
- `docs/WORK_SPLIT_2026-09-05.md` はA〜Jの完了履歴。テンポは「強弱＋各段の下限」で確定、等速3秒には戻さない。
- 食料系・死霊系も既存要素を使って縦検証し、略奪の手数とは違う見せ場を磨く。
- 強欲の回数制限を燃料消費へ変える案は未実装。まず今回の人材間接続の実測と体感を判断材料にする。
- 継承・新種族・長編化は既存候補として維持。今回の実装には含めていない。

- 打撃音はオーナー選定済みの08/09へ統一。再選定を求めず実戦音量を確認する。魔法音は別途評価。

- 最優先: 初見導線・編成表示・実録効果音をオーナー試遊。第1代の案内を見るときは既存セーブを消さず別ブラウザ等の新規保存領域を使う。
- 音: 物理音08/09の選定は完了。実戦の音量・連打感と魔法音を評価する。武器別の再選定や自作合成の再挑戦はしない。

- 敵10役: Codexで6ポーズ接続済み。通常編成とvariants全49エントリをart-coverage.jsで確認。遠隔の射撃・詠唱再生も接続済み。
- 味方11種は6ポーズ接続済み（今回残り7種を追加）。Geminiへの送信/画像受領待ちは不要。

- 種族・役割ごとの映像差を試写で評価する。6キーポーズ＋補間のため、中割り追加は可読性が足りない動作に絞る。

- 骸骨兵の原画更新時はidleのコマ境界の槍を収め、暫定CSSクリップを外す。

- 戦闘表示: 召喚・復活の再生/skip一致は対応済み。増援が多い戦場の縦スクロール負荷を実プレイで確認し、必要なら配置密度を調整する（戦闘人数・ルールは変えない）。

- 戦闘モーション: 全対象の専用絵・接触同期・倍速同期・飛翔体は実装済み。5対5の奥行き表現と初見の攻撃判読性を次の試遊で確認。詳細は `docs/BATTLE_MOTION_REVIEW.md`。

### CodeXへ：拠点接収に接続する能力案（Claudeからの案のみ・コードは触っていない）

拠点接収（0節）で**全ビルドが施設Joker3択に一度は触る**ようになった。
そこで効くのは「施設を持っていること／警戒度が上がっていること」を**既存語彙で読み替える**能力である。
数値を盛る案はここに書かない。採否と実装は `src/data/` 担当のCodeXに委ねる。
Claudeは接続と `tools/sim.js` での測定を担当する。

**1. 《接収者》（前職トレイト案・盗賊／元傭兵あたり）**
「施設Lv.1以上のとき、戦闘開始時に建材+1」。
接収→施設→建材→さらに建設、と**細かった建設ルートを一周させる**。
接続先: 供養代行（戦死→建材）、施設Lv.2/3、墓地。

**2. 《指名手配》（トレイト案）**
「王国警戒度が5以上のとき、自分のダメージ+10%／被ダメージ+10%」。
接収の代償である警戒度が、**罰から入口へ変わる**（0節5番「忠誠・給与を罰からビルド入口へ」と同じ狙い）。
略奪ビルド（警戒度+2/回）とも直結する。接続先: 略奪、拠点接収、精鋭主義。

**3. 《城代》（昇進・将軍の号令の追加分岐案）**
「将軍が生存していると、施設Jokerの発火回数+1」。
昇進・施設・Jokerという別々の語彙を1本に繋ぐ。**コンテンツを増やさず接続数だけ増える**案。

**4. 《取り壊し》（ハプニング案）**
「施設Lv.1のとき発生。施設を失う代わりに建材+6と金貨。あるいは維持して食料-3」。
0節4番「建て替えに代償」の軽い先行版。接収した拠点を手放す判断が生まれ、
**同じ施設を維持する理由**（0節の未確認項目4）を初めて問える。

**5. 敵編成案：拠点奪還軍**
「拠点接収を行ったランでのみ、以降の侵攻に『奪還隊』が混じる」。
自分の選択が敵の形を変える、いちばん短い接続。`src/data/enemies.js` 側の案。

いずれも「強いか」ではなく**既存の何と組み合わさるか**で選ぶこと（設計憲法 第4節）。
1・2・3は接続数が多く、4は判断を作り、5は事件を作る。


### 再設計統合（2026-09-02）

オーナー判断により、今後の最上位体験を「Balatro的なシナジー発見・連鎖・壊れビルド形成」に確定した。
戦闘・建設・生活は削除しないが、対等な独立管理ゲームとして拡張しない。現行仕様との競合、維持要素、
行き止まり、最初の代表ビルドは `docs/SYNERGY_CONNECTION_AUDIT.md` に整理した。

コード着手順は次のとおり。演出やコンテンツ追加を先行させない。

1. 現行のトリガーと戦闘タイムラインを使える共通イベント語彙へ整理（第一段階完了）
2. 代表3系統（略奪経済／暴食飢餓／戦死蘇生）を、発火から再発火まで設計（完了。`docs/SYNERGY_VERTICAL_SLICES.md`）
3. その3系統だけを通せる最小連鎖基盤と無限ループ防止を実装
4. CHAIN、発火理由、貢献者を可視化
5. OVERKILLを実装し、報酬・伝播・召喚の発火点へ接続
6. 食料と施設を維持コスト・単純補正からビルド規則へ変換
7. 2D戦闘、モルモ男女差分、殿堂拡張はコア成立後

新しい部門値、専用資源、管理画面は増やさない。既存の `food / material / wage / recruit / appetite`
を能力の発火条件として再利用する。壊れビルドは勝率差だけで弱体化せず、再現性と判断の消失を測る。

#### 設計憲法への4契約統合（2026-09-02・文書のみ）

`docs/DESIGN_REVIEW_2026-09-02.md`（Fable）と CodeX の仕分けを受け、オーナー判断で次の4契約を
`GAME_DESIGN_PRINCIPLES.md` へ追加した。既存方針は削除していない。コードは未変更。

1. **戦闘の尺は事件の大きさに比例する**（第3節）。通常攻撃と無反応区間を優先して圧縮し、
   CHAIN・OVERKILL・初発見・逆転・永久戦死は圧縮しない。
   → **実装済み（2026-09-02、4節「戦闘の尺は事件の大きさに比例」参照）**。
   一律加速をやめ、`BattleScene.plan()` が保護区間と圧縮対象を分けて計画する
2. **人材本人の能力・発火条件・効果は公開する**（第7節）。隠すのは組み合わせの結果と壊れ方だけ。
   表示は「トリガー：効果」、詳細数値と例外は詳細表示へ。
   → README「条件はゲーム内に一覧表示していない」と `synergies.js` 冒頭コメントの
   「条件はUIにはあえて全て表示しない」はこの契約と競合する。実装時に README を更新し、
   人材本人の能力文を短文化する。組み合わせ結果（シナジー名・壊れ方）は引き続き発動時に開示
3. **魔界史の主要記録は最大CHAIN・最大OVERKILLの2つ**（第11節）。その他の統計は詳細表示または
   廃業届へ。→ **実装済み（2026-09-02、4節「ラン全体の最大CHAIN・最大OVERKILLを魔界史へ」参照）**。
   ラン状態と歴史レコードへ `maxChain / maxOverkill` を保存し、終了画面と魔界史カードへ表示する
4. **KPI測定契約**（第14節）。リトライ率、セッション内ラン数、途中離脱、速度変更、報告スキップ、
   および「ビルド試行」（前戦から人材・編成・特性・施設・狙う資源状態が変化した戦闘）を数える。
   → **実装済み（2026-09-02、4節「KPI測定契約」参照）**。`src/core/kpi.js` と `maou_kpi`。
   分析画面は作らず、読むのは `tools/kpi-report.js`。`tools/sim.js` でも同じ計測が出る

CodeX の仕分けで保留になった案（四半期構造、建材廃止、魔王のルール化、勇者の位階、職業訓練、
廃業届、敵を問いとして設計、離反者が勇者側へ）は採用済みではない。詳細と根拠はレビュー文書を参照。

#### 共通イベント語彙・第一段階（2026-09-02完了）

`Battle.simulate()` が作る全イベントへ戦闘内一意の `eventId` を追加した。因果イベントは
`parentEventId / chainId / chainDepth` を持ち、通常攻撃を深度1として、死亡、白骨による生存、
火球などの追撃、蘇生が原因イベントを引き継ぐ。結果の `chainSummary` はタイムラインから導出する。
既存のイベント `type`、戦闘順、ダメージ式、勝敗は変更していない。

専用の `tools/test-chain-events.js` で、攻撃→死亡、攻撃→火球、死亡→蘇生、最大CHAIN、ID一意性を検証する。
陳腐化していた `tools/king_slime_smoke.js` は現行state契約へ直し、後挿入される合体シナジーにも
`eventId` があることを検証対象にした。

検証結果: 全Nodeテスト通過。`node tools/sim.js 50` は14戦略すべて完走、クリア率6〜52%。
ブラウザは既存Chrome指定で起動し、`effects` のタイムライン再生は通過した。一括回帰は既存テストが
新規ゲーム直後のモルモ全画面報告を閉じず、採用ボタンへのクリックを遮るため完走できない。
最初の試行では書き込み許可外の `.screenshots` も失敗したため、一時フォルダ指定が必要。

#### 略奪経済CHAIN・第一段階（2026-09-02完了）

ゴブリンへ既存《卑怯者》を残したまま《追い剥ぎ》を固定付与し、追加特性候補へ《強欲》を追加した。
追い剥ぎは自身が敵へ初めてダメージを与えたとき `resource_gain(gold, 1)` を生成する。
強欲はその金貨イベントへ反応し、同じCHAIN内で1回だけ威力70%の追加攻撃を行う。

戦闘タイムラインでは `attack → resource_gain → trait_trigger → attack` のCHAIN 4になる。
予約金貨は `Battle.summarizeResourceChanges()` で導出し、勝利時のみ `Game.deploy()` が所持金へ加える。
敗北時はタイムラインに発火記録を残すが、金貨は破棄する。旧セーブのゴブリンにも移行時に追い剥ぎを補う。
戦闘シーンは金貨フロートと強欲発動表示を行う。

`tools/test-loot-chain.js` で因果、追加行動、1回制限、勝利確定、敗北破棄、旧セーブ移行を検証する。
全Nodeテスト通過。`node tools/sim.js 50` は全14戦略2〜62%、ゴブリン統一44%、
ゴブリン統一＋求人22%。唯一解化や進行不能は見られない。

次の略奪経済拡張は、撃破時の《略奪者の連携》、3G到達時の《恐喝帳簿》、OVERKILL伝播。
ただし次の実装優先順位は設計書どおり、暴食・飢餓の戦闘前食料契約を先に検討してよい。

#### 暴食・飢餓CHAIN・第一段階（2026-09-02完了）

通常進行の出撃確定時に戦闘糧食を前払いし、`resource_consume(food)` から《大食漢》、
《魔界料理人》、《暴食の宴》、《飢餓の悪魔》を因果イベントとして発火させる。全軍の従来食料需要を
一度だけ計算し、出撃隊ぶんを戦闘前、残りを戦闘後へ分けるため二重計上・分割丸め増加はない。
戦闘前不足も勤務報告へ引き継ぎ、既存の忠誠低下を適用する。3日間導入プロトタイプ中は従来の日割り処理を維持する。

オーガは旧《怪力》を保ったまま《大食漢》を固定所持し、料理人の前職は《魔界料理人》を得る。
インプには《飢餓の悪魔》が候補として入る。編成画面は消費量、備蓄遷移、不足、発火見込みを表示する。
戦闘では食料消費を起点に各特性を表示し、食料4以上ならラウンド1終了時に最遅の味方が追加行動する。
《巨大厨房》は施設選択基盤と合わせて後続実装とする。

`tools/test-food-chain.js` で消費、倍率、0への遷移、因果イベント、追加行動、忠誠低下、旧セーブ移行を検証。
全Nodeテストと構文検査を通過。`node tools/sim.js 50` は14戦略すべて完走し、クリア率8〜58%。

#### 戦死・蘇生CHAIN・第一段階（2026-09-02完了）

死霊術師へ既存《死霊術》を残したまま《墓守》を固定付与し、骸骨兵の追加特性候補へ《魂の徴収》を追加した。
味方がその戦闘で初めて死亡すると、墓守が `resource_gain(soul)` を生成する。死霊術で蘇生した味方は
戦闘中アンデッド化し、蘇生イベントへ魂の徴収が反応する。魂1を消費して、生存中のアンデッド全員へ
与ダメージ×1.2を累積適用する（徴収者1体につき最大5回）。最終的に倒れたままなら従来どおり永久戦死する。

タイムラインは `death → resource_gain(soul)` と `death → revive → trait_trigger → resource_consume(soul)` を
同じ死亡イベントから分岐させる。編成画面には死亡反応・蘇生・魂消費の接続見込みを表示する。
旧セーブの死霊術師には移行時に墓守を補う。魂は戦闘内だけの状態で、恒久資源や管理画面は増やしていない。

`tools/test-death-chain.js` で因果、個体ごとの魂1回制限、蘇生時アンデッド化、累積強化、再攻撃、
旧セーブ移行を検証。全Nodeテストと構文検査を通過。`node tools/sim.js 50` は14戦略すべて完走し、
クリア率6〜52%、骸骨寄せ34%、骸骨寄せ＋求人6%。唯一解化は見られない。

全滅救済と《殉職手当》は、召喚契約と同時に混ぜず次段階で追加した（後述）。

#### 召喚契約＋墓地施設（2026-09-02完了）

`summon` 因果イベントを追加し、完全なユニットSnapを使って戦闘途中に骸骨従者を追加できるようにした。
DOMレンダラはイベント再生時に味方帯へカードを挿入し、HP・死亡・攻撃の既存表示へ登録する。
召喚ユニットは戦闘内専用ID `psN`、元の最大HP30%、攻撃50%、防御0、速度5、特性なし、アンデッド。
次ラウンドから通常の行動順へ入り、戦闘終了時に保存されない。個人戦果から除外するため、給与、忠誠、
戦功、永久戦死、出撃上限へ流れない。召喚物死亡は墓守の魂を生成しない。

施設選択基盤がない間、《墓地》は施設Lv.1以上＋建設部門に死霊術師を配置すると有効になる。
最初の味方死亡を原因に、ラウンド終了時に1戦闘1体だけ召喚する。`death → facility_trigger → summon`へ
《魂の徴収》が反応し、既存のアンデッド強化へつながる。編成画面に発火見込み、戦果に召喚数を表示する。

`tools/test-summon-chain.js` で因果、能力値、専用ID、次ラウンド行動、魂の徴収、1体上限、個人戦果除外を検証。
`tools/browser-tests/summon.js` はローカルChromeで動的カード追加と召喚後の攻撃再生を通過。
全Nodeテスト・構文検査を通過。`node tools/sim.js 50` は14戦略完走、クリア率4〜60%。
全滅時の《墓地》による救済は次段階で追加した（後述）。

#### 全滅救済＋殉職手当（2026-09-02完了）

一方が全滅した瞬間、通常の `onRoundEnd` 全体は実行せず、`rescueOnWipe` を明示した《執念》と
プレイヤー側の《墓地》だけを敗北確定前に一度解決する。1体でも復帰・召喚できれば、そのラウンドの
残り行動は打ち切って次ラウンドから再開する。救済がなければ従来どおり即時敗北。《執念》《墓地》の
1戦闘1回制限は既存フラグをそのまま使い、通常の再生や死霊術を全滅時だけ追加実行してはいない。

新シナジー《殉職手当》は《死霊術》と《追い剥ぎ》または《強欲》の同時編成で成立する。蘇生した人材が
初めて敵を撃破すると `resource_gain(gold, 2)` を予約し、戦闘終了時に本人が死亡していれば最後の
`death` を親に `resource_forfeit(gold, 2)` を生成する。資源集計は獲得から没収を差し引き、戦闘シーンも
+2Gと-2Gを別イベントとして再生する。召喚物は蘇生者ではないため対象外。手当は個体ごとに1戦闘1回。

`tools/test-rescue-martyr.js` で墓地救済、救済なしの従来敗北、執念救済、次ラウンド再行動、手当予約、
生還時維持、最終戦死時没収、因果、資源集計、1回制限を検証する。
全Nodeテストと構文検査を通過。`node tools/sim.js 50` は14戦略完走、クリア率4〜66%。
ブラウザは既存Chrome指定で `effects`（没収額表示を含む）と `summon` を通過。一括回帰は既知の
モルモ全画面報告が旧テストのクリックを遮り、`smoke` 等がタイムアウトするため完走対象外とした。

#### OVERKILL基盤＋連鎖虐殺（2026-09-02完了）

致死時の実ダメージから攻撃直前の残HPを引き、余剰ダメージと敵最大HP比を `overkill` 因果イベントへ記録する。
100%以上《蹂躙》、300%以上《粉砕》、500%以上《消滅》、1000%以上《魔王級殲滅》。未満の余剰も
通常OVERKILLとして記録する。`Battle.summarizeOverkill()` はタイムラインから回数、総余剰、最大値、
最大割合、ランク、実行者を導出する。戦果画面と個人貢献へ表示し、戦闘中は割合フロート、シェイク、
500%以上のカットインを段階的に再生する。

オーガの追加特性候補へ《連鎖虐殺》を追加。100%以上OVERKILLした余剰の30%を次の生存敵へ伝播し、
伝播先でも100%以上なら再発火する。最大3体で停止し、各 `overkill → trait_trigger → splash` は
元攻撃と同じCHAINを引き継ぐ。これにより暴食オーガの大火力が複数撃破へ接続する。

`tools/test-overkill.js` で余剰計算、境界、ランク、因果、集計、個人貢献、3体伝播上限を検証。
全Nodeテストと構文検査を通過。`node tools/sim.js 50` は14戦略すべて完走し、クリア率8〜60%。
OVERKILL報酬と魔界史の最大記録は未実装。

#### 略奪経済CHAIN・第二段階（2026-09-02完了）

《ゴブリン軍団》を単純倍率30%刻みから、15%刻み＋撃破時《略奪者の連携》1G予約へ変換した。
施設Lv.1以上かつ会計職を出撃させると試験施設《恐喝帳簿》が有効になり、予約金貨が初めて3Gへ達した
瞬間に `facility_trigger` を生成し、次の味方攻撃だけを×1.4する。施設選択UIや新資源は増やしていない。
《追い剥ぎ》《強欲》《略奪者の連携》《恐喝帳簿》は同じ金貨イベント契約を通り、勝利時だけ確定する。

Fableレビューで追加された能力公開契約に合わせ、全シナジーへ成立条件文を追加。編成画面では発動中に加え、
未発動の「組み合わせ候補」も条件・効果を公開する。隠すのは組み合わせ後の連鎖結果と壊れ方だけ。
READMEと`synergies.js`に残っていた「条件を隠す」旧方針は更新した。

`tools/test-loot-chain.js` へ撃破略奪、3G閾値、因果、次攻撃40%、1戦1回制限を追加。
全Nodeテスト・構文検査を通過。`node tools/sim.js 50` は14戦略完走、クリア率6〜54%、
ゴブリン統一36%、ゴブリン統一＋求人28%。

### 高

#### シナジー接続の観測（2026-09-02完了・KPIとtools層のみ）

CodeXと合意した方針: **種族統一ボーナスを厚くするのではなく、異なる条件をつなぐシナジーを中心にする。**
その判断材料を、試遊結果が出るまで**能力もバランスも一切変えずに**取れるようにした。

追加した観測（`src/core/kpi.js`。戦闘結果のタイムラインから導出するだけで、
戦闘計算にもラン状態にも触らない。したがって再起で巻き戻しても減らない）:

| 項目 | 意味 |
|---|---|
| `triggerKinds` | 連鎖に参加した能力の種類と回数（特性・施設・シナジー・事件を別々に） |
| `chainMax` | ランの最大CHAIN（深さ） |
| `chainAbilityMax` | 代表CHAIN（最深経路）を構成した**異なる能力の数** |
| `chainSample` | いちばん条件をまたいだ代表CHAINの中身（能力名の並び） |
| `buildAttempts` | ビルド試行数（既存） |
| `lastScreen` | 停止画面（既存。レポート側で「どこで止まったか」の読みを添えた） |

**深さ（`chainMax`）と種類数（`chainAbilityMax`）は別物として扱うこと。**
同じ能力が何段続いても種類は増えない。見たいのは「1本の連鎖が何種類の条件をまたいだか」で、
これが低いままなら足りないのは能力の数ではなく**異なる条件をつなぐ橋**である。

読み方:

```
node tools/sim.js 30 --kpi /tmp/kpi.json   # 試遊を待たずに経路を通すとき（数値は参考値）
node tools/kpi-report.js /tmp/kpi.json     # 実機は copy(KPI.export()) を保存して渡す
```

AI操作の30ラン計測では **トリガー種類 8.3種/ラン・最大CHAIN 3.7・代表CHAINの能力数 2.8**。
判定は「トリガーは多いが連鎖が同じ能力で閉じている」。人間の試遊でも同じ形なら、
CodeX側の能力追加は「新しい種族条件」より **既存の発火を別の資源・事件へ渡す能力** を優先する。

**CodeXへ**: `src/data/` の能力追加はそのまま任せる（このコミットは触っていない）。
新しい能力に因果メタデータ（`parentEventId` / `chainId` / `chainDepth`）を付けておけば、
この観測に自動的に載る。付け忘れると「発火していない」ように見えるので注意。

**続報（2026-09-02・実測）: 「届かない」も誤りだった。届くが、届くと負ける。**

下の「届かないだけ」は、**引き直しを一度も使わないAI**で測った数字だった（3体8% / 5体0%）。
狙って引き直すプレイで測り直すと、現状の価格のままで **3体以上58% / 5体18%**。手段はある。

さらに求人の数値を振って測った結果（各200ラン・ゴブリンを狙う play）:

| 設定 | 3体以上 | 5体 | クリア率 |
|---|---|---|---|
| 現状（無料1回・2G倍々） | 58% | 18% | **27%** |
| 無料2回 | 70% | 28% | 16% |
| 基本1G | 70% | 27% | 21% |
| 常に2G固定 | 70% | 36% | **14%** |

**引き直しを安くするほどクリア率が下がる。** 金貨が引き直しへ吸われ、給与・食料・施設に回らないため。

そして決定的な数字（400ラン・第5戦時点のゴブリン数別のクリア率）:

| ゴブリン数 | 1体 | 2体 | 3体 | 4体 | **5体** |
|---|---|---|---|---|---|
| ラン数 | 25 | 54 | 64 | 75 | **158** |
| クリア率 | 40% | 30% | 34% | 37% | **12%** |

**ビルドを完成させたランが最も負ける。** 理由は純ゴブリン5の段階別勝率（S6 100% / S7 12% / S8 0%）と
一致する。中盤は無双するが終盤で構造的に詰み、そこへ引き直しの出費が重なる。
《強欲》持ちは5体中平均1.23体しか揃わないので、連鎖も薄いまま。

**結論: いま足りないのは「引き当ての手段」ではなく「中盤の無双を終盤へ橋渡しする出口」。**
稼いだ金貨（ゴブリンは1ラン14.9G略奪する）が終盤の壁に対して何もしないのが本体。
→ ロードマップ⑥「施設の大型Joker化」＝金貨を戦力へ変える出口が、次の本命。
**求人の価格を下げてはいけない**（壊れビルドへの道を整備すると、より確実に詰む）。

**（旧）発見（2026-09-02・実測）: 壊れ方は設計に入っていて動く。届かないだけ。**

オーナーの問い「根本的にビルドによるシナジーでぶっ壊れ感は出せるのか」を測った。

手で極端に寄せた出撃隊を最終戦（勇者アレン一行）へぶつけた結果（各200戦）:

| 出撃隊 | 最終戦勝率 | ラウンド | 最大CHAIN |
|---|---|---|---|
| 普通の強い5体（シナジー無し） | 51% | 7.2 | 2.00 |
| アンデッド全振り（死の軍勢） | 100% | 1.9 | 2.00 |
| 魔法職全振り（魔法結社） | 100% | **1.0** | 2.98 |
| ゴブリン経済全振り（追い剥ぎ+強欲×5） | 100% | 1.8 | **6.28** |
| 精鋭3体 | 100% | 2.0 | 2.00 |

**揃えば壊れる。** 魔法職全振りは初手で敵を全滅させ、ゴブリン経済はCHAIN6.28まで芋づるが伸びる。

ところが実プレイでは揃わない。ゴブリンを狙って集めるプレイ（応募に居れば必ず採用・出撃も優先）を
200ラン再現した、出撃隊のゴブリン数の分布:

| 0体 | 1体 | 2体 | 3体 | 4体 | 5体 |
|---|---|---|---|---|---|
| 42% | 30% | 17% | **8%** | 2% | **0%** |

《ゴブリン軍団》は3体から。**狙っていても10戦に1回しか発動しない。**
《強欲》持ちに至っては0体が77%、3体以上は0%。上表の「壊れた編成」の到達率は実質0%。

**したがって問題は「シナジーの効果が弱い」ではなく「狙った材料を引き当てる手段が無い」。**
（2026-09-02より前の私の診断「効果が弱いので出口を作る」は誤り。記録として残す。）

**重要な帰結: 新種族を先に足してはいけない。** 種族が増えるほど応募の当たりが薄まり、
3体揃う確率はさらに下がる。ロードマップ⑦は⑧以降へ落とす。
先にやるべきは採用の引き当て（引き直しの価格・回数、種族や役割を狙って探す手段、
特性の付け替え、抱えた20体から出撃5体を組み替える自由度）。
現行は「1ターン1名／応募3名ランダム／引き直し2G→4G→8G」で、
Balatroで言えばショップでリロールも選択もできない状態にあたる。

**次に遊ぶときに見てほしいこと（2026-09-02夜の実装ぶん）**

オーナーの就寝中に、測って直したものが4つある。実プレイで見るべき点を先に書いておく。

1. **傭兵市場**（編成画面・給与方針の下）。金貨で「その戦闘だけの6体目」を雇える。
   同族なら顔なじみ価格で安い。→ *毎回とりあえず雇うだけになっていないか*。
   判断として面白くなければ、値段ではなく「雇える回数」や「候補の出方」を疑うこと
   （値段のレバーは3種類とも測って効かなかった。同じ道を再試行しない）。
2. **キングスライム合体を断れる**。既定は従来どおり合体する。
   → *断る判断が一度でも起きるか*。起きないなら、合体しない側の利点（頭数）が薄い。
3. **編成画面のシナジー表示**が「いま ×1.15」「オーガをゴブリンに替えると ×1.30」まで出す。
   《群れの本能》のような特性も「いまの並びで効いている特性」として出る。
   → *これを見て編成の判断が変わるか*。変わらないなら、置き場所か言葉がまだ足りない。
4. **KPIが上の判断を全部記録する**。遊んだあと DevTools で `copy(KPI.export())` して
   JSONを保存し、`node tools/kpi-report.js kpi.json`。
   「金貨の出口」の節に、雇った傭兵・うち同族・払った金貨・合体を断った回数が出る。

**次は実プレイ期間（2026-09-02時点のロードマップ）。**
KPI測定契約まで実装が終わった。順番はオーナー確認済みで、
①KPIイベント契約（完了）→ **②実機で5〜10ラン遊ぶ → ③仮説試行回数と停止箇所を確認**
→ ④戦闘・モルモのテンポ調整 → ⑤敵をビルドへの「問い」にする → ⑥施設を大型Joker化
→ ⑦必要な役割を埋める新種族を1〜2種。
②③はコードではなく計測なので、実プレイの結果（`node tools/kpi-report.js`）が出るまで
④以降へ進まない。特に**新種族（⑦）を先に足さない**こと。
「種族が足りない」のか「既存種族を試せる回数が少ない」のかは、ビルド試行÷戦闘数で切り分ける。

**P3（部門・食料・施設から事件を起こす）の最小実装は完了。**
拡張コアループの最小実装は完了した。戦闘・建設・生活を別ゲームに分けず、既存の出撃選抜を
「誰を今日どこで働かせるか」へ拡張している。モルモの勤務報告は全画面化済み。
次は数値を細かく増やす前に、追加した3事件を実プレイで確認し、配属を変えたくなるかを見る。
設計思想の正本は `GAME_DESIGN_PRINCIPLES.md` だけとし、この引き継ぎには重複転載しない。

-2. **モルモ報告を自動で閉じない**（完了）。台詞を読み終えても報告を残し、1回目の入力で
    全文表示、2回目の明示入力で閉じる。マウスはボタンだけを受け、画面余白の誤操作を防ぐ。

-1. **通常曲と新しい戦闘曲の実機試聴待ち**（2026-09-01）。
    オーナーの「戦闘まで同じBGMで単調」を受け、戦闘専用のCC0実曲へ切り替える実装を追加した。
    音量・ループ継ぎ目・通常曲へ戻るタイミングを実機で確認してもらう。
    以前の聞こえやすさ対応として、
    「まだ小さい」を受けて、音量ではなく音域（足音・行進ベースの基音を
    スマホで再生される帯域へ上げ、倍音を重ねた）とユニゾンで質感を変える対応を
    入れたが、**この修正自体は実機で確認できていない**。詳細は
    「BGM『魔王軍の行進曲』」節の追記と `docs/BGM_DESIGN.md` 3-2節。
    まだ「小さい」と言われたら、**数値をまた機械的に上げるのではなく**
    「音量か・特定の場面だけか・質感（安っぽさ）の話か」を切り分けてから動く。

0. **音声パネルと解雇ボタンの重なり（完了）**。`position: fixed` の固定オーバーレイをやめ、
   `#app` の後ろにある通常フローの操作欄へ移した。応募者カードの高さに関係なくゲーム操作を
   覆わない。音量スライダーとBGMボタンがフォーカス時だけ開く挙動は維持。

1. **魔法結社が突出** — 第8戦の勝率が 魔法結社×5 で73%、他ビルドは4〜28%。
   壊れビルドの発見は設計上むしろ望ましいが、差が大きすぎる可能性。
   実際に遊んだ感触での判断が要る。

### 設計思想レビュー（2026-09-01・P1〜P6すべて完了）

`GAME_DESIGN_PRINCIPLES.md` の統合版と実装を突き合わせた結果。P1（部門適性）、P2（給与方針）、
P3（部門・食料・施設由来の事件）は完了。
**上から順に着手する。**

- **P3（完了）. 部門・食料・施設由来の3事件を追加**（第9・12節）。配置と勤務結果が
  次の経営判断へつながる最小循環を実装した。追加量より実プレイでの因果の伝わり方を先に確認する
- **P4（完了）. 敵編成の揺らぎ**。第1〜第7段階に基本＋代替隊列を各1つ追加。人数・前衛耐久・
  速度配分を変え、総合力は基本の0.93〜1.11倍。編成名と特徴は出撃前に開示し、作戦会議へ
  戻っても再抽選しない。最終戦は勇者アレン一行を固定した。50ラン×14戦略ではクリア率
  2〜66%で、0%・100%固定や進行不能なし（三部門均衡50%、未払い搾取36%）
- **P5（完了）. 殿堂入りと過去人材の再応募**（第10・11節）。各代の功労者を魔界史へ保存し、
  次代以降に低確率で同じ名前・履歴書の人材が再応募する。能力・戦功・階級は引き継がず、
  「攻撃力+1%」型の恒久強化にせず歴史が戻ること自体を報酬にした
- **P6（完了）. 音声パネルを通常フローへ移し、解雇ボタンを覆わないようにした**

### 中

**給与抗議→ストライキ行進（完了）**: 同じ人材への未払い2回で通常の賃上げ要求から抗議へ格上げ。
6G清算、給与+2G協約、無視を選べ、無視した場合だけ後日行進になる。行進は8G支給、代表を
生活部門の労務担当へ異動、強硬鎮圧の三択。`tools/test-labor-events.js` でチェーン・旧セーブを検証。

2. **スライム合体の選択化は完了** — 旧「スライム5体が罠」は再測定で否定済み。
   合体を断る判断も実装済み（第4節参照）。旧勝率を根拠に再調整しない。
3. **骸骨兵だけではシナジーが無い**（死の軍勢は死霊術師が必須）。
   アンデッド単独のシナジーを足すか、意図的にそうしておくかは設計判断。
4. **敵編成の揺らぎは最小実装完了** — 第1〜第7段階に2隊列ずつ。実プレイで、敵情を見て
   出撃順や採用方針を変えたくなるか確認し、必要なら各段階3隊列目を足す。
5. **昇進時の姿の変化** — 能力・特殊能力・UI上の階級章は実装済み。将軍専用の立ち絵変化は未着手。
6. **敵の開戦台詞は最小実装完了** — 汎用 `introQuote` 契約を追加し、勇者アレンの開戦台詞を実装。
   実プレイで尺を確認するまで全敵への大量追加はしない。
### 低

7. 純粋な単一種族が終盤でまだ苦しい（ゴブリン×5は第8戦6%）
8. 敵の戦闘絵は全10役・6ポーズ対応済み。追加量産ではなく、実戦の判読性を評価する。
9. BGMの音源を暫定合成から実録ループへ差し替える（`Music.apply()` の中身だけ）。
   敵側のレイヤー（最終決戦で勇者アレンのテーマが混ざる）、
   魔界史に「その代の軍団の曲」を残して聴き比べる、も候補。
10. 当初構想の未着手分: 装備

---

## 6. テストの走らせ方

### バランス検証（ブラウザ不要・速い）

```bash
node tools/sim.js 50
```

各戦略の行の直後に「施設到達: Lv1以上 xx%（Lv3 yy%）／選択 …／拠点接収 N回」と
「ビルド名: N種/Mラン　多い順 …」が出る。到達率が0の戦略で施設の効果を語らないこと。

ビルド名の行は**ランごとの差が出ているかの物差し**として読む。1種類の名前が半分以上を
占めていたら、その戦略は毎回同じことをしている（または修飾の優先順位が珍しさを表していない）。

14種類の採用・作戦・部門・給与戦略で通常はそれぞれ50ランを回し、クリア率・敗北ステージ・
施設Lv・食料不足・給与方針・シナジー出現数・再起/求人/事件の回数を出す。**データを触ったら必ず流すこと。**
オーナー判断（2026-09-01）により、多少の変なバランスは本作の味として許容し、通常変更で
200ランの厳密さは求めない。戦闘式や経済式を大きく変えた場合だけ100〜200へ増やす。

昇進実装後の想定クリア率（再起1回を使う前提）は、通常ビルドがおおむね10〜70%。
略奪4回ルートは約30%、慎重経営は約45〜55%。古参が育つぶん以前より高いが、
寄り道と育成が報われることを優先した意図的な変化。80〜90%が常態化したら将軍量産を疑う。

軍団と出撃隊の状態契約だけを素早く検証する場合:

```bash
node tools/test-army.js
```

魔界史のビルド名（何が名前になるか・珍しさの順序）を検証する場合:

```bash
node tools/test-build-name.js
```

三部門の給与・食料・建設・施設補正・旧セーブ移行を検証する場合:

```bash
node tools/test-departments.js
```

通常支給・意図的未払い・前払い厚遇の契約を検証する場合:

```bash
node tools/test-payroll.js
```

昇進の閾値・能力上昇・将軍の号令を検証する場合:

```bash
node tools/test-promotions.js
```

戦闘中ハプニングを決定的乱数で検証する場合:

```bash
node tools/test-battle-happenings.js
```

効果音キュー・音量保存・ミュートをブラウザ無しで検証する場合:

```bash
node tools/test-sound.js
```

BGMが軍団の状態を正しく演奏へ翻訳しているか検証する場合:

```bash
node tools/test-music.js
```

尺が事件の大きさに比例しているか（保護対象・圧縮対象・延長規則）を検証する場合:

```bash
node tools/test-battle-pacing.js
```

代表CHAIN経路と非ダメージ貢献の帰属を検証する場合:

```bash
node tools/test-battle-report.js
```

施設の発火要約・全滅回避判定・死者ごとの連鎖と、戦果の `facility` 契約を検証する場合:

```bash
node tools/test-facility-report.js
```

ラン全体の主要記録（最大CHAIN・最大OVERKILL）と魔界史保存を検証する場合:

```bash
node tools/test-run-records.js
```

KPI測定契約（ビルド試行・リトライ・テンポ・離脱箇所）を検証する場合:

```bash
node tools/test-kpi.js
```

編成画面のシナジー予告（実効倍率・入れ替え案内）を検証する場合:

```bash
node tools/test-synergy-preview.js
```

傭兵市場（金貨の出口）の状態契約を検証する場合:

```bash
node tools/test-mercenary.js
```

### ブラウザ回帰テスト（26本）

```bash
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install --no-save playwright   # 初回のみ
sh tools/browser-tests/run-all.sh
```

`run-all.sh` は `/opt/pw-browsers/chromium-*/chrome-linux/chrome` を自動で探す。
別の場所にある場合だけ `CHROME` で指定する:

```bash
CHROME=/path/to/chrome sh tools/browser-tests/run-all.sh
```

**npm が入れた playwright と同梱ブラウザの版が食い違うと**
「Please run `npx playwright install`」で全滅する。`npx playwright install` は
走らせず（帯域も容量も食う）、`CHROME` で既存のバイナリを指すこと。

落ちたテストは `✗ FAILED` と落ちた行を表示し、`run-all.sh` 自体も 1 で終了する。
最後に `✓ 全テスト通過` が出たときだけ通ったと見なしてよい。

**モルモの全画面報告は自動で閉じない。** 報告が出ている間、下の画面のボタンは覆われていて
クリックできない（実プレイでは人が送るので問題にならない）。新しいテストを書くときは
`tools/browser-tests/helpers.js` の `autoDismissMormo(page)` を `goto` の前に呼ぶこと。
報告そのものを見たいときだけ `dismissMormo` / `silenceMormoFromNow` を使う。
開幕3日間プロトタイプより後（作戦会議以降）を見るテストは `enterMissionPhase(page)` を通す。

個別に走らせるときは `GAME` にリポジトリの絶対パスを渡す:

```bash
GAME=$(pwd) CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome \
  node tools/browser-tests/smoke.js
```

スクリーンショットは `.screenshots/` に出る（`.gitignore` 済み）。

| テスト | 見ているもの |
|---|---|
| `smoke` | モルモ表示、タイトル→採用→三部門・給与方針→出撃→勤務報告→セーブ復元の一周 |
| `autoplay` | ラン完走×3、魔界史の蓄積、決着後にセーブが消えること |
| `mission` | 作戦3択・収支表示・略奪後の警戒度と攻略進行 |
| `retry` | 再起の巻き戻し・所持金半減・回数制限・セーブの生死 |
| `softlock` | 部隊0体で詰まないこと |
| `reroll` | 求人の費用が倍々に増えること、採用枠を消費しないこと |
| `eventui` | ハプニングの表示と選択、採用フェーズへの復帰 |
| `contrib` | 戦果パネルの数値とバー |
| `nearmiss` | 敗北時の最も追い詰めた敵HP・割合・最後の出来事 |
| `portrait` | 立ち絵の表示と絵文字フォールバック（`MODE=none/present/broken`） |
| `tier0` | 決着表示の重なり、解雇ボタンの間隔、ヒント文言 |
| `scene`/`cutin` | 戦闘演出とシナジーのカットイン |
| `effects` | ラウンド区切り、攻撃者表示、最終決戦、観戦テンポ |
| `report` | 戦果画面の主要記録2つ・代表CHAIN経路・非ダメージバッジ（クリック非依存） |
| `records` | 終了画面と魔界史カードの最大CHAIN・最大OVERKILL表示（クリック非依存） |
| `kpi` | 速度変更・スキップ・モルモ早送り・画面記録が端末内へ残ること（クリック非依存） |
| `synergy` | 編成画面の実効倍率・入れ替え案内・「あと○体」（クリック非依存） |
| `spectacle` | 見せ場の演出：帯と全画面の出し分け・積み上がるCHAIN・締め・伝播の稲妻・熱・倍速・スキップ・低モーション |
| `synergy-pool` | 発火条件を出撃枠の外まで数えること・効果は出撃者だけ・重ねがけ・予告と本番の一致 |
| `brief` | 指名求人の中盤解禁・倍々の求人費・条件に寄るが確定でない・面接をまたがない |
| `feast` | 宴の成立条件・大食漢と料理人の倍率・アンデッド不成立・備蓄上限の腐敗・飢餓3戦→飢餓適応 |
| `mercenary` | 傭兵市場の雇用・出撃・戦果表示・契約終了（クリック非依存） |
| `kingslime` | キングスライム合体の既定と、編成画面で断れること（クリック非依存） |
| `pacing` | 事件は縮めず通常攻撃だけを圧縮すること、個別倍率でも最後まで再生できること |
| `sound` | 最初の操作での音声解禁、音量・ミュート保存 |
| `music` | BGMの演奏開始、編成・場面への追従、未払いの反映、オンオフ保存 |
| `mormo` | 全画面報告、タイプ表示、発話音、タップ／キー操作、作戦から編成への遷移 |
| `clear`/`resume` | 全クリア画面、履歴書欄 |
| `casualty` | 戦死者の永久退場、欠員募集、蘇生した者が残ること |

### 分析用スクリプト（使い捨て、必要なときだけ）

`tools/analysis-*.js` に、実際に設計判断へ使った測定を残してある。

| ファイル | 測るもの |
|---|---|
| `analysis-casualty.js` | 勝利時の戦死数の分布 |
| `analysis-racecheck.js` | 純粋種族軍 vs 混成軍の勝率を直接比較 |
| `analysis-loyalty.js` | 未払いの発生率と離脱率 |
| `analysis-duration.js` | 戦闘演出の実尺と、尺のうち「縮めない事件」が占める割合（ステージ別） |
| `kpi-report.js` | 実機のKPI（`copy(KPI.export())` で書き出したJSON）を読む。分析画面の代わり。ビルド試行・シナジー接続（トリガー種類／最大CHAIN／代表CHAINの能力数）・停止画面 |
| `analysis-events.js` | ハプニング16種の出現と解決の検証 |
| `test-labor-events.js` | 給与抗議→ストライキ行進の予約・解決・配属変更・旧セーブ移行 |
| `test-enemy-formations.js` | 敵の代替隊列、戦力帯、事前開示、戻る操作で再抽選されないこと |
| `analysis-voices.js` | 戦闘後の一言が状況どおり選ばれるか |
| `analysis-shape.js` | 戦闘のラウンド数・ログ行数 |

### 勇者襲来の縦切り試作（`hero-arrival.html`）

本編とは独立している。本編のデータを変えても壊れないし、逆に試作を変えても本編は動く。

```bash
node tools/test-hero-arrival.js      # 世界ロジックと境界（ブラウザ不要・速い）
node tools/sim-hero-arrival.js 300   # 迎撃の数値校正。編成・育成・酔いで差が出るか
CHROME=/opt/pw-browsers/chromium-1194/chrome-linux/chrome \
  GAME="$(pwd)" SP="$(pwd)/.screenshots" node tools/browser-tests/hero-arrival.js
```

ブラウザテストは実機で B を一周し、発見の一言・敵情への接続・台帳を見たうえで、
**本編の localStorage が試作の前後で1文字も変わらないこと**を差分で確認する。
試作の数値（`HERO_PARTY` / 人物の `base`）を触ったら `sim-hero-arrival.js` を流し直すこと。
本編の `tools/sim.js` は試作とは無関係（試作は `src/data/` を読んでいない）。

---

## 7. オーナーの意向として覚えておくべきこと

過去のやり取りで繰り返し出てきた方向性。

- **「実装できるか」より「もう一回遊びたくなるか」を優先する**
- **恒久成長を「攻撃力+1%」にしない**。増やすのは
  選択肢・組み合わせ・発見・歴史であって、強さそのものではない
- **くすっと笑える世界観を守る**。プレイヤーの意識が効率化に寄りすぎると
  「数字だけのゲーム」になる。履歴書・戦闘後の一言・ハプニングは全部この対策
- **鬼畜・ハプニング寄りのネタを歓迎**（仲間割れ、見せしめ、引き抜き、賭博 等）
- BGMは実装済み（軍団が演奏する方式）。音源の差し替えとSFX候補の採用は、
  オーナーが試聴してから決める
- 画像は CodeX 側で生成する。仕様は README の「絵を発注するときの仕様」を参照
- 変更したら**小さい単位でコミットし、何を・なぜ・どう確認したか・残る問題**を報告する
