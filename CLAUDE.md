# このリポジトリで作業するときの最上位ルール

## 何よりも先に `DESIGN_PRINCIPLES.md` を読むこと

**`DESIGN_PRINCIPLES.md` がこのプロジェクトの最上位の設計指針である。**

個別の仕様、UI、数値、キャラクター数、演出、そしてこのファイルの内容よりも、
`DESIGN_PRINCIPLES.md` の考え方を優先する。実装判断で迷ったら必ずそこへ戻る。

そこに書かれている最重要KPIはただひとつ:

> **「もう1回遊びたいか」**

## 実装前に必ず通す8つの問い

`DESIGN_PRINCIPLES.md` 第15節より。新機能を実装する前に必ず自問し、
**YESが少ないなら、その機能は作らずに再検討する。**

1. これはプレイヤーに新しい判断を与えるか？
2. 既存要素との新しい組み合わせが生まれるか？
3. プレイヤーが発見したと感じられるか？
4. プレイごとの差が増えるか？
5. もう1回遊びたくなる理由になるか？
6. 人に話したくなる事件が起こるか？
7. 画面や動画を見ただけでも魅力が伝わるか？
8. 単純に作業量を増やしているだけではないか？

## 特に踏み外しやすい判断

- **実装が簡単だからという理由だけで、ゲーム性の弱い機能を優先しない。**
- **大量の機能を実装することを進捗と考えない。**
- 新要素は「強いか」ではなく **「既存の何と組み合わさると面白いか」** で評価する。
  コンテンツ数より **既存要素との接続数**（`DESIGN_PRINCIPLES.md` 第4節）。
- 恒久成長を「攻撃力+1%」のような単純な数値上昇にしない。
  増やすのは **選択肢・組み合わせ・発見・歴史**（第9節）。
- バランスを取りすぎない。プレイヤーが「これ、強すぎない？」と思う瞬間を
  意図的に作る（第5節）。ただし毎回同じ壊れ方はさせない。
- 敗北を単なる損失にしない。**笑える／悔しい／人に話したくなる**敗北にする（第7節）。
- くすっと笑える世界観を守る。数字だけのゲームになりかけていないか常に見る（第11・12節）。

迷った場合は、**よりシンプルで、より意外性があり、より組み合わせが増え、
よりプレイヤー自身の発見が発生する方**を選ぶ。

---

## 作業の進め方

- 続きから入る場合は **`HANDOFF.md`** を読む。実装状況・壊してはいけない設計の約束・
  過去に踏んだ落とし穴・優先度つきバックログ・テストの走らせ方が書いてある。
- ゲームの現在の仕様は **`README.md`**。
- データを触ったら **必ず `node tools/sim.js 200`** でバランスを測る。
  クリア率がおおむね5〜50%の帯から大きく外れたら何かを壊している。
- UIやフローを触ったら **`sh tools/browser-tests/run-all.sh`** を通す。
- 変更は小さい単位でコミットし、**何を・なぜ・どう確認したか・残っている問題**を報告する。
- 画像まわりは `AGENTS.md` と `.codex/skills/demon-army-monster-art/SKILL.md` に従う。

---

## Claude と CodeX の分担ルール

この2つのAIは**お互いのセッションが見えない**。人間（オーナー）が唯一の調整役である。
そのため、以下を守らないと静かに壊れる。

### 層で分ける（機能では分けない）

| 層 | 主担当 | 理由 |
|---|---|---|
| `assets/` `src/data/` | **CodeX** | 絵の生成パイプラインを持つ。データは末尾追記なので衝突しにくい |
| `src/core/` `tools/` | **Claude** | 変更のたびに `tools/sim.js` で測って検証する必要がある |
| `src/ui/` | どちらでも | ただし同時着手は避ける |

役割の言い換え:
**CodeX＝増やす**（絵・モンスター・台詞・イベント・敵データ）/
**Claude＝繋ぐ・測る・直す**（エンジン、相互作用、バランス検証、テスト）。

これは `DESIGN_PRINCIPLES.md` 第19節「大量生成 → 選別 → 調整」の分業に対応する。

### 専有ではなく優先権

どちらかが利用上限に達しても開発が止まらないよう、担当外の層も触ってよい。
ただし以下は必ず守ること。

1. **着手前に必ず `git pull`**（怠るとpushが弾かれる）
2. **1タスク＝1コミット＝即push**。作りかけを手元に置かない
3. **`src/core/run.js` と `src/core/battle.js` は同時に触らない**。最も衝突しやすい
4. **数値バランスを変えたら `node tools/sim.js 200` の結果をコミットメッセージに書く**。
   相手がそれを見て判断できるようにする
5. **契約（`HANDOFF.md` 第2節）を変えたらコミットメッセージで宣言し、HANDOFFも更新する**

### 本当の危険はマージ衝突ではない

gitは大抵きれいにマージする。危険なのは**意味の衝突**である。

- 片方がバランスを測っている最中に、もう片方が数値を変える → 測定結果が嘘になる
- 片方が `contribution` などの構造を変える → その上に作られたUIが静かに壊れる

テキストとしては綺麗にマージされるのに、ゲームが壊れる。だから 4. と 5. が重要。

### 引き継ぎの作法

作業を終えたら `HANDOFF.md` の「次にやること」と「バックログ」を更新する。
相手が**文脈ゼロで拾える**状態にしておくこと。
設計アイデアを出してもらった場合も、採用したものはバックログに落とす。
