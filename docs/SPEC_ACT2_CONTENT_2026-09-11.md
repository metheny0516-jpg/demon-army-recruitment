# 仕様：第二幕の中身——新種族3・上位技6・敵段階9〜14（データのみ）（2026-09-11）

実装担当：Opus。設計は `docs/DESIGN_ACT2_2026-09-11.md`。この文書と `HANDOFF.md` §0・§2 だけで始められる。
**CodeX が `src/ui/*` を大きく組み替え中。`src/ui/*`・`src/core/*` は触らない。** データとテストだけ。
迷ったら末尾「未決」を見て、無ければ既存の同種のデータをいちばん近く写す。

## 0. 一文で

第二幕で増える中身を、**今の幕では出ないように**データだけ先に入れる。応募には混ざらず、敵は段階9以降なので今のランでは到達しない。幕の進行（run.js）は別仕様。

## 1. 触ってよいファイル

- `src/data/monsters.js` — 新種族3（`tier: 4`。**`rollApplicant` は tier 4 を今は引かないことを確認して、引くなら tier の重みで 0 にするのではなく、テンプレートに `act: 2` を足して `MONSTER_TEMPLATES` の絞り込みで除く。絞り込みは run.js なので、確認だけして HANDOFF に書き、除外は幕の進行の仕様に回す。今の段階で応募に混ざるなら tier: 4 を `MONSTER_RULES.maxTier` 的な既存の仕組みで止められないか調べ、無ければ `act: 2` だけ付けて報告**）。
- `src/data/traits.js` — 1段目の技3（新種族用）と上位技3（`skill: { species, tier: 2, replaces }`、`order`、`autoLimit: 1`、`lines.unlock/use/order`）。既存の癖・共通特性は触らない。
- `src/data/enemies.js` — 段階9〜14の6行（`variants` 各1）。段階14は「勇者一行（再）」（新メンバー「竜騎士カイ」）。
- `src/data/events.js` — 新種族に関わる事件2つ（既存の形。cast は種族で絞る）。
- `tools/test-act2-content.js`（新規）。`HANDOFF.md` §0 に5行。

触らない：`src/core/*`、`src/ui/*`、`tools/sim.js`、browser-tests、`assets/`（絵はオーナー。無い間はフォールバック）。

## 2. 新種族（3）

| id | 種族 | tier | tags | 役 | 1段目の技 | 上位技（置き換え） |
|---|---|---|---|---|---|---|
| `succubus` | サキュバス | 4 | ["caster"] | 魅惑的な女性。敵を惑わせ、味方を鼓舞する後衛（オーナー：「サキュバスみたいな魅惑的な女性キャラに」） | `allure`「誘惑」：攻撃した敵の攻撃力−2（悪戯の強い版。0 未満にしない） | `enthrall`「魅了」：攻撃した敵が、立っている別の敵1体を本来の70%で殴る（`postAttack` で `ctx.dealRaw(ctx.target, other, …)`。自動は1戦闘1回、号令なら相手の攻撃力に関係なく必ず）。号令の見出し「魅了せよ」 |
| `minotaur` | ミノタウロス | 4 | [] | 重い前衛。突進 | `charge`「突進」：ラウンド1の攻撃で相手を後ろへ下げる（`moveEnemyBack` の形） | `rampage`「暴走」：HPが半分以下のとき与ダメ+50%、被ダメ+20% |
| `lich` | リッチ | 4 | ["undead","caster"] | 死霊の上位。術 | `soul_drain`「魂吸い」：与ダメの30%を回復 | `death_pulse`「死の波動」：3ラウンド目に敵全体へ攻撃の60%（1戦闘1回） |

- 数値は既存の tier 3（オーガ 45/14/6/4、魔法使い、死霊術師）を**少し上回る**程度（第二幕の応募は魔王軍レベルで伸びるので、基礎は控えめ）。
- `fixedTraits` に1段目の技、`traitPool` は既存の癖から3つ（非戦闘特性を増やさない。**共通特性・号令できる技以外は入れない**）。
- 履歴書：`jobs` 5、`prevJobs` 8、`motives` 8、`flaws` 8、`voices` は既存と同じキー（dead/mvp/idle/hurt/unpaid/win/lose/hungry/feast/chain）各3本以上。**数値を言わない、全角28文字以内は台詞（lines）だけの縛り。履歴書の文は自由。**
- 技のフックは既存のものだけ（`modDealt` / `modTaken` / `postAttack` / `onRoundEnd` / `onLethal`）。**新しいフックは作らない**（後列を狙う・行動順を変えるは `postAttack` / `onRoundEnd` の中でできる範囲で。できなければ未決に書いて簡略化：急降下は「ラウンド1〜2 +30%」でよい）。

## 3. 敵段階 9〜14

| 段階 | 軍 | 地域 | 隊列の癖（variants） |
|---|---|---|---|
| 9 | 国境警備隊 | 隣国国境 | 大盾2＋弩1 |
| 10 | 隣国重装歩兵 | 街道の砦 | 硬い前衛3 |
| 11 | 聖教会審問団 | 大聖堂 | 審問官（攻高・脆い）＋聖歌隊（回復役の代わりに高HP） |
| 12 | 竜騎兵団 | 高地 | 速い2＋重い1 |
| 13 | 連合軍砲兵陣地 | 連合本陣前 | 砲兵（攻撃極高・防0）2＋護衛2 |
| 14 | 勇者アレン一行（再） | 連合本陣 | 勇者（`hero_awaken`）・戦士・聖女・賢者・**竜騎士カイ**（新） |

数値は段階8の勇者一行を基準に、段階9は段階8の**通常兵**より少し上、段階14は段階8の勇者一行の1.3倍程度。`reward` は段階8から続けて伸ばす。

## 4. 事件（2）

既存の形（`check` / `cast` / `text` / `options`）。cast は新種族で絞る（`EV_PICK` の第2引数は「好み」で絞り込みではない——`check` で種族の在籍を見る）。
- サキュバス：「噂」（城下に噂が広まり、次の面接の応募者+1 か 忠誠が揺れる。留守番のときだけ）。
- ミノタウロス：「扉」（狭い場所に入れず、施設の建設が1回遅れるか、壁を壊して建材+）。

## 5. テスト `tools/test-act2-content.js`

- 3種族の形（id・race・tier 4・jobs/prevJobs/motives/flaws の本数・voices のキー）。
- 6技：`skill.species` がテンプレートの id と一致（`wizard`≠`mage` の事故の再発防止）、`replaces` が 1段目の id、`order.cost` 1〜3、`lines` 各3本・28文字以内・数字なし、`autoLimit: 1`。
- 技が battle.js で落ちない：`Battle.simulate` を各技持ちで1回回す（vm。`tools/test-skills-battle.js` を写す）。
- 段階9〜14の形（`stage` 連番、`units` 2〜5、variants 1以上、reward 単調増加）。
- **今の幕で出ない**：`Game.rollApplicant()` を200回引いて tier 4 が混ざらない（混ざるなら報告し、テストは「混ざる」を記録して通す。除外は幕の進行で）。`ENEMY_STAGES.length` が伸びたことで `MAX_CONQUEST` が 14 になる → **これは第一幕の着地を壊す**ので、`MAX_CONQUEST` を `ENEMY_STAGES.length` から切り離す必要がある。run.js を触れないので、`enemies.js` に `ACT_STAGE_CAP = { 1: 8, 2: 14 }` を置き、HANDOFF に「run.js の MAX_CONQUEST を ACT_STAGE_CAP[1] に固定するのは幕の進行の仕様で」と書く。**このコミットの時点で第一幕が段階9へ進めてしまうなら、段階9〜14は `ENEMY_STAGES_ACT2` という別配列に置く**（既定：別配列。安全側）。
- 既存 node テスト全通過。sim 1回（煙感知器：0%の戦略なし・戦闘数の急減なし）。

## 6. 未決（既定）

- U1 新種族の絵が無い間の表示。既定：既存のフォールバック。
- U2 段階9〜14の置き場。既定：`ENEMY_STAGES_ACT2`（別配列）。
- U3 突進の「押し下げ」・魅了の「同士討ち」が既存フックで書けないとき。既定：数値の技に簡略化して未決に残す（魅了は `dealRaw` で書けるはず）。
- U4 サキュバスの履歴書（前職・志望動機・欠点・台詞）は「魅惑的で食えない」路線。露骨な性的表現は入れない（全年齢）。
