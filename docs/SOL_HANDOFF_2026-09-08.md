# Sol への引き継ぎ（2026-09-08・Opus → Sol／司令塔）

短くまとめる。細部は `HANDOFF.md` 第0節の先頭に日付順で全部ある。

---

## 1. まず知ってほしいこと

**今日やったことは全部「伝わるか」のための仕事で、まだ一度も遊ばれていない。**
試遊の予定はあるが未実施。したがって**ここから先を測定だけで積むと精度が落ちる**。

オーナーの直近の試遊での言葉:
> 火力は感じられた。問題は、何が起点で、なぜ次へ連鎖し、どうすれば意図的に伸ばせるのかが
> ほぼ伝わらないこと。「よく分からないけどつながった。これでいいのか？」という感覚になる。

今日の作業は全部これへの回答であって、**回答が当たったかは未検証**。

---

## 2. ブランチ

| ブランチ | HEAD | 状態 |
|---|---|---|
| `claude/game-handoff-chain-ui-y3nmsx` | `a8f8252` | Opus の作業ブランチ。**最新** |
| `claude/owner-playtest-tuning-3vr0tt` | `5606921` | 正ブランチ。**6コミット遅れている** |

**未送信の6コミット**: B1 / U2 / D1 / R3+U3 / R2 / Geminiマージ。
早送りできる（衝突なし）。**送るかどうかはオーナー判断で、まだ送っていない。**

テストは**全緑**（Node 53本・ブラウザ52本）。着手前に必ず確認すること。

---

## 3. 今日入れたもの（1行ずつ）

- **勝利ファンファーレ**: 原因3つを実測で切り分けて修正。合成音が実録WAVより23dB下・
  決着後の「飛ばす」が自分で音を切っていた・BGMが鳴っている上に重なっていた。
- **連鎖の見取り図**（編成画面）: 略奪連鎖1本だけ。起点 → 金貨獲得 → 反応 → 回り続けるか。
- **答え合わせ**（戦闘中・1回だけ）: 「《追い剥ぎ》の金貨に、《強欲》が反応した」
- **戦果の1文**: 「グルグの追い剥ぎを受け、ボルが追撃した。衛兵を撃破した」
- **モルモの反応**: 同じ事実から別の声で。「グルグ殿の集金で、ボル殿まで走り出しましたヨ！？」
- **魔界史の記憶**: ランで一番遠くまで届いた出来事1件。
- **前回出撃との差分**: 「今回動かした人が、こう働いた」に見出しが変わる。

全部**同じ1つの事実（`lastBattle.spotlight`）**から作っている。言い方の管理は
`UI.spotlightText()` と `MormoScene.spotlightLine()` の2か所だけ。増やさないこと。

---

## 4. 判断が要る3つ

### A. 未送信6コミットを正ブランチへ送るか
早送りで済む。送れば試遊できる。**私は送って試遊を推す。**

### B. ハプニング条件のV2切替
Gemini が18,000ランでA/B測定済み（`docs/CHAIN_V2_HAPPENING_GATE_MEASURE_2026-09-08.md`）。
勝率差 +0.02pt、実発火数 91→91件で完全一致。**遊びの体感は変わらない。**

切り替える理由は遊びではなく**二重基準の解消**（倍率はV2なのにハプニング条件だけraw）。
放置すると次にCHAINを触る人が毎回どちらの段数かを確かめることになる。急ぎではない。
オーナーの現指示は「**まだ切り替えないでください**」。解除されるまで触らない。

### C. `chain_stagefright`（連鎖に出遅れ）が死んでいる
18,000ラン中 成立0件・発火0件。`duringChain` で追加行動できる味方が `greedy` だけなので、
`!greedy && coward` を満たす者が判定機会に入れない。**誰も持てない事件。**
以前消した「回復の祈り」と同じ型。消すか、届く距離へ寄せるか。私は**寄せる**を推す
（事件は接続数を増やす材料なので）。

---

## 5. 設計書のチケット残り

`docs/BUILD_EXPERIENCE_DESIGN_2026-09-06.md` 第9節の表。

| ID | 状態 |
|---|---|
| V0〜V3 / U1 / B1 / U2 / D1 / R2 / R3 | **完了** |
| R1（初回の応募補助） | **要否は試遊で判断**と設計書が指定。測定では決められない |
| U3の残り（ツケの元選択表示） | 小粒・未着手 |
| P1（初見試遊）→ E1（敵の相性） | **試遊が入口** |

**試遊を待たずに進められるものは、ほぼ尽きている。**

---

## 6. 保留中（触らないこと）

`claude/game-core-fun-discussion-g3km6r`（未マージ・15コミット）。
三部門廃止・階級1段・給与2択・傭兵市場削除・孤児コード削除・深追い/深淵のバグ修正。

**オーナーが「Astra設計の修正プロジェクトが終わるまで保留」と明言済み。**
正ブランチはその後56コミット進んでおり、意味の衝突がある
（このブランチが消した `departmentRoster("construction")` の上に、墓地・食事・施設が乗っている）。
一括マージは非推奨。詳細は `HANDOFF.md`。

---

## 7. 守ってほしい規律

- 着手前に `git pull`。1タスク＝1コミット＝即push。
- データを触ったら `node tools/sim.js 50`、UIを触ったら `sh tools/browser-tests/run-all.sh`。
- **「音を生成したか」だけを測るテストを書かない**（実機の無音を見逃す・落とし穴3-7）。
  今日この落とし穴を実際に踏んだ。`tools/browser-tests/fanfare.js` が正しい測り方の見本。
- **証拠が無ければ書かない。** spotlight 系は全部この原則で作ってある。
  推測で1文を出すと「変えたから勝った」という反実仮想になる。
- 契約を変えたら `HANDOFF.md` 第2節とコミットメッセージへ明記。
