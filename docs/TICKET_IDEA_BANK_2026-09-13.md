# チケット G（CodeX・案出しのみ）：モンスター種族・敵・技のたたき台

目的：次に足す中身の「候補の山」を先に作る。**採用は後でオーナーが選ぶ**。ここでは実装もデータ追加もしない。
絵は「選ぶための下絵」であって完成品ではない。

## 前提（読むだけ）
- `src/data/monsters.js`（味方種族17）、`src/data/enemies.js`（敵・役8種・地域）、`src/data/skills.js`＋`src/data/skills_catalog.js`（技17＋上位8＋カタログ31。kind の一覧は `docs/SPEC_SKILLS_2026-09-12.md`）
- `docs/DESIGN_ACT2_2026-09-11.md`（第二幕の隣国）、`docs/DESIGN_ECONOMY_2026-09-12.md`（裏方の職業）
- 画風：`assets/battle/units/*/idle.webp`（太いインク、くすんだマット、90年代ベスティアリ）
- 設計の憲法：**難しくしない**（中高生にも）。効果は一文で言える。既存の kind の組み合わせで作れる技を優先し、新しい kind は「どうしても」の場合だけ1〜3個。

## 出してほしいもの（`docs/IDEA_BANK_2026-09-13.md` 1本＋下絵）

1. **味方の新種族 15案**：名前／一言の性格／役目（前衛・遠隔・支援・裏方のどれか）／種族技1本（kind と一文）／面接で言いそうな台詞1本／どの地域で応募してきそうか。
   既存17種と役目が被るものは「何が違うか」を一行で。
2. **敵キャラ 20案**：名前／どの段階・地域（第一幕8段階・第二幕の隣国）／役（既存8役のどれか、または新役の提案）／行動の癖1本。
   ボス格（各幕の節目）を5体は含める。
3. **技 30案**：名前／誰向け（種族技・上位技・敵の行動）／kind／気合コスト／一文／台詞1本。
   既存カタログ31と重なるものは出さない。**新 kind は最大3つ**まで、必要なら理由を書く。
4. **下絵**：新種族15と敵20の**一覧シート**（1枚に9〜12体並べたサムネイル、3〜4枚）。1体ずつの完成絵は描かない。
   既存の `*-review.jpg` と同じ形式でよい。オーナーが「これとこれ」と指せるのが目的。
5. **おすすめ**：全体から「まず入れると遊びが広がる」種族3・敵5・技10を選んで理由を一行ずつ。

## 規則
- 文書と下絵シート3〜4枚だけ。`src/` `assets/battle/units/` は触らない。ブランチ `codex/idea-bank`（`claude/hero-arrival-tavern-prototype-uy2toh` から切る）。1コミット。
- 名前は中高生が読める言い方。ダジャレ・ネタは1割まで。
- 既存の魔王軍の色（「人を取らない」「裏方に居場所」「くすっと笑える」）を守る。

## 貼り付け用（オーナー → CodeX）
```
docs/TICKET_IDEA_BANK_2026-09-13.md を読んで、新種族15・敵20・技30の案を docs/IDEA_BANK_2026-09-13.md に書いて。
一覧のサムネイルシート3〜4枚（1枚に9〜12体）を assets/battle/units/ の外、docs/idea-bank/ に置いて。個別の完成絵は描かない。
ブランチ codex/idea-bank（claude/hero-arrival-tavern-prototype-uy2toh から切る）。src/ と assets/battle/units/ は触らない。1コミット。
```
