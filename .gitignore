# playwright を入れるとここに落ちる（npm install --no-save でも .bin は残る）
node_modules/
package-lock.json

# ブラウザテストが吐くスクリーンショット。中身は毎回変わるのでコミットしない。
# （assets/monsters/*.png は成果物なので追跡対象のまま）
.screenshots/
/*.png

# SP 未設定でテストを個別に走らせたときの取りこぼし先。
# 実体は .screenshots へ寄せたが、再発してもコミットされないようにしておく。
undefined/
